# 同行方案借鉴分析

> 分析对象：`http://47.98.251.194`（同组同学部署的「问数星途」实现）
> 对照对象：本项目 `wenshu-fullstack`
> 分析日期：2026-09-15
> 取证方式：HTTP 拉取其全部前端产物（9 个页面片段 + `xingtu-api.js`）后逐段阅读，配合 Playwright 实测

---

## 一、结论速览

**先说结论：他的工程架构不如你，但他的交互细节和"教学可视化"值得抄。**

| 维度 | 本项目 | 同行方案 | 谁更强 |
|---|---|---|---|
| 后端架构 | routes / services / repositories / middleware 分层，60+ 模块 | 单文件 NGINX 托管的若干 `/api/*`，仅 7 个接口 | **本项目** |
| Agent 能力 | 12 个 tool calling（讲题/变式/归因/报告/计划…）+ guardrail + blocklist + memory | 无 Agent 工具层，只有模板化问答 | **本项目** |
| 账号体系 | 完整登录注册 + 角色权限（学生/运营） | 只有一个体验登录，无注册 | **本项目** |
| 代码组织 | 16 个前端脚本 + 3 个 CSS，模块化 | 1 个 4003 行脚本 + 静态页打补丁 | **本项目** |
| 语音方案 | 浏览器 `speechSynthesis` / `SpeechRecognition`（依赖 HTTPS） | **服务端 TTS + 服务端 ASR**，HTTP 下也能播 | **同行** |
| 讲题引导 | 有步骤文案 | **遮罩高亮 + 气泡 + 可点步骤的引导系统** | **同行** |
| 教学可视化 | 有图形 | **数形结合：边长画点、星尘倾倒动画** | **同行** |
| 界面精致度 | 已有宇宙主题 | 手绘星球插画 + M3 语义配色 + 粒子特效 | **同行（局部）** |

---

## 二、值得借鉴清单（按性价比排序）

### ★★★ 优先级 1：服务端 TTS + 预取 + 三级降级

**他怎么做**（`xingtu-api.js:22-202`）：

```js
function ttsSrc(text, voice) {
  return "/api/tts?text=" + encodeURIComponent(text) + "&v=29"
       + (voice ? "&voice=" + encodeURIComponent(voice) : "");
}
```

三层设计：

1. **预取缓存**（`:31-40`）：进入页面前 `fetch` 一次，把 `ArrayBuffer` 存进 `window.__xingtuTtsBuf[text]`，
   播放时用 `URL.createObjectURL(new Blob([cached]))` 直接出音，**零延迟**。
2. **token 失效机制**（`:99-142`）：每次播放 `__xingtuVoiceToken += 1`，回调里比对 token，
   不匹配就直接 return —— 彻底解决"快速连点后旧语音的回调把新状态覆盖掉"的竞态。
3. **三级降级链**：
   - `new Audio(url)` 播放服务端 MP3
   - 失败 → WebAudio `decodeAudioData` + `createBufferSource` 播
   - 再失败 → `speechSynthesis` 浏览器本地合成

**为什么值得抄**：你的 `voice.js` 现在**只有第 3 级**（浏览器内置），在
`http://` 下、或用户关了系统语音包时直接哑掉。加了服务端 TTS 后，
**断网以外任何情况都能出声**，而且音色统一（不依赖用户电脑装了什么声音）。

**落地成本**：后端加一个 `/api/tts` 路由（对接任意 TTS 云服务或本地 edge-tts），
前端把 `voice.js` 包一层降级链即可。

---

### ★★★ 优先级 2：讲题步骤高亮引导系统

**他怎么做**（`blackboard.html` 内嵌脚本）：每个页面配一份步骤表：

```js
STEPS = [
  { sel:'#grid-a', extraSel:'#grid-b', text:'左边这两个小正方形里，装着金黄星尘和青蓝星尘…' },
  { sel:'#hold-to-speak-btn', text:'在线学习过程中，有什么问题都可以及时和小问沟通哦！' },
  { sel:'button.quick-chip[data-msg*="刚好"]', text:'为了快速体验，可以点击这个语言快捷键哦！' },
  { sel:'#pour-stardust-btn', text:'点一下「倾倒星尘」，把两种星尘倒进大正方形…' },
  { sel:'#advance-step-btn', clickable:true, text:'讲解完成啦，我们一起进入实战练习吧！' }
];
```

配套机制：

- **遮罩 + 气泡**：`.xg-overlay` 铺满全屏，`.xg-bubble` 跟随高亮元素定位
- **高亮画法**：`outline: 3px solid #FF8C42` + `box-shadow: 0 0 0 8px rgba(255,140,66,.4)` + 脉冲动画
- **非当前步骤锁死**：`cur.style.pointerEvents = 'none'`，只能点当前那一个 —— 强制按顺序走
- **气泡自动避让**：算 `getBoundingClientRect().bottom + 14`，若 `+160 > window.innerHeight` 就翻到元素上方
- **可点步骤**：`clickable: true` 时允许交互并 `scrollIntoView({block:'center'})`
- **进度持久化**：`sessionStorage.setItem('xingtu_blackboard_guide_s1','1')`，看过就不再弹

**为什么值得抄**：你之前提的"完善讲题步骤"，他这套就是**现成的答案**——而且比纯文字步骤强得多，
因为它把"讲解"变成了"手把手带着点"。建议直接把 `STEPS` 数据结构 + overlay/bubble 机制搬过来，
再喂你自己 Agent 生成的步骤文案。

---

### ★★★ 优先级 3：数形结合的几何可视化

**他怎么做**（`:3067-3181`）：

```js
function svgSideDots(x1, y1, x2, y2, n, color) {
  let html = "";
  for (let i = 1; i <= count; i++) {          // 在一条边上均匀画 n 个圆点
    const t = i / count;
    html += '<circle cx="' + (x1+(x2-x1)*t).toFixed(1) + '" cy="' + ... + '" r="4"></circle>';
  }
}
```

- `a` 边画 **a 个金黄点**，`b` 边画 **b 个青蓝点**，斜边用 `stroke-dasharray="8 8"` 渐变虚线 + `animate-pulse`
- 图形随题目参数动态重算：`viewBox` 按 `scale = Math.min(300/bb, 132/aa)` 自动缩放
- 直角标记、三个顶点彩色圆点、`hypoCard` 按百分比定位贴在斜边中点
- 讲勾股定理时用「9 块金黄星尘 + 16 块青蓝星尘 → 倒进斜边大正方形」的**动画隐喻**
  （`#pour-stardust-btn`），把 `a²+b²=c²` 变成看得见的"倒积木"

**为什么值得抄**：你的 Agent 能生成讲题步骤和变式题，但**缺"画出来"这一步**。
把 `svgSideDots` 这种"按数量画点"的思路接进你的讲题页，
每道几何题都能自动生成**数格子的配图**，这正是"生成对应图片"最轻量可靠的实现
（比调文生图模型稳定得多，还不花钱）。

---

### ★★ 优先级 4：按住说话 → 服务端语音识别

**他怎么做**（`:1348-1547`）：

```
pointerdown → getUserMedia({audio:true}) → createScriptProcessor(4096,1,1) 收 PCM
pointerup   → mergeChunks → resample(输入采样率 → 16000) → encodeWav(16bit 单声道)
            → fetch('/api/speech', { method:'POST', headers:{'Content-Type':'audio/wav'}, body: blob })
            → { ok, text, message }  → text 直接当成聊天输入
```

细节到位的地方：

- `createGain()` 并把 `gain.value = 0`，把录音链路接到 destination 上**静音输出**
  （部分浏览器不接 destination 就不触发 `onaudioprocess`）
- 手写 `encodeWav`（`:1416-1441`）：44 字节 RIFF 头 + PCM16，不依赖任何库
- 时长校验：`elapsed < 400ms` 直接提示"请按住说话"
- 优雅降级：`navigator.mediaDevices` 不存在时提示"当前浏览器不能录音，请切换键盘输入"
- `setPointerCapture` + `contextmenu` 屏蔽，移动端长按不会弹菜单

**注意**：`getUserMedia` **要求安全上下文**，所以这套在 `http://47.98.251.194` 上其实也**用不了**
（他代码里那句降级提示就是为这准备的）。**它的价值在于：一旦你上了 HTTPS，
服务端 ASR 比浏览器 `SpeechRecognition` 兼容性好得多**（后者基本只有 Chrome 能用）。

---

### ★★ 优先级 5：五维能力雷达图

**他怎么做**（`:2402-2428`）：SVG `viewBox="0 0 300 300"`，5 条轴的角度硬编码：

```js
const axes = [[150,30],[264,113],[221,247],[79,247],[36,113]];
const pts = axes.map((xy, i) => {
  const t = Math.max(0.12, Math.min(1, Number(scores[i] || 0) / 100));   // 下限 0.12 防止缩成一点
  return [150 + (xy[0]-150)*t, 150 + (xy[1]-150)*t];
});
poly.setAttribute("points", pts.map(p => p.toFixed(1)+","+p.toFixed(1)).join(" "));
```

**值得抄的点**：`Math.max(0.12, …)` 这个**下限保护**——0 分时图形不会塌成一个点，
视觉上仍然是个五边形，观感好很多。

**你现在**：雷达图已有（`report.js`），可以对照加下限保护 + 顶点圆点跟随。

---

### ★★ 优先级 6：草稿纸（Canvas 手写）

**他怎么做**（`:3305-3480`）：`paintScratchFigure(host, a, b)` 在 canvas 上先画题目图形作为**底图**，
再 `wireScratchpad` 叠一层可手写画布，`pointerdown/move/up` 记录轨迹。
`sizeScratchCanvas` 按 `devicePixelRatio` 缩放避免模糊。

**值得抄的点**：**"题目图形当底图 + 上面手写"** 这个组合。
他讲题时反复说"先去草稿纸上算一下"，把草稿纸和图形放在一起，
比自己另开一块空白画布更贴合解题场景。

---

### ★★ 优先级 7：陪学宠物 + 亲密度 + 粒子特效

**他怎么做**：

- `wireDesktopPet`（`:3598-3664`）：点宠物头 → 冒 `+1` 和 `✦` 粒子（`burstPetFx`，8 个方向飞散，
  CSS keyframes 用 `--dx/--dy` 变量控制方向），同时弹鼓励语
- `PET_CHEERS`（`:3533-3540`）：6 句鼓励文案轮播（"加把劲！我们很快就拿下啦"）
- 亲密度：`renderAffection(n)` 写 `亲密度 +n`，`>= 100` 换成 `pet-max.png` 形象

**注意**：`getAffection()` 里 **`return 0;` 是写死的**——这块他只做了皮，没接数据。
**你抄的时候要做真的**：把亲密度接到你已有的 gamificationService 上。

---

### ★★ 优先级 8：专注计时器 + 护眼/夜航模式

**他怎么做**（`:2059-2155`）：

- `sessionStorage` 存当天起始时间，`renderFocus()` 每分钟更新"今日星空专注: N 分钟"
- 提醒阈值可选 `[10,15,20,25]` 分钟，存 `localStorage`，点标题弹选择器
- 进度条 `shimmer-bar` 宽度 = `elapsedMin / reminder * 100%`
- 到点 `toast("休息一下，眺望远方")` + `playVoice(...)`（**语音+视觉双通道提醒**，很贴心）
- 全局"夜航/护眼模式"切换（`dark` class）+ 文案"柔和睡前微光（蓝光过滤中）"

**为什么值得抄**：你的用户画像里有"7-14 岁"，**防沉迷 + 护眼**是家长最买账的功能点，
且实现成本极低（纯前端 + 两个 storage）。

---

### ★ 优先级 9：VIP 套餐 + 结算弹窗

**他怎么做**（`:2206-2345`）：`plans = { monthly:{...}, yearly:{...} }` 两份文案（标题/原价/优惠/实付），
`selectPlan` 切套餐并同步所有价格节点，`openCheckout` 弹"立即支付/确认协议并开通特权"弹窗，
成功后 `status.textContent = plans[current].status` 并 `go("/home")`。

**注意**：他的支付是**纯前端假成功**（没有任何后端扣款）。
你第 12 轮已经做了真的钱包/星钻体系，**这块你比他强，不用抄**；
唯一可抄的是**"套餐对比 + 价格锚点（原价划线）+ 结算弹窗"的呈现方式**。

---

### ★ 优先级 10：Material Design 3 语义化调色板

他的 `tailwind.config` 用的是标准 M3 命名：

```js
colors: {
  "primary": "#8d4b00",  "on-primary": "#ffffff",  "primary-container": "#b15f00",
  "surface": "#f9f9ff",  "surface-container": "#e7eeff",  "surface-container-high": "#dee8ff",
  "on-surface-variant": "#554336",  "outline": "#887364",  "outline-variant": "#dbc2b0",
  "inverse-surface": "#263143",  "inverse-on-surface": "#ecf1ff",  "surface-tint": "#904d00"
}
```

`on-*` / `-container` / `inverse-*` / `surface-*` 这套命名是 M3 的核心价值：
**"内容色"和"背景色"成对出现**，做浅色/深色双模式时不会出现"深色底上写深色字"的经典事故。

**你现在**：`common.css` 里是 `--color-primary: #2BA4A0` 这类零散变量（第 13 轮才从 Tailwind 语法修正成真实 `:root`）。
建议**按 M3 思路补齐语义对**（`--color-on-surface` / `--color-surface-container` 等），
但这属于锦上添花，优先级排最后。

---

## 三、反面教材（这些**不要**抄）

| 他的做法 | 具体证据 | 为什么不要抄 |
|---|---|---|
| **按中文文本定位 DOM** | `[...document.querySelectorAll("span")].find(el => el.textContent.trim() === "错题点亮闭环")` | 改一个字就全崩；无法 i18n；性能差（全文档遍历） |
| **按 Tailwind class 判断状态** | `guestOn.className.indexOf("bg-primary-container") >= 0` 判断"游客 Tab 是否选中" | 改样式即改逻辑，极其脆弱 |
| **单文件 4003 行** | `xingtu-api.js` 166 KB 一个 IIFE | 无法复用、无法测试、merge 必冲突 |
| **`document.write` 注入页面** | 外壳里 `document.open(); document.write(html); document.close();` | 导致同一份脚本被注入两次，触发 `Identifier 'quoteIndex' has already been declared`；且阻塞渲染 |
| **全局 `window.__xingtu*` 满天飞** | `__xingtuVoices` / `__xingtuTtsBuf` / `__xingtuVoiceToken` / `__xingtuBufferSource` … | 命名污染 + 无法回收 + 调试困难 |
| **硬编码** | `viewBox="0 0 300 300"` 靠这个字符串反查雷达图 SVG；颜色 hex 散落在字符串拼接里 | 一改设计就找不到 |
| **用隐藏/剥离函数打补丁** | `hideRemediateCornerstones` / `stripDemoPour` / `hideCanonCard` / `hideRemediateExtras` | 说明 HTML 与逻辑不同源，靠运行期"擦掉不想要的部分"；页面会闪 |
| **`viewport` 写死 1280** | `<meta name="viewport" content="width=1280">` | 手机上只能整体缩放，不是真响应式 |
| **假数据留桩** | `function getAffection(){ return 0; }` | 上线了还是 0 |
| **无 HTTPS、Cookie 无 HttpOnly** | `Set-Cookie: learner_id=…; SameSite=lax`（无 HttpOnly/Secure） | 会话可被 JS 读取；且麦克风/剪贴板等能力全部被浏览器禁用 |

**根因判断**：他这套是 **「AI/设计工具生成的静态页 + 一个巨型运行期补丁脚本」**，
不是"写代码"，而是"给已有 HTML 打补丁"。所以才有大量 `findByText` / `hideXxx` / 按 class 判断状态。
这条路**起步快、看着炫，但不可维护**。你的分层架构是对的方向，**不要为了抄效果而退回他的写法**。

---

## 四、两边能力对照（要抄就抄这些）

| 能力 | 本项目现状 | 同行现状 | 建议 |
|---|---|---|---|
| 服务端 TTS | ❌ 只有浏览器内置 | ✅ `/api/tts?text=&voice=` | **抄** |
| TTS 预取缓存 | ❌ | ✅ `__xingtuTtsBuf` | **抄** |
| 播放竞态 token | ❌ | ✅ `__xingtuVoiceToken` | **抄** |
| 语音三级降级 | ❌ 一级 | ✅ 三级 | **抄** |
| 服务端 ASR | ❌ | ✅ `POST /api/speech`（wav） | **抄**（需 HTTPS 才有意义） |
| 前端录音+重采样+WAV编码 | ❌ | ✅ `:1398-1441` | **抄** |
| 讲题步骤高亮引导 | ⚠️ 只有文字 | ✅ overlay+bubble+STEPS | **抄** |
| 边长画点的数形结合 | ✅ 已落地 | ✅ `svgSideDots` | 已抄（`sideDots()` 打点 + `cellGrid()` 把正方形切成 9/16/25 个可数的小方格） |
| 图形随参数自适应 | ⚠️ | ✅ 动态 `viewBox` | **抄** |
| 草稿纸画布 | ❌ | ✅ canvas | **抄** |
| 雷达图下限保护 | ⚠️ | ✅ `Math.max(0.12, …)` | **抄（小改）** |
| 专注计时+护眼 | ✅ 已落地 | ✅ 完整 | 已抄（`focus.js` 四档环形倒计时 + 到点提示/语音/粒子；护眼双主题早已有） |
| 宠物粒子互动 | ⚠️ 有形象 | ✅ 粒子+文案轮播 | **可抄** |
| 亲密度 | ❌ | ⚠️ 有皮无数据 | 自己实现 |
| M3 语义化调色板 | ⚠️ 零散变量 | ✅ 完整语义对 | 可选 |
| VIP 真实扣款 | ✅ 星尘/星钻钱包 | ❌ 纯前端假成功 | **你更强，不抄** |
| Agent 工具层 | ✅ 12 个 tool | ❌ 无 | **你更强，不抄** |
| 账号/注册/权限 | ✅ 三角色 | ❌ 只有体验登录 | **你更强，不抄** |
| 分层架构 | ✅ routes/services/repos | ❌ 单文件补丁 | **你更强，不抄** |

---

## 五、落地建议（按顺序做）

**第一步（收益最大，1-2 天）**
1. 后端加 `/api/tts`，前端 `voice.js` 包上「预取缓存 + token + 三级降级」。
2. 讲题页接入 `STEPS` 引导系统（遮罩 + 气泡 + 高亮 + 可点步骤 + sessionStorage 记忆）。

**第二步（强化"讲题"这个核心卖点，2-3 天）**
3. 几何题动态作图：把 `svgSideDots`「按数量画点」的思路接进讲题页，让每道几何题自动配图。
4. 图形参数自适应（动态 `viewBox` + `preserveAspectRatio`）。
5. 草稿纸 canvas 叠在题目图形上。

**第三步（体验与留存，2-3 天）**
6. 专注计时器（10/15/20/25 分钟，语音+toast 双提醒）+ 护眼/夜航模式。
7. 宠物亲密度：粒子特效 + 鼓励文案轮播，**接到真实的 gamificationService**。
8. 雷达图加下限保护。

**第四步（可选）**
9. 把 `common.css` 的颜色变量按 M3 语义对补齐。
10. 上 HTTPS（自签或域名 + 证书），届时服务端 ASR 与麦克风才能真正可用。

---

## 六、备注

- 同行站点的路由是 `/landing /login /home /path /blackboard /practice /remediate /profile /vip`，
  与你本项目的 `/pages/student/*.html` 不同源，**是同一品牌的两套独立实现**，不是你的部署产物。
- 他的页面产物合计约 **376 KB HTML + 166 KB JS ≈ 542 KB**，且全部是未压缩的运行期 DOM 补丁逻辑。
- 本文档中所有行号均指从其站点拉取的 `xingtu-api.js`（4003 行 / 165,806 字节）。