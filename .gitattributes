# 换行符策略
# 这个仓库的代码最终要部署到 Linux（云服务器 / 容器），所以 shell 脚本
# 必须是 LF —— 一旦带上 CRLF，Linux 上跑 `bash tools/deploy-server.sh`
# 会直接报 `$'\r': command not found`。Windows 上默认可能是 CRLF，
# 这里用属性文件强制锁死，避免「本机能跑、服务器上报错」。

*.sh        text eol=lf
*.bash      text eol=lf

# 二进制资源不做任何行尾 / 编码转换
*.png       binary
*.jpg       binary
*.jpeg      binary
*.gif       binary
*.webp      binary
*.ico       binary
*.woff      binary
*.woff2     binary
*.ttf       binary
*.otf       binary
*.pdf       binary
*.zip       binary