# 问数星途 · 部署文档

> 目标：把 `http://localhost:3000` 变成别人也能打开的地址。
> 先看第 0 节的「上线前必改」，再看选哪个方案。**推荐先做方案 A**（局域网，5 分钟，不用服务器不用账号）。

## 路线对照：先看这张表

| 路线 | 代码跑在哪 | 别人怎么访问 | **自己电脑关机后还能开？** | 成本 | 适合 |
|------|-----------|--------------|--------------------------|------|------|
| **A 局域网**（第 1 节） | 你自己电脑 | 同一 WiFi 的 `http://192.168.x.x:3000` | ❌ 关机就没了 | 0 元 | 课堂 / 宿舍内部演示 |
| **B 内网穿透**（第 2 节） | 你自己电脑 | 隧道工具给的 `https://xxx` | ❌ 关机就没了 | 0 元 | 给外校同学看一眼 |
| **C 云服务器**（第 3 节）= **路线二** | 一台云主机 | `https://你的域名` | ✅ 可以 | 约 10~40 元/月 | 正式交付 / 长期开放 |
| **D 免费托管**（第 4 节）= **路线一** | 托管平台 | 平台给的 `https://xxx.onrender.com` | ✅ 可以 | 0 元（有配额与休眠） | 先白嫖一个公网地址 |

> **只有 C（路线二）和 D（路线一）能做到「自己电脑关机后网站还能打开」** ——
> 因为代码得跑在别人的机器上。A / B 的本质是「你的电脑就是服务器」。

**两条正式路线的取舍**

- **路线一（第 4 节 · 免费托管）**：把仓库推给平台，平台帮你构建、运行、发 https。**不用买机器、不用连服务器敲命令**。代价是免费档会休眠（十几分钟没人访问就睡，下次打开要等几十秒冷启动），而且数据存在内存里，**平台一旦重启就回到初始演示数据**。
- **路线二（第 3 节 · 云服务器）**：自己租一台云主机，配置一次长期稳定，域名、HTTPS、端口全归你管，进程也能安心常驻。代价是要花点钱（学生认证后约 10 元/月量级），以及得敲几条 Linux 命令（项目已带一键脚本）。

---

## 0. 上线前必改（漏了会出事）

| # | 事项 | 为什么 | 怎么做 |
|---|------|--------|--------|
| 1 | 换掉 `JWT_SECRET` | 默认值写在代码里（`wenshu_dev_secret_key_2025`），知道它就能伪造任意用户的登录态 | 在 `backend/.env` 里设 `JWT_SECRET=<随机串>`：`node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"` |
| 2 | 保护大模型 Key | `backend/.env` 里有真实的 DeepSeek `LLM_API_KEY`。公开部署后任何人都能通过 `/api/agent/chat` 消耗你的额度 | 别把 `.env` 提交到 Git（已加进 `.gitignore`）；线上设充值上限；或先设 `AGENT_PROVIDER=rules` 走离线规则引擎 |
| 3 | 改演示账号密码 | `13800000001/123456`、`admin/admin123` 是公开的 | 正式对外前在 `backend/src/models/mockData.js` 里改掉，或限制注册入口 |

另外 `NODE_ENV` 对外时应设成 `production`。

---

## 1. 方案 A：局域网部署（推荐先做这个）

同一个 WiFi / 同一个内网里的其他电脑、手机，直接用 IP 访问。**不需要服务器、不需要账号、不需要外网。**

### 1.1 启动

```powershell
cd "E:\企业实训\代码\新建文件夹\wenshu-fullstack\backend"
npm install        # 第一次才需要
npm start
```

启动横幅里会多出这一行，那个 IP 就是别人要访问的地址：

```
🌐 局域网可访问（同一 WiFi / 内网的其他电脑和手机直接打开）:
     http://192.168.1.23:3000/pages/auth/login.html
```

### 1.2 放行防火墙（Windows 第一次会拦）

如果别的电脑打不开，多半是被 Windows 防火墙挡了。用**管理员** PowerShell 跑一次：

```powershell
New-NetFirewallRule -DisplayName "问数星途 3000" -Direction Inbound -Protocol TCP -LocalPort 3000 -Action Allow
```

### 1.3 注意

- 服务绑在 `0.0.0.0`，所以本机和内网都能访问；手机连同一个 WiFi 也能用。
- **语音唤醒 / 语音播报在 `http://` 下会被浏览器禁用**（浏览器规定麦克风只能在 HTTPS 或 localhost 用）。要演示语音，看第 5 节。
- 换网络（换 WiFi）后 IP 会变，重新看启动横幅即可。

---

## 2. 方案 B：内网穿透（要一个公网链接，但不用买服务器）

本机继续跑，用隧道工具把它映射到一个公网 https 地址。**顺带解决了语音要 HTTPS 的问题。**

| 工具 | 特点 | 命令 |
|------|------|------|
| cpolar | 国内节点，注册免费版够用 | `cpolar http 3000`，返回一个 `https://xxx.cpolar.cn` 地址 |
| ngrok | 国外节点，稳定 | `ngrok http 3000` |
| Cloudflare Tunnel | 免费、需要自有域名 | `cloudflared tunnel --url http://localhost:3000` |

外网受限的机器用不了这类工具；国内演示首选 cpolar。

---

## 3. 方案 C：云服务器（正式部署）= 路线二

### 3.0 先选一台机器

| 选项 | 配置 | 参考价 | 说明 |
|------|------|--------|------|
| 阿里云 / 腾讯云 <br>**轻量应用服务器** | 2 核 2G · 3~5 M 带宽 · Ubuntu 22.04 | 学生认证后约 10 元/月；新用户常规档 60~110 元/年 | 首选，控制台简单，系统镜像直接选 Ubuntu |
| 华为云 / 百度云 轻量 | 同上 | 相近 | 同理 |
| 国外 VPS（Vultr / 搬瓦工 …） | 1 核 1G | 约 $6/月起 | **免备案**，国内访问速度看线路 |

这台机器跑我们这个项目**绰绰有余**：Node 进程常驻内存约 60~90 MB，前端是纯静态文件，没有数据库。

两个注意点：

- **系统选 Ubuntu 22.04 / 24.04**（`tools/deploy-server.sh` 是按这套写的；Debian 11/12 也能跑）。
- **国内服务器挂域名要备案**，没备案只能用 IP 访问 —— 能用，但**语音功能必须有 HTTPS，而 Let's Encrypt 不给纯 IP 签证书**。不想备案就用国外 VPS，或者直接走**路线一（第 4 节）**。

### 3.1 把代码传上去

> ⚠️ **不要传 `node_modules`。** 本地这份是 **pnpm 的软链接结构**（`express` 这些包都指向 `.pnpm/` 里的真身），拷到 Linux 上就是一堆坏链接，服务器上必须重新装。

Windows 本地先打一个干净的上传包：

```powershell
cd "E:\企业实训\代码\新建文件夹\wenshu-fullstack"
powershell -ExecutionPolicy Bypass -File tools\pack-for-server.ps1
# 产出：dist-package\wenshu-fullstack-<时间戳>.zip
# 已自动排除 node_modules / .pnpm-store / .git / frontend\dist / .env（密钥不进包）
```

再传到服务器解压：

```bash
scp dist-package/wenshu-fullstack-<时间戳>.zip root@<服务器IP>:/root/
ssh root@<服务器IP>
apt-get update && apt-get install -y unzip
unzip -q /root/wenshu-fullstack-<时间戳>.zip -d /root/wenshu-src
cd /root/wenshu-src
```

> 想用 `git clone` 也行，但两条都要守住：**别把 `node_modules` 带上去**（理由同上），**确认 `.env` 没被提交**（`.gitignore` 里已经排除了）。

### 3.2 一键部署（推荐直接跑这个）

项目里带了脚本，一条命令装完 Node、依赖、systemd 常驻服务、Nginx 反向代理：

```bash
cd /root/wenshu-src

# 只有 IP：只跑 HTTP，别人用 http://IP 访问
sudo bash tools/deploy-server.sh

# 有域名且已解析到这台机器：顺带申请 HTTPS
sudo bash tools/deploy-server.sh math.example.com

# 部署时就把大模型 Key 带上（不传 = 走离线规则引擎，功能不受影响）
sudo LLM_API_KEY=sk-xxxxxxxx bash tools/deploy-server.sh math.example.com

# 顺便给 certbot 留个邮箱（不传则用 --register-unsafely-without-email）
sudo CERT_EMAIL=me@qq.com bash tools/deploy-server.sh math.example.com
```

脚本每一步都有中文提示，它做这些事：

| 步骤 | 做了什么 |
|------|----------|
| 1 | 装 `curl / ca-certificates / gnupg / tar`，用 NodeSource 源装 **Node 20**；拉不到源就退回系统自带（**≥18 就能跑**，脚本会校验） |
| 2 | 把代码同步到 `/opt/wenshu`（排除 `node_modules` / `.pnpm-store` / `.git` / `frontend/dist`） |
| 3 | 生成 `/opt/wenshu/backend/.env`：**自动签发 64 位随机 `JWT_SECRET`**、`NODE_ENV=production`、`LLM_API_KEY` 取环境变量（不传则留空 = 离线规则引擎），权限 `600` |
| 4 | `npm install --omit=dev`；失败自动换淘宝镜像重试，最后校验 `node_modules/express` 确实装上了 |
| 5 | 写 systemd 服务并设为**开机自启**（`Restart=always`、`User=www-data`、`ProtectSystem=full`） |
| 6 | 写 Nginx 站点（**已带 `proxy_buffering off`**，流式回复才能逐字出），移除默认站点，放行本机 ufw / firewalld 的 80 / 443 |
| 7 | 传了域名就装 certbot 申请证书并自动 301 跳 HTTPS；跑完打印健康检查结果和常用运维命令 |

跑完会打印类似：

```
[v] 服务已启动并设为开机自启
[v] Nginx 已加载新配置
============================================================
部署完成
健康检查 : {"status":"ok","env":"production","db":"memory","cache":"memory","llm":"rules",...}
对外地址 : http://1.2.3.4/pages/auth/login.html
```

### 3.3 手动部署（不想用脚本 / 想搞懂每一步）

<details>
<summary>点开看逐条命令</summary>

**① 装 Node**（Ubuntu 22.04 自带的 node 只有 12，太老）

```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs
node -v      # 应该是 v20.x
```

**② 装依赖**

```bash
cd /opt/wenshu/backend
npm install --omit=dev
```

**③ 生成 .env**

```bash
cp .env.example .env
# 生成一个随机密钥（生产环境密钥不足 32 位，后端会直接拒绝启动，这是故意的护栏）
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
nano .env     # 填 JWT_SECRET=上面那串；确认 NODE_ENV=production
```

**④ systemd 常驻**

```bash
sudo tee /etc/systemd/system/wenshu.service >/dev/null <<'EOF'
[Unit]
Description=WenShu XingTu
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/opt/wenshu/backend
EnvironmentFile=/opt/wenshu/backend/.env
Environment=NODE_ENV=production
ExecStart=/usr/bin/node /opt/wenshu/backend/src/app.js
Restart=always
RestartSec=3
NoNewPrivileges=true
PrivateTmp=true

[Install]
WantedBy=multi-user.target
EOF

sudo systemctl daemon-reload
sudo systemctl enable --now wenshu
sudo systemctl status wenshu
```

**⑤ Nginx 反向代理**

```nginx
server {
    listen 80;
    server_name your-domain.com;      # 只有 IP 就写 _

    client_max_body_size 4m;          # 头像走 data URL，后端限 2MB，这里留余量

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_buffering off;      # 智能体是 NDJSON 流式返回，必须关缓冲才能逐字出
        proxy_read_timeout 300s;
    }
}
```

```bash
sudo ln -sf /etc/nginx/sites-available/wenshu /etc/nginx/sites-enabled/wenshu
sudo rm -f /etc/nginx/sites-enabled/default
sudo nginx -t && sudo systemctl reload nginx
```

</details>

### 3.4 上 HTTPS（国内服务器需要域名 + 备案）

```bash
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

有了 HTTPS，语音唤醒和语音播报才会生效，而且微信里打开也正常。

> **云服务器控制台的「安全组 / 网络防火墙」记得放行 80 和 443** —— 这一步只能在网页上点，脚本改不了。

### 3.5 日常运维

| 想干什么 | 命令 |
|----------|------|
| 看实时日志 | `sudo journalctl -u wenshu -f` |
| 重启服务 | `sudo systemctl restart wenshu` |
| 填大模型 Key / 改配置 | `sudo nano /opt/wenshu/backend/.env` → `sudo systemctl restart wenshu` |
| 更新代码 | 本地重跑 `pack-for-server.ps1` → 上传解压 → 再跑一次 `sudo bash tools/deploy-server.sh` |
| 看服务健康 | `curl http://127.0.0.1:3000/api/health` |
| 证书续期 | certbot 装完自带 systemd timer，可用 `sudo certbot renew --dry-run` 试跑 |
## 4. 方案 D：免费托管平台 = 路线一

不用买机器、不用连服务器敲命令。把仓库推给 GitHub，平台自己拉代码、构建镜像、跑起来、发一个 https 地址。**自己电脑关机也能开。**

### 4.0 几个平台的差别

| 平台 | 免费档 | 部署方式 | 域名 | 卡点 |
|------|--------|----------|------|------|
| **Render** | 有（Web Service） | 认仓库里的 `Dockerfile` | `https://xxx.onrender.com` | 免费实例闲置约 15 分钟休眠，下次打开冷启动几十秒 |
| **Koyeb** | 有（1 个服务） | 认 `Dockerfile` | `https://xxx.koyeb.app` | 免费档同样会 scale-to-zero |
| **Fly.io** | 有额度（按用量） | `fly.toml` + `Dockerfile` | `https://xxx.fly.dev` | 要装 `flyctl` 敲命令；额度用完要绑卡 |
| **Railway** | 试用额度 | 认 `Dockerfile` | `https://xxx.up.railway.app` | 免费额度已不是长期免费 |
| **微信云托管 / CloudBase（国内）** | 有免费额度 | 需要镜像 | 自定义域名要备案 | 上手步骤更多，但**国内访问快** |

> 这份文档主推 **Render**（步骤最少，仓库里已经放好 `render.yaml` 一键配置）。
> 其余平台吃的是**同一个 `Dockerfile`**，流程大同小异。

### 4.1 Render 部署（主推）

**前置：代码已经在 GitHub 上**

当前仓库：<https://github.com/Ricardo-M-Lw/wenshu111>（`main` 分支）。改完代码推一次即可：

```powershell
cd "E:\企业实训\代码\新建文件夹\wenshu-fullstack"

git status          # ⚠️ 确认列表里没有 backend/.env（里面有真实 DeepSeek Key）
git add .
git commit -m "接入 DeepSeek + 实例保活"
git push
```

`.gitignore` 已经排除了 `node_modules/`、`.pnpm-store/`、`.env`，推上去的是干净的约 6 MB 源码。

**再在 Render 上点几下**

1. 打开 [render.com](https://render.com) → 用 GitHub 账号登录。
2. 点 **New +** → **Blueprint**。
3. 选中刚推的仓库 → Render 自动读到仓库里的 `render.yaml` → 点 **Apply**。
4. 等构建完成（第一次约 3~6 分钟：构建镜像 + 装依赖）。
5. 拿到地址：`https://<服务名>.onrender.com/pages/auth/login.html` —— **从这一刻起，你电脑关机它也能打开。**

`render.yaml` 里已经配好这些，不用手填：

| 配置 | 值 | 为什么这么设 |
|------|-----|--------------|
| `healthCheckPath` | `/api/health` | 后端已实现，返回 `{"status":"ok",...}`；Render 靠它判断服务活着 |
| `JWT_SECRET` | `generateValue: true` | 让平台生成 256 位随机串 —— 后端生产模式**要求密钥 ≥32 位，否则拒绝启动** |
| `AGENT_PROVIDER` | 不设（=自动） | **有 Key 就走 DeepSeek，没 Key 自动降级离线规则引擎**。想强制离线就把文件里那两行的注释打开 |
| `LLM_API_KEY` | `sync: false` | 值不写进文件，Key 不进 Git；在控制台手填（见下） |
| `LLM_MODEL` | `deepseek-flash` | 首选模型；账号里没有这个 id 时会自动回退 `deepseek-chat` → `deepseek-reasoner`，不用改代码 |
| `LLM_DAILY_CAP` | `300` | **全局每日大模型调用上限**：公开地址谁都能调，用满后自动切回离线规则引擎（功能不受影响，只是不花钱了）。`0` = 不限量 |
| `LLM_TIMEOUT` | `45000` | 云端首包慢，超时给宽一点 |
| `PORT` | 不设 | Render 自动注入，后端读的就是 `process.env.PORT` |

**接入 DeepSeek（三步，约 1 分钟）**

1. DeepSeek 开放平台 → **API keys** → 复制 `sk-xxxx`（[platform.deepseek.com](https://platform.deepseek.com)）。
   DeepSeek 后台 → **充值 / 用量** → 设一个**消费上限**（比如 10 元），兜底防刷。
2. Render 控制台 → 选中服务 `wenshu-xingtu` → 左侧 **Environment** → **Add Environment Variable**：
   - Key：`LLM_API_KEY`，Value：`sk-xxxx`
   - 保存 → Render 自动重新部署（约 1~3 分钟）。
3. 验证：登录后打开「智能体」页，发一句「给我一点提示」。
   - 页面上模型标识变成 **deepseek-flash**（或回退后的模型名）= 接通了；
   - 还显示「内置规则引擎」= Key 没生效（检查 `AGENT_PROVIDER` 是否被设成了 `rules`）。
   另外 `GET /api/agent/session` 会返回 `provider.budget`，能看到今天用了多少次、还剩多少。

> 说明：`render.yaml` 是「蓝图」，只在**首次创建服务**时要求你填 `sync: false` 的变量。
> 服务已经建好了，就直接在 **Environment** 里加，改完保存即可，不用重新走 Blueprint。

### 4.2 其余平台

<details>
<summary>Fly.io / Koyeb / Railway 怎么弄</summary>

**Fly.io**（仓库里已经放好 `fly.toml`）

```bash
# 先装 flyctl：https://fly.io/docs/flyctl/install/
fly auth login
fly launch --no-deploy --copy-config --name <全局唯一的名字>   # 名字被别人占了就换一个
fly secrets set JWT_SECRET=<32位以上随机串>                    # 密钥走 secrets，不落在文件里
fly deploy
fly open
```

`fly.toml` 里已经开了 `force_https = true`（语音功能需要安全上下文）和 `/api/health` 健康检查。

**Koyeb / Railway / Zeabur / Sealos**

都是同一套：连 GitHub → 选仓库 → 平台认仓库根目录的 `Dockerfile` → 设环境变量 → 部署。
环境变量同上：`NODE_ENV=production`、`JWT_SECRET=<随机串>`，可选 `LLM_API_KEY`。

</details>

### 4.3 免费档必须知道的三个坑

| 坑 | 现象 | 怎么应对 |
|----|------|----------|
| **实例休眠** | 十几分钟没人访问后睡下，下一次打开要等 30~60 秒 | 演示前先自己打开一次把服务「唤醒」；或者换路线二（云服务器不睡） |
| **内存数据会重置** | 平台重启 / 重新部署后，注册的账号、订单、学习记录全回到初始演示数据 | 演示直接用演示账号；真要持久化见第 7 节 |
| **额度与刷额度** | 免费档有 CPU 时长、带宽、构建次数上限；公开地址可能被陌生人调 `/api/agent/chat` | 后端已有三层护栏：**每个免费账号每天 10 次** → **全站每天 `LLM_DAILY_CAP`（默认 300）次** → 超了自动退回离线规则引擎；再去 DeepSeek 后台设消费上限 |

### 4.4 容器文件系统是临时的

托管平台给的是**临时容器**，写进容器里的文件在重启后都会丢。好消息是这个项目**没有运行时写盘行为**（唯一的 `backend/src/db/dump-sql.js` 是本地离线工具），所以不会踩到；但你要记住这条，以后想「把用户上传的图片存服务器上」时就得改方案：

- 要么挂 **Volume / Disk**（Render Disk、Fly Volumes）
- 要么接**免费的 Postgres / Redis**，见第 7 节
### 4.5 保活：让免费实例不睡（定时 ping）

免费档闲置约 15 分钟就休眠，给别人演示时冷启动要等 30~60 秒。三种保活方式，按推荐排序：

**① 云端保活（推荐，不用开电脑）**：仓库里的 `.github/workflows/keepalive.yml`

- 每 5 分钟从 GitHub 的服务器 ping 一次 `https://wenshu-xingtu.onrender.com/api/health`。
- 生效条件：**推到默认分支 `main` 之后**才会开始跑（定时任务只认默认分支上的 YAML）。
- 看结果：GitHub 仓库 → **Actions** → 左侧「Render 保活」→ 每次运行约 3~10 秒。
- 手动试一次：Actions → Render 保活 → 右上角 **Run workflow**。
- 两个已知限制：
  1. GitHub 的定时任务在高峰期可能延迟几分钟，所以间隔取 5 分钟（Render 是 15 分钟才休眠，留了 3 倍余量）；
  2. 公开仓库连续 **60 天零提交**时 GitHub 会自动暂停定时任务并发邮件提醒，点一下 **Enable** 即可恢复。

**② 外部监控网站（最稳，完全不用改代码）**：[cron-job.org](https://cron-job.org) 或 [UptimeRobot](https://uptimerobot.com)

- 新建一个 HTTP 监控，URL 填 `https://wenshu-xingtu.onrender.com/api/health`，间隔 5 分钟，免费额度足够；
- UptimeRobot 还能顺带发「网站挂了」的告警邮件；
- 可以和 ① 同时开，重复 ping 没什么成本。

**③ 本机脚本（临时演示用）**：`tools/keepalive.ps1`

```powershell
cd "E:\企业实训\代码\新建文件夹\wenshu-fullstack"

# 只探一次（自检）
powershell -ExecutionPolicy Bypass -File tools\keepalive.ps1 -Once

# 一直挂着，每 10 分钟探一次（Ctrl+C 停止）
powershell -ExecutionPolicy Bypass -File tools\keepalive.ps1

# 装成「登录后自动跑」的计划任务（适合经常演示的人）
powershell -ExecutionPolicy Bypass -File tools\keepalive.ps1 -InstallTask
# 不想要了就卸掉
powershell -ExecutionPolicy Bypass -File tools\keepalive.ps1 -RemoveTask
```

> **为什么不用 Render 自带的健康检查保活？** 健康检查只在服务**已经醒着**的时候才跑，救不了已经睡过去的实例；必须由外部定时发请求把它叫醒。

## 5. 语音功能为什么要 HTTPS

浏览器的硬性规定：麦克风（`getUserMedia`）和语音识别（`SpeechRecognition`）只在**安全上下文**可用，也就是 `https://` 或 `localhost`。

- 本机用 `http://localhost:3000` → 语音正常
- 内网用 `http://192.168.x.x:3000` → 语音不可用（按钮会自动隐藏，键盘输入不受影响）
- 公网用 `https://域名` → 语音正常

**局域网演示又想用语音**，可以给内网配自签证书（浏览器会提示"不安全"，手动信任一次即可）：

```powershell
# 管理员 PowerShell：生成一张自签证书（含本机局域网 IP）
$ip = (Get-NetIPAddress -AddressFamily IPv4 | Where-Object { $_.IPAddress -notlike '127.*' -and $_.PrefixOrigin -ne 'WellKnown' }).IPAddress[0]
New-SelfSignedCertificate -DnsName "localhost", $ip -CertStoreLocation "Cert:\LocalMachine\My" -FriendlyName "WenShu Dev"
# 然后在「证书管理器 → 个人 → 证书」里导出为 .pfx，再用一个 https 启动脚本加载它
```

懒得折腾就先用方案 B（穿透工具自带的 https 域名）。

---

## 6. Docker 部署（可选，最省环境问题）

```bash
docker build -t wenshu-fullstack .

docker run -d --name wenshu \
  -p 3000:3000 \
  --env-file backend/.env \
  --restart unless-stopped \
  wenshu-fullstack
```

镜像里已经包含 `frontend/`（后端按 `../../frontend` 定位前端目录，两个目录在镜像里保持并列）。

仓库里另有一份 `docker-compose.yml`，云服务器上更省事（自带 `restart: unless-stopped`、健康检查、日志轮转）：

```bash
cp backend/.env.example backend/.env    # 填好 JWT_SECRET（>=32 位）
docker compose up -d --build
docker compose logs -f
```

> 路线一（第 4 节）的托管平台用的就是**同一个 `Dockerfile`**：Render / Koyeb / Railway 都直接认仓库根目录的它。

---

## 7. 数据会丢：内存 → MySQL

**当前数据全部存在服务端内存里**，进程一重启就回到初始状态（注册的账号、订单、学习记录都会没）。演示够用，给真实用户用必须落库。

迁移路径已经把坑挖好了：

1. `backend/src/db/schema.js` 是 18 张表的唯一事实来源，执行 `cd backend && npm run db:schema` 生成 `schema.sql`。
2. 把这个 SQL 导进 MySQL。
3. 装 `mysql2`，按 `backend/src/db/table.js` 的调用面写一个 MySQL 驱动实现（仓储层 `repositories/*` 不用动）。
4. 在 `.env` 里设 `DB_DRIVER=mysql` 及相关连接参数。

> 这套分层就是为了这一步：业务层只依赖仓储层的接口，换存储不动业务代码。

---

## 8. 排错

| 现象 | 原因 / 处理 |
|------|-------------|
| 别人打不开，本机能开 | Windows 防火墙拦了 → 见 1.2；或不在同一网段 |
| 页面能开但图标都没了 | 之前依赖外网 CDN 的 Lucide，已在本轮改成项目内 `frontend/assets/vendor/lucide.min.js`，确认这个文件在 |
| 字体变成默认黑体 | 字体走 `fonts.googleapis.cn`，无外网时浏览器会自动回退，不影响功能 |
| 智能体回答很模板化 | 外网不通，自动降级到内置规则引擎（`/api/health` 仍显示配置的模型名）。配好 `LLM_API_KEY` + 能访问 `api.deepseek.com` 即可走大模型 |
| 智能体回复不是逐字出现 | Nginx 忘了 `proxy_buffering off` |
| 重启后数据没了 | 预期行为，见第 7 节 |
| 托管平台上一直转圈 / 502 | 免费实例休眠后正在冷启动，等 30~60 秒再刷新；或者是你自己电脑刚关机（路线 A / B 就会这样） |
| 托管平台构建失败 | 看构建日志。镜像里只用 `npm install` 装 4 个依赖，失败基本是网络问题，重跑一次构建 |
| 平台日志报「生产环境必须配置 JWT_SECRET」 | 平台没注入 `JWT_SECRET`。`render.yaml` 已配 `generateValue`；手动建服务时要自己加一个 ≥32 位的随机串 |
| 云服务器 `systemctl status wenshu` 是 failed | `sudo journalctl -u wenshu -n 50` 看报错；最常见是 `.env` 里 `JWT_SECRET` 没填 / 太短 |
| 云服务器服务正常但外网打不开 | 云控制台**安全组**没放行 80 / 443（这一步脚本改不了，只能网页上点） |
| certbot 申请 HTTPS 失败 | 域名没解析到这台机器，或 80 端口不通。`ping 你的域名` 确认解析后再重跑脚本 |

---

## 9. 上线自查清单

- [ ] `JWT_SECRET` 已换成随机串
- [ ] `NODE_ENV=production`
- [ ] `.env` 没有被提交进 Git（`git status` 里看不到）
- [ ] 演示账号密码已改，或已关闭注册
- [ ] 大模型 Key 设了额度上限，或改用 `AGENT_PROVIDER=rules`
- [ ] 对外访问走的是 `https://`
- [ ] 已确认数据丢失可接受，或已完成第 7 节的落库
- [ ] `node tests/run-all.js` 仍 11/11 通过

**分路线的额外自查**

- 路线一（免费托管）：已接受「实例休眠 + 重启后数据重置」；`AGENT_PROVIDER=rules`，或已给大模型 Key 设了消费上限；构建日志无报错
- 路线二（云服务器）：云控制台安全组已放行 80 / 443；`sudo journalctl -u wenshu` 没有报错；`JWT_SECRET` 由脚本自动签发（64 位随机）