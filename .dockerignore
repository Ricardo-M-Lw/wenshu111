# ---------------------------------------------------------------------------
# 构建上下文排除清单
# 注意 dockerignore 的匹配是「相对构建上下文根」的，
# 单个 ".env" 只匹配根目录那一层 —— backend/.env 会被照打进镜像，
# 里面是真实的 LLM_API_KEY，所以必须写成 **/.env。
# ---------------------------------------------------------------------------

# ---- 依赖：镜像里自己装，本地这份不进上下文（还是 pnpm 的软链接结构）----
node_modules
**/node_modules
.pnpm-store

# ---- 环境变量 / 密钥（按任意层级排除，别改成单个 .env）----
.env
**/.env
.env.local
**/.env.local
.env.*.local
**/.env.*.local
!.env.example
!**/.env.example
*.pem
*.key

# ---- 版本控制 / 编辑器 / 系统 ----
.git
.gitignore
.vscode
.idea
.DS_Store
Thumbs.db

# ---- 运行时产物 ----
*.log
logs

# 前端构建产物：后端优先服务 frontend/ 源码目录，dist 是历史遗留，不用进镜像
frontend/dist

# 测试 / 素材脚本 / 原型文档不进运行镜像
tests
tools
docs
*.md
!README.md

# 本地打包产物
dist-package
*.zip
*.tar.gz