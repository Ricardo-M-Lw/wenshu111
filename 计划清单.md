# 问数星途 · 计划清单（总）

> 更新：2026-09-17　状态：**天梯赛阶段 1 已动工完成**（见文末「五、本轮已落地」）
> 产品定位：**面向学生与家长**——家长与学生共用同一入口（「学习报告」页即家长视角）。
> 因此**不设教师端、不设班级课堂**；教学引导全部由智能体「小问」承担。
>
> 本文件是三份清单的**唯一汇总入口**，另两份的历史来源：
> - `待改进清单.md`：第 5 天的 P0~P2 扫描，绝大部分已修完（剩 4 条并入本文件 P0/P1）。
> - `outputs/问数星途-优化清单-待执行.md`（第 6 天，工作目录）：本文件「一、技术债」的来源。
>
> **排期建议**：一（P0）→ 一（P1/P2/P3）→ 三阶段 0 → 三天梯赛 MVP → 二（功能）。

---

## 进度速览

| 维度 | 条目数 | 状态 |
|------|--------|------|
| 技术债 P0（会造成错误行为） | 5 | ⏳ 待执行 |
| 技术债 P1（性能与体感） | 6 | ⏳ 待执行 |
| 技术债 P2（结构与可维护性） | 6 | ⏳ 待执行 |
| 技术债 P3（安全与可观测） | 3 | ⏳ 待执行 |
| 新增方向（架构 / 实现 / 功能） | 21 | ⏳ 待执行 |
| 天梯赛（联机挑战） | 分 4 期 | 🚀 **阶段 1 已落地并验证**，阶段 2–3 待执行 |
| 本轮已落地（下线教师端） | 15 文件 | ✅ 已完成并验证 |
| 本轮已落地（星际天梯 · 阶段 1） | 39 文件 | ✅ 已完成并验证（13/13 测试套件全绿） |

---

## 本轮已落地：下线教师端与班级维度

| # | 改动 | 文件 |
|---|------|------|
| 1 | `ROLES` 删 `teacher`；4 个 `admin:*` 权限码收敛为 `['ops']`；`isStaff()` 只认 `ops` | `backend/src/permissions.js` |
| 2 | 删教师演示账号 `t1`；删 `className` 字段；`shareToTeacher` → `shareToFamily` | `backend/src/models/mockData.js` |
| 3 | 看板不再返回 `teacher` 计数；注释同步为「运营」 | `backend/src/routes/admin.js` |
| 4 | 4 处下发结构去掉 `className`；设置默认值改 `shareToFamily` | `backend/src/routes/report.js` |
| 5 | 新学生默认设置改 `shareToFamily` | `backend/src/repositories/studyRepository.js` |
| 6 | `qw_user` 去掉 `class_name` 列；表 / 角色注释改为「学生 / 运营」 | `backend/src/db/schema.js`、`schema.sql` |
| 7 | 启动横幅与注释措辞清理 | `backend/src/app.js`、`middleware/auth.js`、`services/vipService.js` |
| 8 | `ROLE_HOME` 删 `teacher`；`isStaff()` 只认 `ops`；守卫文案改「运营」 | `frontend/src/scripts/auth.js` |
| 9 | 演示账号芯片 3 → 2 个（学生 / 运营） | `frontend/src/scripts/auth-page.js` |
| 10 | 模块 `roles` 去掉 `teacher`；删「教师」KPI；`roleName()` 去教师 | `frontend/src/scripts/admin.js` |
| 11 | 「参与班级排行」→「参与星系排行榜」；「共享给老师」→「共享给家长」；副标题 `className` → `school` | `frontend/src/scripts/report.js` |
| 12 | 账号卡副标题去掉 `className` | `frontend/src/scripts/app.js` |
| 13 | 权限断言改为运营 / 学生对照；**新增**「教师账号返回 401」与「学生越权访问 403」用例 | `tests/layers-smoke.js` |
| 14 | 控制台用例改用 `admin / admin123` | `tests/dom-smoke.js` |
| 15 | README / 部署文档 / 开发文档 / 同行方案借鉴分析 全部措辞收敛 | 4 份文档 |

**保留项（刻意不改）**：`agent/blocklist.js` 里「请让家长或老师来操作」「学校班级」两处是**未成年人隐私保护文案**，与班级课堂无关。

**验证结果**：`node tests/run-all.js` → **12/12 套件全绿**（接口 17、分层 38、课堂 12、页面 66、智能体前端 23、小问 26、骨架 27、额度 11、配额 13、意图 15 条等）；16 个改动 JS 文件 `node --check` 全过；后端启动横幅确认 `qw_user(2)`（教师账号已消失）。

---

## 本轮已落地：星际天梯（阶段 1 · 异步排位赛）

> 2026-09-17 落地。用户要求「可以加入天梯赛」，按本文件「三、天梯赛专项设计」的阶段 1 实现。
> **关键取舍**：纯 REST 异步天梯——不需要 WebSocket、不加新依赖、**全程不调大模型**（边际成本 0），现有内存数据层即可跑通。

| # | 改动 | 文件 |
|---|------|------|
| 1 | **新增**确定性题库：`mulberry32` + FNV hash 播种，kp1 四型 / kp2 四型 / kp3 三型；九段星阶 `TIERS`；规则常量 `RULES`（每题 20 秒 / 一局 8 题）；起分 `START_RATING = 900`（新号从「陨石」起步）；`deltaFor` 胜 +25 / 平 +10 / 负 -15 + 连胜加成（3 连起 +5，上限 +10）；`tierFloor` 段位保护；`questionScore` 100 + 剩余秒 × 3（上限 45） | `backend/src/models/ladderBank.js` |
| 2 | **新增 4 张表**（赛季 / 段位档案 / 对局 / 逐题作答），共 **18 张**；对局表带 `current_seq`、`seq_started_at`，作答表带唯一索引 `uk_lanswer_match_seq`（幂等防重放） | `backend/src/db/schema.js` |
| 3 | 用 `schema.toSql()` **重新生成** DDL（含天梯四表、星钻两表，教师列已删） | `backend/src/db/schema.sql` |
| 4 | 播种赛季 `s1` / 我的档案 `u1`（1128 分「流光」）/ 6 名星海对手；**同时删掉已下线的教师账号 `t1`**（用户只剩 u1、o1） | `backend/src/models/mockData.js` |
| 5 | **新增**仓储：赛季 / 档案 / 对局 / 作答 + `rivals()`、`rivalsOf(rating)`（按分匹配对手） | `backend/src/repositories/ladderRepository.js`、`repositories/index.js` |
| 6 | **新增**服务层：赛季 / 我的 / 天梯榜 / 开局 / 逐题作答 / 结算 / 家长守护；`questionView()` 剥离 `correctAnswer`，**答案只在结算后揭晓**；每日场次免费 3 局 / 会员 10 局，跨天重置；家长守护复用 `studyWindow` / `quietHours`（北京时间 UTC+8）；结算幂等；星尘奖励胜 12 / 平 8 / 负 3 走 `walletService.record()` | `backend/src/services/ladderService.js` |
| 7 | **新增路由** `GET /api/ladder/season|leaderboard|me`、`POST /api/ladder/guard|match|match/:id/answer|match/:id/finish`、`GET /api/ladder/match/:id`；挂 `app.js` + 启动横幅加「🛸 星际天梯」 | `backend/src/routes/ladder.js`、`backend/src/app.js` |
| 8 | 钱包分类标签加 `ladder: '星际天梯'` | `backend/src/services/walletService.js` |
| 9 | **新增页面** `ladder.html`（引 `common.css` / `space.css` / `ladder.css` + `auth.js` / `xiaowen.js` / `nav.js` / `ladder.js` / `space-art.js` / `space.js`） | `frontend/pages/student/ladder.html` |
| 10 | **新增前端逻辑**三视图：主界面（段位徽章 + 赛季规则 + 开始匹配 + 每日场次星点 + 近 10 局 + 天梯榜前三星台 + 家长守护开关）/ 对局（并排进度条 + 倒计时环 + 连击粒子）/ 结算（结算卡 + 逐题回放） | `frontend/src/scripts/ladder.js` |
| 11 | **新增样式**（`sql-` 前缀，全部走设计令牌 `--sq-*`，`color-mix` 带 `@supports not` 回退） | `frontend/src/styles/ladder.css` |
| 12 | 导航接入：`ALIAS.ladder = 'planet'`，顶栏 tab 加「🛸 星际天梯」 | `frontend/src/scripts/nav.js` |
| 13 | 加 `student-ladder` 入口 | `frontend/vite.config.js` |
| 14 | **新增测试套件**（自起后端 3201，50 条断言）：赛季 / 我的 / 开局不泄答案 / 服务端判分 / 超时 / 重复提交 / 结算与段位保护 / 幂等 / 真星尘流水 / 每日上限 429 / 家长守护 403 / 天梯榜 / 越权 403 / 新号 900 分「陨石」 | `tests/ladder-smoke.js` |
| 15 | 注册天梯套件（`{ selfHost: true }`，排在 `classroom-ui-smoke` 之后） | `tests/run-all.js` |
| 16 | 加 `ladder.html` 页面断言 + `ladder.js` / `ladder.css` / 3 个 `/api/ladder/*` 只读断言（页面总数 15 → 16） | `tests/page-check.js` |
| 17 | 加 `['student-ladder', '/pages/student/ladder.html']` 执行用例 | `tests/dom-smoke.js` |
| 18 | 把写死的「14 张表」改成「≥14 张 + 每张有主键 + 表名不重复（当前 18 张）」+ 新增「天梯四张表已登记」（37 → 39 条断言） | `tests/layers-smoke.js` |
| 19 | README 更新项目结构 / 页面地址表 / 测试表 / 接口文档 / 功能清单；本文件标记阶段 1 完成；开发文档追加当日条目；更新说明补充交付清单 | 4 份文档 |

**验证结果**：`node tests/run-all.js` → **13/13 套件全绿**（接口 17、分层 39、页面 72、智能体前端 23、小问 26、课堂 12、天梯 52 等）；后端启动横幅确认 `qw_user(2)` + 🛸 星际天梯入口。

---

## 一、技术债修复（20 条）

### P0 · 会造成错误行为（5 条）

**P0-1　日界口径三套不一致**
- 现象：`agentQuotaService.js:22`、`gamificationService.js:8` 用 `toISOString().split('T')[0]`（UTC 日）；`mockData.js` 的 `stamp()` 带 `+08:00`；`routes/report.js` 用 `createdAt.slice(0,10)`（本地日）；`agent/index.js` 的 `dayKey()` 又是本地日。
- 后果：北京时间 00:00–08:00 的操作会算到前一天——签到算错日、报告对不上、**免费额度实际是早上 8 点重置**。
- 改法：新建 `backend/src/utils/day.js`，按 `Asia/Shanghai` 出日期键（`dayKey(date)` / `startOfDay()`），上述 4 处统一替换。
- 验收：`tests/layers-smoke.js` 增补「跨零点的免费额度与签到归同一自然日」用例。

**P0-2　数据全在内存，重启即清空** ⚠️ 天梯赛的硬前置
- 现象：星尘 / 等级 / 错题本 / 充值订单 / 免费配额全部在 `models/mockData.js` 与各仓储的内存表里。
- 后果：每次部署（Render 免费实例每天都会休眠重启）用户余额、错题本、**排行榜全部归零**。
- 改法：接 `DB_DRIVER=mysql`（`db/schema.js` 的 18 张表 + `repositories/` 已就绪，工作量为「换驱动 + 补 MyBatis 风格查询」）；过渡期可先做写操作 debounce 落盘 JSON + 启动回载。
- 验收：重启后端后 `13800000001` 的星尘余额、错题本条目数与重启前一致。

**P0-3　智能体记忆无淘汰，可被灌爆**
- 现象：`agent/memory.js` 的 `sessions` / `profiles` 两个 Map 没有 TTL，公开地址下匿名 `userId` 可以无限写。
- 改法：LRU（上限 ~5000）+ 30 分钟不活跃回收；session 数超限时淘汰最旧。
- 验收：新增用例循环写入 6000 个 userId，断言 Map 大小被压在上限内。

**P0-4　静态资源没有缓存头**
- 现象：`backend/src/app.js` 的 `express.static()` 未传 `maxAge`（默认 `max-age=0`），2.7MB 小问形象包、388KB `lucide.min.js` 每次访问都重新协商。
- 改法：`maxAge: '30d'` + `immutable`，配 `?v=<hash>` 版本参数；HTML 保持 `no-cache`。
- 验收：响应头出现 `Cache-Control: public, max-age=2592000, immutable`；二次访问静态资源返回 304 / 内存缓存。

**P0-5　兜底路由吞掉缺失资源**
- 现象：`app.get('*')` 无条件返回 `index.html`，缺图 / 缺 JS 时返回 `200 + text/html`，浏览器报「Unexpected token <」。
- 改法：路径带扩展名（`/\.\w+$/`）且未命中时直接 404；只有无扩展名的前端路由才回落。
- 验收：`GET /assets/nope.png` → 404。

### P1 · 性能与体感（6 条）

**P1-6　智能体页首屏 ~999KB**
- 构成：`lucide.min.js` 388KB（gzip 91KB）+ `common.css` 96KB + `space.css` 151KB + `idle.webp` 222KB + 脚本 141KB。
- 问题：全站实际只用了十来个 lucide 图标，却同步阻塞在 `<head>`。
- 改法：图标子集化（或直接内联 SVG，参考 `nav.js` 里 `PLANET_TAB_SVG` 的做法）+ `<script defer>`。
- 验收：智能体页首屏传输量降到 ~400KB 以内。

**P1-7　小问 12 张动图每张 200–270KB**
- 改法：预取 `idle` / `listen` / `thinking` 三张；其余压到 ~80KB（`tools/prepare-xiaowen-assets.js` 已有管线，加一道质量参数即可）。
- 验收：切换状态不再出现白帧；首屏只拉 3 张。

**P1-8　`/chat/stream` 是伪流式**
- 现象：`backend/src/routes/agent.js` 先 `await` 完整 reply，再 `wait(200)` 分块吐出去——首字延迟等于整段生成时间。
- 改法：真 SSE 透传 DeepSeek 的 `stream: true`，逐 chunk 下发；保留「出站改写」护栏（对已发出内容做增量校验）。
- 验收：首字延迟从 2–5 秒降到 <800ms；`agent-ui-smoke` 的 `delta` 事件仍能拼出完整文本。

**P1-9　`routes/report.js` 每次全量重算**
- 现象：十几次 `filter` + 7 天逐日过滤，完全没用现成的 `cache.wrap`。
- 改法：单遍聚合出 `{today, mastery, calendar, problems}`，再用 `cache.wrap(keys.hot('report:'+userId), 30~60s)` 包一层。
- 验收：报告页接口 P95 从 ~200ms 降到 <50ms（缓存命中）。

**P1-10　`cache/index.js` 的 Redis 驱动有隐患**
- 现象：`keys()` 用 `KEYS`（阻塞 Redis）、`size()` 回落内存计数、连不上就切内存 → 同一个 key 可能有两份数据。
- 改法：`SCAN` 迭代替代 `KEYS`；`size()` 用 `DBSIZE`；失败时明确降级策略（读内存、写两边）并打日志，避免「双份数据」静默发生。

**P1-11　线上发的是源码目录，`dist` 形同虚设**
- 现象：`express.static(frontendDir)` 排在前面，`frontend/dist` 从未被托管。
- 改法：加构建步骤（Vite 已在 `frontend/vite.config.js` 就绪）→ 发 `dist` + hash 文件名 + 长缓存；同时解决 P0-4 与 P1-6 的压缩问题。

### P2 · 结构与可维护性（6 条）

**P2-12　`schema.js`(17KB) 与 `schema.sql`(9.2KB) 双份手写**
- 改法：以 `schema.js` 为唯一事实来源，`npm run db:schema` 生成 SQL（脚本已在），**CI 里校验生成结果与仓库一致**，防止再出现两边不同步。

**P2-13　单文件过大**
- 清单：`models/lessons.js` 24.7KB、`agent/scaffold.js` 16.9KB、`agent/tools.js` 19.4KB、`frontend/src/scripts/app.js` 65KB、`space.css` 151KB。
- 改法：教学内容（讲题脚本 / 错因矩阵 / 题库）外置到 `backend/src/content/*.json`，代码只留读取与渲染；CSS 按页拆（`space.css` → `space-core.css` + `space-pages.css`）。

**P2-14　规则引擎与大模型各维护一套教学逻辑**
- 现象：`providers/rules.js` 与 `prompts.js` + `scaffold.js` 是两套话术与推进逻辑，改一边忘一边。
- 改法：抽出公共内核（阶段推进 / 提示分级 / 错因判定），让规则引擎走同一份骨架，差异只在「谁来措辞」。

**P2-15　「本次对话画像」语义不一致**
- 现象：`agent.reset()` 只清消息，不清 `profiles` 里的画像，UI 上却写「本次对话」。
- 改法：`reset()` 一并重置画像，或把文案改成「你的长期画像」。

**P2-16　13 套测试没有 CI**
- 改法：加 `.github/workflows/tests.yml`，push / PR 跑 `node tests/run-all.js`；本轮已在 `layers-smoke.js` 补上**越权用例**（学生访问 `/api/admin/orders` 应 403），后续再补并发用例。

**P2-17　可观测性只有 `console.log`**
- 现象：`degraded` / `guardType` 这类字段已经采集了，但没有面板、没有请求 ID、无法定位一次线上会话。
- 改法：请求 ID（`X-Request-Id`）+ 结构化 JSON 日志 + `GET /api/admin/metrics`（大模型调用数、降级率、护栏命中率、P95 延迟）。

### P3 · 安全（3 条）

**P3-18　CORS 与 CSP**
- 现状：`cors()` 已支持 `CORS_ORIGIN` 白名单，生产必须显式配置；CSP 仍留白（前端多处 `innerHTML`，需要先把内联脚本与事件属性收干净）。
- 改法：生产设 `CORS_ORIGIN=https://wenshu-xingtu.onrender.com`；逐步用 `textContent` / 模板化替代 `innerHTML`，再上 CSP。

**P3-19　头像走 2MB JSON body**
- 改法：独立上传接口 + 前端压到 128×128 再传；后端限制 256KB 并在存库前转存缩略图。

**P3-20　免费额度只按 userId，可被注册绕过**
- 现象：免费版每天 10 次对话只认 `userId`，换手机号注册即可重置。
- 改法：叠加 IP / 设备维度（`X-Device-Id`）做二级限流，与已有 `rateLimit` 中间件复用；全站 `LLM_DAILY_CAP` 继续兜底。

---

## 二、新增方向

### A. 架构（6）

| # | 方向 | 为什么值得做 / 工作量 |
|---|------|----------------------|
| A1 | **内容层与知识点编辑器**（知识点 / 讲题脚本 / 题库 / 错因字典外置为 `content/*.json`，加一个运营端编辑页） | 性价比最高：现在加一个知识点要改 `lessons.js` + `mockData.js` + `scaffold.js`；外置后运营端可视化增删，天梯赛与间隔重复都靠它喂数据 |
| A2 | **append-only 事件流数据模型**（`qw_event` 表：谁在什么时候做了什么） | 报告、雷达图、天梯积分、学习时长全部可以由事件流派生，不再各自维护一份统计；P0-2 落库时顺手做最省事 |
| A3 | **工具化检索层（轻量 RAG）** | 现阶段 3 个知识点，真向量库收益为负；但知识点扩到 50+ 后再做。先用 BM25 + 结构化过滤（按知识点 / 难度 / 错因），检索层做成 `agent/tools.js` 里的一个工具，接口不变 |
| A4 | **AI 能力网关**（chat / vision / asr / tts / embedding 统一入口 + 配额 + 降级） | 现在只有 chat 一个通道，且配额 / 降级 / 兜底逻辑散在 `agent/index.js`。网关化后接新模态只加一个 provider |
| A5 | **可观测 + Prompt A/B** | 与 P2-17 合并做；prompt 版本号入库，按 userId 分桶，用「一次讲透率 / 重问率」选优 |
| A6 | **多实例化前提**（会话 / 缓存 / 匹配队列全部外置到 Redis，本地不存状态） | 天梯赛实时匹配与横向扩容都依赖这一步 |

### B. 实现（9）

| # | 方向 | 说明 |
|---|------|------|
| B1 | 真 SSE 流式 | 见 P1-8 |
| B2 | 服务端 TTS / ASR | 现在只靠浏览器 `speechSynthesis`（音色不可选、离线难听）。服务端 TTS 可统一音色 + 缓存音频，`部署文档` 已有方案 |
| B3 | 拍照 OCR 讲题 | 孩子拍照上传 → OCR 出题干 → 走现有讲题流水线。这是「家长最想要」的功能之一 |
| B4 | 参数化动态图 | 一次函数拖滑块改 `k` / `b`，图像实时联动；`board.js` 已有 SVG 生成器，加参数绑定即可 |
| B5 | 符号等价判题 | 现在判题靠选项下标；支持 `2x+1` 与 `1+2x` 判等后，才能出填空 / 解答题 |
| B6 | 幂等 + 账本 | 充值 / 兑换 / 签到全部走幂等键 + 双向流水，避免重复点击造成余额错乱 |
| B7 | Prompt 回归测试进 CI | 把「不泄答案」「提示不超过 3 级」等红线做成断言，改 prompt 必须过 |
| B8 | Vite 构建 + 模板化 | 见 P1-11；顺带解决 `innerHTML` 拼接的 XSS 面（P3-18） |
| B9 | 无障碍与移动端 | 键盘可达、`aria` 标注、触控热区 ≥44px；家长多在手机上打开 |

### C. 功能（6）

| # | 功能 | 说明 |
|---|------|------|
| C1 | **间隔重复复习**（最划算） | 错题本已有 `createdAt` / `knowledgePointId` / 错因，按 SM-2 变体排「今天该复习哪 3 道」，直接把错题本变成每日任务 |
| C2 | 报告分享长图 / PDF | 家长要的是「能发到家庭群」的结果；现有报告页数据齐全，加一个 canvas 出图即可 |
| C3 | 目标 - 任务 - 打卡闭环 | 学期目标 → 周任务 → 日打卡，与现有签到 / 专注计时打通 |
| C4 | 星系副本与挑战关 | 知识点做成「星系」，每星系 3 档副本（基础 / 进阶 / 挑战），通关才点亮下一星系 |
| C5 | 同伴激励 + UGC 讲解卡 | 孩子把自己的讲解录成卡片分享，配合 C2 的分享链路 |
| C6 | 卡牌图鉴与装扮 | 徽章 / 星球 / 小问皮肤收集，纯留存向 |
> 说明：原清单里的「班级课堂模式」按产品定位**已删除**，不再排期。

---

## 三、天梯赛（联机挑战）专项设计

### 结论

**能加，而且比想象中简单——但真正的实时对战要等数据层。**
推荐拆成四期：其中的 **阶段 1「异步天梯」用现有 REST 就能做，不需要 WebSocket、不需要新依赖、不烧大模型额度**，可以直接上线；师生对战的「真·实时竞速」放在阶段 2。

### 为什么可以借鉴小猿口算

| 小猿口算的做法 | 能否搬到问数星途 |
|----------------|------------------|
| 短平快：一局 60 秒 / 5–8 题 | ✅ 直接可搬，孩子注意力窗口正好 |
| 天梯排位（段位 + 赛季） | ✅ 能搬，且可换成**星际段位**，与现有星系主题天然一致 |
| 实时 1v1 同题竞速 | ⚠️ 需要匹配队列 + 长连接，排在阶段 2 |
| 无人时用机器人补位 | ✅ 必做（否则人少时匹配不到人，体验直接崩） |
| 排行榜（周榜 / 赛季榜） | ✅ 能搬，但**必须先落库**，否则每次重启榜单一键清零 |
| 固定题库 | ✅ **必须**：实时对战不能用大模型现生成题目（不确定 + 慢 + 烧钱） |

### 三个硬约束（决定了排期）

1. **题库太小**：现在只有 3 个知识点 / 14 道题。天梯赛需要一个**确定性、可服务端判分**的题库，目标 ~200 题（按知识点 × 难度 × 限时分布）。顺带把间隔重复（C1）也一起喂饱。
2. **数据在内存**：排行榜是强持久化需求，`P0-2` 不解决就永远只是演示。
3. **零大模型成本**：一局对战绝不能调大模型——固定题库判分，边际成本为 0。这条同时也是**防作弊**的基础（答案只在服务端，沿用现有「不泄答案」不变量）。

### 玩法设计（对齐星际主题）

**星际天梯 · 段位**（积分赛，赛季制）

| 段位 | 积分区间 | 图标建议 |
|------|----------|----------|
| 陨石 | 0 – 999 | 🪨 |
| 流光 | 1000 – 1199 | ☄️ |
| 新星 | 1200 – 1399 | ✨ |
| 行星 | 1400 – 1599 | 🪐 |
| 恒星 | 1600 – 1799 | 🌞 |
| 星团 | 1800 – 1999 | 🌌 |
| 星云 | 2000 – 2199 | 🌫️ |
| 星系 | 2200 – 2399 | 🌀 |
| 星海之王 | 2400+ | 👑 |

**一局规则（阶段 1「闯关计时」）**
- 服务端按段位抽 8 道题（知识点按玩家最近薄弱项加权），下发**不含答案**的题面。
- 限时 90 秒；每题作答后服务端即时判分并返回对错（**不返回正确答案**，沿用 `guardrail` 的出站改写）。
- 计分：`正确数 × 100 + 剩余秒数 × 5 - 提示次数 × 30`；段位积分按 `Elo（K=32）` 或简化版「胜 +25 / 负 -15、段位不掉星」结算。
- 结果页：对错回放 + 错因归因（复用 `agent/records.js` 的错因矩阵）+ 本局积分变化 + 一键「再战」。

**阶段 2 追加「真·实时 1v1」**：同题竞速，先答对者得该题分；对手掉线 15 秒判胜。

### 数据模型（新增 5 张表，接 P0-2 一起落库）

```sql
qw_ladder_season   (id, name, start_at, end_at, status)              -- 赛季
qw_match           (id, mode, season_id, kp_ids, question_ids,       -- 一局对战
                    host_user_id, guest_user_id, status,            -- status: waiting/playing/done/abandoned
                    score_host, score_guest, winner_id, started_at, ended_at)
qw_match_answer    (id, match_id, user_id, question_id, answer,      -- 逐题作答（服务端判分）
                    correct, cost_ms, created_at, uk: (match_id, user_id, question_id))
qw_ladder_rating   (user_id, season_id, rating, tier, wins, losses,  -- 段位与积分
                    streak, best_streak, matches, updated_at, pk: (user_id, season_id))
qw_ladder_rank_snapshot (id, season_id, scope, snapshot_at, payload) -- 周榜快照（避免全表排序）
```

> 题库直接复用 `qw_question`，只加 `difficulty` / `time_limit` / `used_count` 三个字段。

### 接口设计

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/ladder/season` | 当前赛季、剩余天数、我的段位 |
| GET | `/api/ladder/me` | 积分 / 段位 / 连胜 / 近 10 局战绩 |
| GET | `/api/ladder/leaderboard?scope=week\|season&limit=50` | 天梯榜（读快照，不做全表排序） |
| POST | `/api/ladder/match` | 开一局（阶段 1：生成闯关卷；阶段 2：进入匹配队列） |
| GET | `/api/ladder/match/:id` | 取本局题面 + 进度（阶段 2 用于轮询对手进度） |
| POST | `/api/ladder/match/:id/answer` | 逐题提交，服务端判分，**不下发答案** |
| POST | `/api/ladder/match/:id/finish` | 结算：算分、写 `qw_match`、更新 `qw_ladder_rating` |
| GET | `/api/ladder/match/:id/stream`（阶段 2） | SSE：对手进度 / 结束事件 |

### 前端
- 新页 `frontend/pages/student/ladder.html`：段位徽章 + 「开始匹配」大按钮 + 赛季倒计时 + 天梯榜（前三名做成星台）。
- 底部导航把「星球」tab 扩成两级，或把天梯挂在「星球」页顶部（`planet.html` 已是星系地图，点开后进入对应关卡——两者语义一致，建议**合并到星球页**，减少一个 tab）。
- 一局内 UI：题目卡 + 倒计时环 + 对手进度条（阶段 2）+ 连击特效（复用 `space-art.js` 的 `burst()` 粒子）。

### 防作弊与家长友好（这是本产品和小猿最大的差异化）

1. **服务端判分 + 答案零下发**：沿用现有不变量，前端拿不到正确答案。
2. **复用家长设置做防沉迷**：`studySettings.studyWindow` / `quietHours` 直接约束天梯赛开放时段——家长设了 21:30 免打扰，天梯赛自动关闭。**这一条是现成代码，几乎零成本**。
3. **每日场次上限**：免费 3 场 / 会员 10 场，与 `agentQuotaService` 同构；超限引导用「星尘兑换加次」。
4. **积分不惩罚**：段位不掉星、连败保护（连败 3 局后下一局只加不减），避免孩子被挫败感劝退。
5. **家长可关**：`privacy.shareRanking` 关闭后不上榜（本轮已把文案改成「参与星系排行榜」，语义正好对上）。

### 成本与风险

| 项 | 评估 |
|----|------|
| 大模型成本 | **0**（固定题库，不调模型） |
| 新增依赖 | 阶段 1：**无**（纯 REST + 轮询）。阶段 2：SSE 即可，不必上 WebSocket 库 |
| 新增存储 | 5 张表 + 题库扩容（~200 题）；Redis 用于匹配队列与榜单缓存（阶段 2） |
| 并发 | 阶段 1 单实例够用；阶段 2 免费实例冷启动会有 30–60 秒首连延迟，建议**先只做阶段 1 上线** |
| 主要风险 | ① 人少时匹配不到人 → 用机器人补位 + 阶段 1 的异步模式天然规避；② 题库太薄导致重复出题 → 先扩容再上 |

### 分期落地

| 期 | 内容 | 前置 | 预估 |
|----|------|------|------|
| 阶段 0 | 数据落库（P0-2）+ 题库扩到 ~200 题 + 统一日界（P0-1） | — | 最大的一块，但 P0-2 本来就要做 |
| 阶段 1 | ✅ **已完成**：**异步天梯**——限时闯关 → 计分 → 段位 → 天梯榜（纯 REST，已上线，全程不调大模型） | 阶段 0（用内存数据先行落地） | 中等 |
| 阶段 2 | **实时 1v1**：匹配队列 + 同题竞速 + 断线重连 + 机器人补位 | 阶段 1 + Redis | 较大 |
| 阶段 3 | 赛季重置 + 好友房 + 观战 + 战绩分享长图（接 C2） | 阶段 2 | 中等 |

---

## 四、建议推进顺序

**第 1 步 · 打地基（做完这一步，后面所有功能才好接）**
1. `P0-1` 统一日界（小，但影响所有统计口径）
2. `P0-2` 数据落库 + `P2-12` schema 单一事实来源 + `A1` 内容层外置
3. `P0-3` / `P0-4` / `P0-5` 三个小修（加起来半天）

**第 2 步 · 立刻能感知的价值**
4. `C1` 间隔重复复习（错题本已有数据，投入小、留存涨）
5. `C2` 报告分享长图（家长最买账）
6. ~~天梯赛**阶段 1**（异步天梯）~~ ✅ 已完成（2026-09-17）

**第 3 步 · 体验与规模**
7. `P1-8` 真流式 + `P1-6/P1-7` 首屏与素材瘦身
8. `B2` 服务端 TTS + `B3` 拍照 OCR 讲题
9. 天梯赛**阶段 2**（实时 1v1）

**明确不建议做**
- 真向量 RAG：只有 3 个知识点，收益为负；等 `A1` 把知识点扩到 50+ 再说。
- 模型微调：数据量不足，且当前 DeepSeek + 骨架提示词已能覆盖教学场景。
- 容器编排（K8s）：单实例 Render 完全够用，过早引入只会增加运维负担。
