# 问数星途 · 开发文档（每日改动记录）

> 用途：按天记录**需求 → 改动 → 验证**，方便回溯「哪天改了什么、为什么改、有没有验证」。
> 记录原则：只写真实落地的改动（文件级），并注明验证方式；未验证或未完成的写进「待办 / 已知问题」。
> 项目：`E:\企业实训\代码\新建文件夹\wenshu-fullstack`（前端原生多页 + Express 后端 + 内存数据，13 套冒烟测试）
> 文档最后更新：2026-09-17

---

## 0. 快速索引

| 项目 | 内容 |
|------|------|
| 启动 | `cd backend && npm install && npm start` → http://localhost:3000 |
| 演示账号 | 学生 `13800000001` / `123456`；运营 `admin` / `admin123`（产品面向学生 + 家长，**无教师账号**） |
| 自检 | `node tests/run-all.js`（13 个套件，默认离线规则引擎）；`AGENT_PROVIDER=live` 可测真实大模型 |
| 建表脚本 | `cd backend && npm run db:schema` → `src/db/schema.sql` |
| 架构蓝图 | `README.md` 第 88 行起「🏗 项目框架（目标架构）」 |

---

## 1. 开发日志

### 2026-09-10（第 1 天）项目起步 · 讲题课堂 · 智能体 v1

**当天需求**：先把「讲题步骤 + 配图 + 智能体」跑起来，形成一个能演示的学生端雏形。

**改动（按时间）**

| 时间 | 改动 | 说明 |
|------|------|------|
| 16:02 | `frontend/pages/*.html` 9 个单文件原型 | student-home / blackboard-classroom / quiz-planet / quiz-training / checkin-points / error-correction / learning-result / badge-popup / level-system，先定视觉稿 |
| 16:03 | `backend/src/utils/seed.js` | 内存种子数据（日期按「今天」动态推算，保证演示永远显示近期数据） |
| 17:41 | `backend/src/utils/auth.js` | HMAC 签名令牌 + 加盐密码哈希（无第三方依赖，可平滑换 JWT） |
| 17:43–17:45 | `backend/src/routes/{quiz,dashboard}.js`、`backend/src/models/lessons.js` | 题库接口（不下发答案）、首页看板、3 知识点 × 5 讲题步骤脚本 |
| 17:47 | `frontend/src/scripts/board.js` | 黑板 SVG 图形生成器，覆盖 11 类几何图形——**「讲题步骤生成对应图片」的关键实现** |
| 19:42 | `frontend/src/scripts/classroom.js` | 五步讲题课堂主逻辑：步骤轨道 + 气泡讲解 + 检查点 |
| 19:51–19:54 | `backend/src/agent/{tools,memory,providers/rules}.js` | 智能体 v1：12 个工具 + 会话记忆 + 离线规则引擎 |
| 19:58 | `frontend/assets/images/knowledge/*.svg` | 3 个知识点配图（一次函数 / 内角和 / 勾股定理） |
| 20:00 | `backend/src/routes/learning.js`、`frontend/src/scripts/correction.js` | 讲题脚本接口（经 `safeLesson()` 剥离答案）+ 错题订正页 |
| 20:08 | `tests/{agent-smoke,agent-persona-smoke,classroom-ui-smoke}.js` | 首批冒烟测试：智能体意图 / 多角色 / 课堂全流程 |

**验证**：`node tests/classroom-ui-smoke.js` 通过（黑板 SVG → 逐步气泡 → 检查点判题 → 下一步）。

---

### 2026-09-11（第 2 天）登录注册 · 学生端收敛 · 引导式教学 · 语音与吉祥物

**当天需求**：① 家长端 / 学生端不再分家，把家长端功能压进学生端；② 增加登录注册；③ 完善智能体的引导式教学（通用模版）；④ 前端更童趣、可语音对话；⑤ 智能体页改成对话单独一侧。

**改动（按时间）**

| 时间 | 改动 | 说明 |
|------|------|------|
| 09:13、09:25 | `backend/src/routes/report.js`、`frontend/src/scripts/report.js` | 学习报告（总览 / 掌握 / 记录 / 提醒设置）落地——**原家长端功能正式并入学生端「报告」** |
| 10:08 | `frontend/assets/images/mascot/{xiaowen,rocket}.svg` | 领航员小问 + 火箭形象 |
| 10:18–10:19 | `frontend/pages/auth/{login,register}.html` | **登录注册模块**：记住我、演示账号一键填入、验证码注册（演示环境回显） |
| 10:22 | `frontend/pages/student/*.html` | 学生端多页应用成型（由原型页收敛为正式页面） |
| 11:39 | `backend/src/agent/records.js`、`routes/student.js`、`agent/ability.js` | 错题档案 + **五维能力雷达图**数据接口 |
| 11:45 | `backend/src/agent/providers/openai.js` | 接入 OpenAI 兼容 function calling（默认 DeepSeek），失败自动降级规则引擎 |
| 14:39 | `frontend/src/scripts/space.js` | 星野装饰 + 白天 / 夜晚双主题 |
| 14:48 | `backend/src/agent/blocklist.js` | **限制词表**：要答案 / 不当内容等分类拦截 |
| 15:32–15:34 | `backend/src/agent/{prompts,personas,guardrail,index,scaffold}.js`、`routes/agent.js` | 三角色（讲题老师 / 错题侦探 / 学习规划师）+ 出站泄答案复检 + **引导式教学通用模版（五步闭环 / 答题六步 SOP / 错因矩阵）** |
| 15:36 | `frontend/pages/student/{agent,classroom}.html` | 智能体独立页 + 课堂内嵌智能体面板 |
| 15:34、15:38 | `tests/{scaffold-smoke,agent-ui-smoke}.js` | 教学骨架冒烟 + 智能体前端全流程冒烟 |
| 16:23–16:25 | `frontend/src/styles/common.css`、`src/scripts/voice.js` | **语音对话**：语音识别 + 播报 + 声音列表与语速选择 |
| 16:34、16:50 | `frontend/assets/images/mascot/cheese-cat.svg`、`src/scripts/mascot.js` | 小问吉祥物（奶酪猫）常驻右下角，点击展开小聊天框 |
| 16:42 | `frontend/src/scripts/agent-page.js` | 智能体页改为**左右两栏**：左侧上下文（角色 / 五步 / 知识点 / 步骤），右侧独立对话区 |

**验证**：`tests/agent-ui-smoke.js`、`tests/scaffold-smoke.js` 通过；浏览器确认语音、吉祥物、两栏布局。

---

### 2026-09-12（第 3 天）VIP 充值 · 分层架构落地 · 管理控制台 · 智能体排队修复

**当天需求**：① 加 VIP 充值界面（付款码随机、演示账号直接付款成功）；② 先确立完整项目框架并更新 README，再按框架完善代码；③ 整理开发文档。

**改动（按时间）**

| 时间 | 改动 | 说明 |
|------|------|------|
| 09:13、09:25 | `frontend/pages/student/vip.html`、`src/scripts/vip.js` | **会员领航舱**：三档方案（免费 / 月度 ¥29 / 年度 ¥259）+ 3 渠道 + Canvas 随机付款码 + 支付成功弹窗（演示账号即付即通，不接真实网关） |
| 09:26 | `frontend/src/scripts/app.js`、`src/styles/space.css` | 头像可选可自定义（emoji / 文字 / 图片 / data URL）、星球图标美化、顶部导航修正 |
| 09:38 | `tests/api-smoke.js` | 认证 / 学习 / 答题 / 报告 / 会员充值接口冒烟 |
| 09:48 | `frontend/src/scripts/nav.js` | 底部菜单星球图标改为自绘 SVG（lucide 部分版本缺 `planet`），顶栏会员皇冠按 `/api/auth/me` 校正 |
| 10:23–10:36 | **后端分层落地**（见下表） | 按 README 蓝图把 11 层里的 9 层真正拆出来 |
| 10:44 | `tests/run-all.js`、`frontend/src/scripts/auth.js` | 测试默认锁定 `AGENT_PROVIDER=rules` 保证文案确定；`auth.js` 增加教师 / 运营的 `ROLE_HOME` 与 `staff` 守卫 |
| 10:45 | `frontend/src/scripts/auth-page.js` | 登录页三个演示号芯片（学生 / 教师 / 运营），登录后按角色分流 |
| 10:50 | `frontend/src/scripts/admin.js`、`tests/{dom-smoke,page-check}.js` | 管理控制台脚本；测试补管理端入口与资源检查 |
| 10:54 | `backend/src/app.js` | 主入口改为按层装配 + 启动横幅打印分层与数据表 |
| 10:56–10:57 | `frontend/pages/admin/console.html`、`src/styles/admin.css` | **管理控制台（教师 / 运营）** 页面与样式 |
| 10:59 | `frontend/src/scripts/agent.js` | 修：小问回答期间再发消息被静默丢弃 → 改为排队续发；发送按钮不再置灰（置灰会让回车提交整条失效） |
| 11:02 | `README.md` | 补「🏗 项目框架（目标架构）」8 小节蓝图；演示账号、项目结构、分层总览「当前实现」列、管理端接口、测试说明同步更新 |
| 11:15 | `开发文档.md`（新建）、`README.md` 顶部指引 | 按天整理「需求 → 改动 → 验证」；附需求对照表、已知问题 / 待办与每日记录模板 |

**后端分层落地明细（10:23–10:36）**

| 层 | 新增文件 | 作用 |
|----|----------|------|
| 配置 | `backend/src/config/index.js` | 集中读环境变量；`dotenv` 改用绝对路径，修掉「必须 cd backend 才能读到 .env」的坑 |
| 权限与基座 | `backend/src/permissions.js` | 角色 + 权限码（`模块:资源:动作`，对齐若依），`can / permissionsOf / isStaff` |
| 缓存 | `backend/src/cache/index.js` | Redis 语义双驱动：会话 / 注销标记 / 验证码 / 频率计数，配了 `REDIS_URL` 自动切换、连不上自动降级 |
| 数据库 | `backend/src/db/{schema.js,table.js,dump-sql.js,schema.sql}` | 12 张表的唯一事实来源 + 轻量表引擎 + 生成 DDL |
| 持久层 | `backend/src/repositories/*.js`（6 个 + index） | user / learning / quiz / gamification / study / vip 仓储出口 |
| 业务层 | `backend/src/services/{auth,gamification,vip}Service.js` | 统一返回 `{ status, body }` |
| 接入层 | `backend/src/middleware/{error,auth,rateLimit,validate}.js` | 统一异常 / 鉴权与权限码 / 限流 / 参数校验 |
| 管理端接口 | `backend/src/routes/admin.js` | 看板 / 题库 / 错因字典 / 订单 / 成员 5 个接口 |
| 工具 | `backend/src/utils/reply.js` | 响应信封（成功 `{ data }`，失败 `{ error }` + 状态码） |

**管理端权限对照（教师 vs 运营）**

| 模块 | 权限码 | 教师 | 运营 |
|------|--------|------|------|
| 经营概览 | `admin:overview:read` | ✅ | ✅ |
| 题库浏览 | `admin:question:list` | ✅ | ✅ |
| 错因字典 | `admin:error-type:list` | ✅ | ✅ |
| 会员成员 | `admin:member:list` | ✅ | ✅ |
| 会员订单 | `admin:order:list` | ❌ 403（页面显示「受限」卡片） | ✅ |

**验证**

- 全量冒烟：`node tests/run-all.js` → **9/9 套件通过**（接口 17、分层 37、教学骨架 27、页面 61、智能体前端 23、课堂 12 等断言全绿）。
- 浏览器实跑：教师登录 → 控制台（会员订单显示「受限」，点入为 403 权限卡）；运营登录 → 订单 / 成员 / 错因正常；学生登录 → 回学习首页；智能体连发两条消息，两条都有回复（排队生效）。

---

### 2026-09-12（第 3 天 · 下半场）小问形象 30 态接入 · 语音唤醒 · 素材构建脚本

**当天需求**：把设计稿目录 `E:\企业实训\素材图\素材图\小问形象设计` 里的各种状态真正用到前端；智能体正式定名「小问」，并且能语音对话（喊一声「小问」它就答应）。

**改动（按时间）**

| 时间 | 改动 | 说明 |
|------|------|------|
| 15:24 | `tools/prepare-xiaowen-assets.js`（新建） | 零依赖素材管线：自己解 PNG（`zlib` 解压 + 逐行反过滤）→ 按 alpha 通道取角色外接框 → 面积平均缩到最长边 320 → 五种行过滤器各压一遍取最小的那版重新编码。动图 WebP 原样拷贝 |
| 15:31 | `frontend/assets/images/mascot/xiaowen/*`（34 个文件，4.1 MB） | 12 个透明动图 WebP + 22 个静帧 PNG。原始设计稿单张 130 KB–583 KB 且带大片透明留白，裁切重编码后 51–85 KB，而且每个状态的角色大小一致，切换状态不会「跳大小」 |
| 15:38 | `frontend/src/scripts/xiaowen.js`（新建） | 小问形象控制器：30 个状态 + 语义反应（`react('correct')` → 庆祝）+ `data-qw-xiaowen` 元素自动换图 + 语音唤醒引擎（`QWXiaowen.wake`） |
| 15:45 | `frontend/src/scripts/agent.js` | 头像换成小问动图；接线状态钩子：发出问题 → `thinking`、第一个字吐出 → `explain`、答完 → `expect`、答对 → `celebrate`、答错 → `comfort`、变式题 → `curious`、报告 → `proud`、彻底断连 → `sorry`；语音行新增「🔔 喊"小问"唤醒」开关 |
| 15:52 | `frontend/src/scripts/mascot.js` | 整段重写：形象跟随全局状态；面板头部加唤醒开关；监听 `qw-xiaowen-wake` 自动展开小聊天框；唤醒时带半句提问就直接发给智能体（智能体还没挂载好就先排队）；语音提问自动打开语音播报 |
| 15:58 | `frontend/{index.html,pages/auth/login.html,pages/auth/register.html,pages/admin/console.html}`、`src/scripts/{nav,app}.js` | 6 处手绘 SVG 引用全部换成形象包：品牌位用钉死的静帧 `idle.png`，需要「活」的位置（首页领航员 / 登录页飞行员卡 / 对话头像 / 右下角吉祥物）用 `data-qw-xiaowen="follow"` 跟随状态 |
| 16:01 | 16 个页面 + `src/styles/{space,admin}.css` | 每个含 `auth.js` 的页面紧跟其后注入 `xiaowen.js`（保证页面脚本渲染形象时控制器已就绪）；圆形头像池加 `object-fit: contain` 防拉伸并留出内边距；新增第 26.4b 节（唤醒开关 / 呼吸光环 / 夜间模式 / `prefers-reduced-motion` 降级） |
| 16:04 | `tests/xiaowen-ui-smoke.js`（新建）、`tests/run-all.js`、`tests/page-check.js` | 小问形象冒烟 26 条断言；接入一键测试（9 → 10 套）；静态资源清单换成新的形象包 |
| 16:08 | `README.md`、`开发文档.md` | 项目结构补 `tools/` 与 `xiaowen.js`、冒烟测试表补新套件、功能清单与主题章节补小问形象 / 语音唤醒 |

**修掉的 bug（本轮真实踩到的）**

| 问题 | 原因 | 修法 |
|------|------|------|
| 小问形象没有 `data-qw-xiaowen-state` 标记 | `paintElement()` 里「目标 src 和当前 src 一样就提前 return」写在写标记之前，而品牌图的 HTML `src` 本来就是 idle，于是永远走提前返回 | 先写状态标记，再判断要不要换图 |
| 唤醒按钮点了像「没反应」 | 麦克风被拒时只把开关关掉，界面上没有任何说明 | 唤醒事件带上 `reason`，`agent.js` 收到 `not-allowed` / `unstable` 时在语音提示区写明原因 |

**小问 30 个状态怎么用（对照表）**

| 状态 | 形态 | 什么时候出现 |
|------|------|--------------|
| `idle` | 动图 | 默认待机；顶栏品牌位固定用它（静帧） |
| `wave` | 动图 | 打招呼：进入对话、点开唤醒开关、唤醒应答 |
| `expect` | 动图 | 等你作答 / 等你继续问（一轮答完的落点） |
| `listen` | 动图 | 麦克风拾音中（按住说话、唤醒后接话） |
| `thinking` | 动图 | 问题已发出、等大模型或工具返回 |
| `explain` | 动图 | 小问正在流式输出 / 正在语音播报 |
| `curious` | 动图 + 静帧 | 给出变式题 |
| `aha` | 动图 | 给出提示 |
| `celebrate` | 动图 + 静帧 | 判题答对 |
| `comfort` | 动图 | 判题答错 |
| `encourage` | 动图 | 需要再试一次 |
| `confused` | 动图 | 小问没听懂（换个说法） |
| `focused`/`puzzled`/`proud`/`sorry`/`sad`/`shy`/`sleepy`/`like`/`nervous`/`determined`/`discouraged`/`awkward`/`cry`/`scared`/`shocked`/`speechless`/`wronged`/`angry` | 静帧 | 18 个情绪表情，通过 `flash()` / `react()` 在气泡或吉祥物上做瞬时反应 |

**素材取舍（为什么只搬了这些）**

| 设计稿内容 | 处理 | 原因 |
|------------|------|------|
| 12 个动图 WebP（512px，带 ALPH 通道） | 原样拷贝 | 透明 + 紧贴角色裁切，体积已经很小 |
| `06-情绪表情/` 的 16 张 PNG | 裁切重编码后保留 | 真 PNG（RGBA 透明），可用 |
| `xiao-wen-{wave,explain,listen,expect,aha,comfort,encourage}.png`、根目录散图、`-blink` 系列 | **不用** | 扩展名写 `.png`，实际是 JPEG，没有透明通道，贴到深色星空底上会露出一块白底 |
| `xiao-wen-state-*.png`、`16frame-sheet`、`walk-*.png`、`f-01..f16-*.png` | 不进仓库 | 是带手写批注的设计稿 / 分镜帧，不是能直接用的成品素材 |

**验证**

- `node tests/run-all.js` → **10/10 套件通过**，其中新增的 `tests/xiaowen-ui-smoke.js` 26/26（30 态素材逐个核对文件是否存在、状态机切换与回弹、钉死的静帧品牌图不受影响、近音词「小文」也能唤醒、唤醒词后半句转发、1.6 秒防抖、`pause()/resume()` 给「按住说话」让位）。
- 浏览器实跑（学生演示号）：登录页飞行员卡、学生首页顶栏与首页伴学卡、智能体页对话头像、讲题课堂右下角都能看到小问；在首页右下角点开「小问快问」发一条消息，采样到形象状态 `idle|thinking|thinking` → 回落 `idle|expect|expect`；智能体页语音行能看到「🔔 喊"小问"唤醒」按钮。
- 未验证（需要人工）：真实麦克风喊「小问」的端到端效果——需要出声并授权麦克风，自动化测不了；唤醒逻辑用假 `SpeechRecognition` 在冒烟测试里覆盖。

---

### 2026-09-12（第 3 天 · 夜场）智能体对话框布局重构 · 语音设置收进浮层 · 小问面板默认收起

**当天需求**：三个位置的智能体对话框都偏小、内容显示不全，重新分配前端布局。

**改前实测（1280×720 视口，物理像素）**

| 位置 | 对话区高 | 语音操作条高 | 问题 |
|------|---------|------------|------|
| 独立智能体页 `.qw-agent-main` | 160（内容 177） | 116 | 首屏内容直接溢出被裁；语音条 6 个子元素换行成三行 |
| 右下角「小问快问」`#qwMascotPanel` | 107 | 182 | 语音条是对话区的 1.7 倍，反倒成了主体 |
| 讲题课堂内嵌面板 `.qw-agent-compact` | 180（上限 288） | 128 | 卡在 `min-height` 下限上不去 |

**改动（按时间）**

| 时间 | 改动 | 说明 |
|------|------|------|
| 16:40 | `frontend/src/styles/space.css`（26.3 节） | 「语音设置」从流内块改成**浮层**：`.qw-agent-voicepick` 改 `position:absolute; right:0; bottom:calc(100% + 10px)`、宽 `min(430px,92vw)`，收起时彻底不占高度；补 `.qw-agent-voicepick[hidden]{display:none}`（原来 `display:flex` 把 `[hidden]` 顶掉了，声音区其实一直是展开的）；`.qw-agent-voice` 加 `position:relative` 当锚点 |
| 16:44 | `frontend/src/styles/space.css`（26.3 / 26.4b 节） | 新增 `.qw-agent-vset`「⚙ 语音设置」开关键（默认收起 / 展开态高亮）；夜间模式补 `body.qw-night .qw-agent-vset` 与浮层底色 `#1E2A48` |
| 16:48 | `frontend/src/scripts/agent.js` | 语音行模板插入 `#qwAgentVSet`；只有浏览器真有可选嗓音时才亮出这个键（`fillVoiceOptions` 里由「展开 pickBox」改为「亮出开关键」）；点击展开/收起，点浮层外面或按 Esc 自动收起；`send()` 里给面板打 `has-msgs` 标记 |
| 16:52 | `frontend/src/styles/space.css`（语音条附近） | 紧凑面板 `.qw-agent-compact .qw-agent-stream` 保底高度 180 → **260**、去掉 288 的上限；`.qw-agent.has-msgs .qw-agent-quick{display:none}`（学生一发消息灵感条就收起，把高度还给对话）；`.qw-agent-quick:empty{display:none}`；紧凑面板里的唤醒键 `flex:1 1 auto` → `0 0 auto`，不再独占一整行 |
| 17:00 | `frontend/src/styles/space.css`（25.2 节） | 智能体页：`#agentApp` 上内边距 28 → 18；`.qw-agent-main` 高度 `calc(100vh - 256px)` → `calc((100vh - 218px) / 1.08)` 并显式 `box-sizing:border-box`；对话区保底 96 → 170；语音设置键跟随卡片尺寸压缩；左栏 `max-height` 与卡片取同一公式 |
| 17:08 | `frontend/src/styles/space.css`（26.4 节） | 小聊天框：宽 386 → **420**；高 `398px` → `min(64vh,540px)` 且 `box-sizing:border-box`；面板 `bottom:94px` → `70px`（贴着吉祥物上方，少浪费 24px）；对话区保底 110 → 160；麦克风改 `flex:0 1 auto` 并藏掉「，和小问交流」长文案，语音条从三行压到两行 |
| 17:14 | `frontend/src/styles/space.css`（26.4 节） | 补 `.qw-mascot-panel[hidden]{display:none}` |

**修掉的 bug（本轮真实踩到的）**

| 问题 | 原因 | 修法 |
|------|------|------|
| 右下角「小问快问」一直浮在页面右侧，把智能体页 / 课堂页的对话卡挡住 | `.qw-mascot-panel` 自己写了 `display:flex`，作者样式压过浏览器默认的 `[hidden]{display:none}`，于是 `panel.hidden = true` 形同虚设 | 补 `.qw-mascot-panel[hidden]{display:none}` |
| 「语音设置」区（声音 / 语速 / 试听）一直占着对话区上方 87px | 同上，`.qw-agent-voicepick{display:flex}` 顶掉了 `[hidden]` | 改成浮层并补 `[hidden]` 规则 |
| 卡片高度算对了、卡底还是压到底部导航条上 | `.qw-agent-main` 是默认的 `content-box`，`height` 不含上下 30px 内边距，实际比预期高 32px | 显式 `box-sizing:border-box` |
| 视口变高后卡底反而溢出更多（1440×900 时压住底栏 9px） | 页面有 `zoom:1.08`，卡片按 `100vh - 常数` 长高时会以 1.08 倍速度超过视口，固定常数只在 720 高度下刚好 | 高度改成 `calc((100vh - 218px) / 1.08)`：218 = 顶栏 + 页头 + 底部导航条 + 9px 余量，再除页面缩放 |
| （顺手清理）`xiaowen.svg` / `cheese-cat.svg` 两个无人引用的旧文件 | 上一轮 `Remove-Item` 被策略拦下 | 改用 `[System.IO.File]::Delete()` 删除，已从「已知问题」移除 |

**验证**

- `node tests/run-all.js` → **10/10 套件全绿**；`tests/page-check.js` 单跑 66 条断言通过；`agent.js` 过了 `node --check`；`space.css` 花括号 863/863 配平。
- 无头 Edge（`--headless=new --no-sandbox --virtual-time-budget`）在 1280×720 下实测前后对比：

| 位置 | 对话区 | 语音条 | 卡底 vs 底栏顶 |
|------|--------|--------|---------------|
| 独立智能体页 | 160 → **221**（开聊后灵感条收起再多约 50） | 116 → **51**（收成一行） | 646 < 655 ✅ |
| 右下角「小问快问」 | 107 → **221** | 182 → **89** | 面板 49~574，完整可见 ✅ |
| 讲题课堂内嵌面板 | 180 → **268** | 128 → 128（麦克风整行保留） | 面板在页内可滚动 ✅ |

- 多分辨率回归（卡底与底栏顶的间距）：1024×768、1280×720、1280×640、1440×900、1920×1080 全部保持 **9px**，不再随视口高度漂移。
- 截图确认：三处对话区内容不再被裁切、语音条只占一行/两行、语音设置浮层默认收起、右下角小问不再遮挡页面。
- 未验证（需要人工）：真实麦克风下的语音链路（同上一轮，自动化测不了）。

### 2026-09-14（第 4 天）首页伴学卡重叠修复 · 智能体页左栏收窄 · 开场白与落款 bug

**当天需求**

1. 首页有元素重叠，修掉。
2. 智能体对话页左栏偏宽、对话框偏小：缩小左栏，把宽度让给对话框。

**改动（按时间）**

| 时间 | 改动 | 说明 |
|------|------|------|
| 10:57 | `frontend/src/scripts/app.js`（第 316 行） | 首页「伴学领航员 · 小问」卡片补回 `<img src="/assets/images/mascot/xiaowen/idle.webp" data-qw-xiaowen="follow" alt="小问领航员">` 与 `</span>` 闭合标签（上一轮替换小问形象包时漏了这一处，另外 6 处都已正确）。文件 52884 → 52989 字节 |
| 10:57 | `frontend/src/styles/space.css`（25.2 节，第 1821 行） | `.qw-agent-page-grid` 桌面两栏 `336px → 280px`、`gap 22px → 18px`；宽度全部让给右栏对话框。文件 113440 → 113515 字节，花括号 863/863 配平 |
| 11:04 | `backend/src/agent/personas.js`（`list()`，第 138 行） | 补 `intro: persona.intro`。接口 `/api/agent/personas` 原来只吐 `id/name/icon/tagline/abilities/quickReplies`，前端拿不到开场白 |
| 11:04 | `frontend/src/scripts/agent.js`（第 157 行） | `speakerVerb()` 用户侧返回值 `'你的回答'` → `'的回答'`（模板里已经拼了 `'你'`，重复了一个「你」） |

**修掉的 bug（本轮真实踩到的）**

| 问题 | 原因 | 修法 |
|------|------|------|
| 首页底部「伴学领航员」卡片正文被压成 77px 竖排、文字盖到下一张卡上 | **不是 CSS 问题，是标签没闭合**：`app.js` 第 316 行只有 `'    <span class="sq-companion-avatar">'`，既没放 `<img>` 也没写 `</span>`。浏览器自动闭合后，紧随其后的 `<div class="sq-companion-body">` 被嵌进了 74px 的头像圆里 | 补上 `<img>` 与 `</span>` |
| 智能体页一进来，小问的开场白气泡是**空的**（只有「🤖 11:06 · 小问轻声提问」，没有正文） | 前端 `greet()` 读 `current.intro`，但后端 `personas.list()` 没输出 `intro` 字段 → `undefined` → 气泡文本被写成空串。受影响两处：首次进入 `agent.js:764`、切换角色的开场白 `agent.js:656` | 后端 `list()` 补 `intro`，前端两处调用点不用改 |
| 自己发出去的消息落款显示「你你的回答」 | `agent.js:417` 模板已经拼了 `'你'`，而 `speakerVerb('user')` 又返回 `'你的回答'` | 用户侧改返回 `'的回答'`，拼出来是「你的回答」 |

**验证**

- 浏览器实测首页（`pages/student/home.html`）：`.sq-companion-body` 是否被嵌进头像 → `true` 改后 **`false`**；宽度 **77px → 711px**；头像节点从「无 `<img>`」变成「有 `<img>`（`idle.webp`）」；`.sq-companion` 子节点从只有 `avatar` 变成 `avatar` + `body`。截图确认底部不再重叠。
- 浏览器实测智能体页（`pages/student/agent.html`，1070×746 视口）：`grid-template-columns` 实测由 `336px` 变 `280px`；右栏对话框宽度 **+60px**；`document.documentElement.scrollWidth === clientWidth === 1070`，**无横向溢出**；页头/卡底位置不变，不压底部导航条。控制台无 error / warn。
- 智能体端到端：在输入框发「我不太懂 k 是什么意思」→ 用户气泡落款为「11:01 · **你的回答**」；小问回复正常成段（`provider=rules`，本机外网受限时按设计降级）；重载后开场白正文正常显示（「你好，我是问数的讲题老师 🎓 …」）。自动滚到底部生效（`scrollTop + clientHeight >= scrollHeight`）。
- `node tests/run-all.js` → **10/10 套件全绿**（其中 `agent-ui-smoke` 23 条、`xiaowen-ui-smoke` 26 条、`classroom-ui-smoke` 12 条）；`app.js` / `agent.js` / `personas.js` 均过 `node --check`。
- 未验证（需要人工）：真实麦克风下的语音链路；≥1366px 宽屏下的观感（本机视口只有 1070px）。

### 2026-09-14（第 4 天 · 下半场）智能体对话框加高 · 上下工具栏压缩 · 对话贴底修复

**当天需求**：智能体页的对话框还是偏小——把上下两侧的选项条、工具栏、打字输入框压小一点，把高度让给真正的对话区。

**改前 / 改后实测（1280×720 视口；页面有 `zoom:1.08`，下表数值已折算回 CSS px）**

| 区块 | 改前高 | 改后高 | 说明 |
|------|-------|-------|------|
| `.qw-agent-main` 卡片 | 465 | **465** | 卡片高度由 `calc((100vh - 218px)/1.08)` 决定，不动；卡底 591、底栏顶 607，**16px 余量保持不变** |
| `.qw-agent-head`（讲题老师 / 清空对话） | 59 | **53** | 内边距 9→6，头像 34→30，清空键 12px→11.6px |
| **`.qw-agent-stream`（对话区）** | **211** | **255** | **+44px（+21%）**；学生发过第一条消息后灵感条自动收起，进一步涨到 **290px（+79 / +37%）** |
| `.qw-agent-quick`（推荐灵感） | 30 | **28** | 胶囊内边距 8px 14px → 5px 12px |
| `.qw-agent-voice`（语音操作条） | 49 | **35** | 麦克风 / 键盘输入 / 语音播报 / 语音设置 / 喊「小问」唤醒 压到 35px，1280 宽下**一整行放得下**（改前会折到 49px） |
| `.qw-agent-input`（打字输入） | 49 | **42** | 壳内边距 6px→4px，发送键 8px 18px → 7px 16px |
| `#agentPanel` 行间距 | 9 | **7** | 5 个可见子元素，省下 8px |

**改动（按时间）**

| 时间 | 改动 | 说明 |
|------|------|------|
| 15:04 | `frontend/src/styles/space.css`（25.2 节） | `.qw-agent-main` 内边距 `14px 18px 16px → 10px 18px 12px`；`#agentPanel` 行间距 `9px → 7px` |
| 15:04 | `frontend/src/styles/space.css`（25.2 节） | `.qw-agent-head` 内边距 `9px 12px → 6px 12px`、`gap 10px → 9px`；`.qw-agent-avatar` `34px → 30px` |
| 15:04 | `frontend/src/styles/space.css`（25.2 节） | `.qw-agent-quick` `gap 7px→6px`、`padding-bottom 2px→0`；`.qw-agent-chip` `padding 8px 14px → 5px 12px`、字号 `12.8px → 12.2px` |
| 15:04 | `frontend/src/styles/space.css`（25.2 节） | `.qw-agent-voice` `gap 8px→6px`；`.qw-agent-mic` `padding 10px 15px → 7px 13px`、字号 `13.4px→13px`；`.qw-agent-kbd/.qw-agent-speak/.qw-agent-vset` `padding 10px 12px → 7px 11px`、字号 `12.6px→12.2px`，并把 `.qw-agent-wake` 并入同一组（对话框里 5 个键一起压） |
| 15:04 | `frontend/src/styles/space.css`（25.2 节） | `.qw-agent-input` `padding 6px 6px 6px 13px → 4px 5px 4px 12px`；新增桌面覆写 `.qw-agent-send { padding: 7px 16px; font-size: 12.6px; }` 与 `.qw-agent-clear { padding: 4px 11px; font-size: 11.6px; }`。文件 113515 → 113690 字节，花括号 865/865 配平 |
| 15:04 | `frontend/src/scripts/agent.js`（`scroll()`） | 追加一帧 `requestAnimationFrame` 二次贴底。文件 38270 → 38567 字节 |

**修掉的 bug（本轮真实踩到的）**

| 问题 | 原因 | 修法 |
|------|------|------|
| 对话区明明还有 21px 可滚，最后一行（时间戳「15:03 · 小问轻声提问」）却被裁在框底外面 | `greet()` 先 `push()`（此时 `scroll()` 贴底，`scrollHeight` 还没把灵感条算进去），紧接着 `renderQuick()` 才把「推荐灵感」条插进来——灵感条占 30px 高度 + 7px 行间距，把对话区压矮 37px，而这次布局变化之后**没有再调 `scroll()`**，`scrollTop` 就停在 0 | 在 `scroll()` 里再挂一帧 rAF 贴底：同一轮任务里 `push()` 之后的 `renderQuick()` 已经执行完，rAF 拿到的是最终高度 |

**验证**

- 浏览器实测（1280×720）：改前对话区 211 → 改后 **255**；发一条消息后 **290**。卡片底部始终停在 591、底栏顶 607，**16px 余量不变**；`scrollWidth === clientWidth === 1280`，无横向溢出；控制台无 error / warn。
- 发消息端到端回归：用户气泡落款「15:05 · **你的回答**」，小问按引导式教学给出「第 1 级 · 概念提醒」；`scrollTop 0 → 20`，`atBottom` 由 `false` 变 **`true`**。
- 窄屏回归（1070×746）：语音条折成两行（76px），对话区仍有 **273px**，无横向溢出。测完已 `viewport.reset()` 还原。
- `node tests/run-all.js` → **10/10 套件全绿**；`agent.js` 过 `node --check`。
- 未验证（需要人工）：≥1600px 超宽屏下工具栏是否会更宽松（本机视口最大 1280）。

### 2026-09-14（第 4 天 · 夜场）智能体页再压缩 · 错题订正页重构（错题本 + 错因雷达图）· 星球跳转核查

**当天需求（一条消息里三条要求）**

1. 智能体页：上下那些选项条 / 工具条 / 输入框再小一点，把高度都让给对话框。
2. 错题订正页：完善这一页，图形紧凑一些，并补上「错题本」和「错因雷达图」。
3. 首页「知识星系」：点对应的星球要跳到对应的讲题课堂。

**需求 1 · 智能体页再压缩（1075×750 视口；页面有 `zoom:1.08`，下表已折算回 CSS px）**

| 区块 | 改前高 | 改后高 | 关键改法 |
|------|-------|-------|----------|
| 页头 `h1` 字号 | 25px | **21px** | `.qw-agent-page-head` 的 `gap 18→14`、`margin-bottom 10→6` |
| `.qw-agent-main` 卡片 | 493 | **515** | 卡片高度公式 `calc((100vh - 218px)/1.08)` → `calc((100vh - 194px)/1.08)`，卡底距底栏 16 → **11px** |
| `.qw-agent-head`（角色行） | 53 | **45** | 内边距 `6px 12px → 4px 11px`，头像 `30 → 26px` |
| **`.qw-agent-stream`（对话区）** | **245** | **331** | **+86px（+35%）**；发过第一条消息后「推荐灵感」条让位，进一步到 **364px** |
| `.qw-agent-quick`（推荐灵感） | 25 | **27** | 胶囊内边距 `5px 12px → 4px 11px`、字号 `12.2 → 11.8px` |
| `.qw-agent-voice`（语音工具条） | 76（折两行） | **31（一整行）** | 5 个键统一压到 `padding 5px 10px`、字号 `11.8px`；并隐藏麦克风键后半句「，和小问交流」 |
| `.qw-agent-input`（打字输入） | 42 | **38** | 壳内边距 `4px 5px 4px 12px → 3px 4px 3px 11px`，输入字号 `14.2 → 13.6px` |
| 气泡 / 主动建议卡 | — | 各压一行 | `.qw-agent-bubble` 字号 `14.4 → 14px`；新增 `.qw-agent-main .qw-agent-suggest*` 一组压缩规则 |

**改动（需求 1）**

| 时间 | 改动 | 说明 |
|------|------|------|
| 15:22 | `frontend/src/styles/space.css`（25.2 节） | 上述 8 组数值全部落在这一节里；`main#agentApp.qw-page` 的 `padding-top 18→10`、`padding-bottom 10→8`，抵消 `nav.js` 给 `main` 加的 `pb-20`，避免页头被顶栏压住 |
| 15:22 | `frontend/src/styles/space.css`（25.2 节） | 新增 `.qw-agent-mic i { display: none; }`。语音条从两行变一行的**关键**就是这句：麦克风键里那半句「，和小问交流」在 1075px 宽下会把整条挤到第二行 |

**需求 2 · 错题订正页重构**

改前：`correction.js` 只有 74 行，整页是「一个大 hero + 一列大卡片 + 一列重讲记录」，卡片内边距 18/20px，没有错题本概念、也没有任何图形。

改后：

| 模块 | 内容 | 落地 |
|------|------|------|
| 概览数值条 | 错误总次数 / 错因种类 / 待重讲记录 / 订正率 | `.qwe-stats` × 4 |
| **错题本** | 按错因汇总的紧凑小卡：错因图标 + 名称 + 说明 + 次数徽章 + 频率条 + 订正动作 + 知识点标签 + 「去重讲 →」 | `.qwe-items` / `.qwe-item`，左右两栏自适应（`auto-fill minmax(236px,1fr)`） |
| **错因雷达图** | 自绘 SVG 五边形雷达（取前 6 类错因，以最高频错因归一化为 100%），配彩色图例 + 频率条 + 次数 | `causeRadar()` + `.qwe-legend` |
| 重讲记录 | 保留原来的 `.qw-err-session` 列表，整体压紧 | `#correctionApp .qw-err-*` 覆写 |
| 首屏卡 | 从 28px 内边距压到 `15px 20px`，`h2` 从 24px 压到 17.2px | `.qw-err-hero.qwe-hero` |

**改动（需求 2）**

| 时间 | 改动 | 说明 |
|------|------|------|
| 15:35 | `backend/src/routes/learning.js`（`GET /api/learning/error-book`） | 按错因分桶时补 `knowledgePointIds: []`，与原有 `knowledgePoints`（名称）同步 push `kp.id`。**原因**：错题本小卡要放「去重讲 →」，跳 `classroom.html?kp=kpX` 需要的是 id，光有名称前端还得再查一次接口 |
| 15:35 | `backend/src/routes/learning.js` | 返回值新增 `errorSignals`（错因总处数）、`fixRate`（订正率 %）、`causes`（前 6 类错因，带 `count` 与归一化 `ratio`），供概览条与雷达图直接用。文件 7542 → 8230 字节 |
| 15:42 | `frontend/src/scripts/correction.js` | 整文件重写（74 行 → 约 250 行）：新增错因配色/图标常量表（7 类错因固定顺序，保证雷达图、图例、错题本三处同色）、`causeRadar()` 自绘 SVG 雷达、`bookItem()` 紧凑小卡、概览条与两栏栅格。`causes` 缺失时前端自己按 `items` 归一化兜底。文件 3614 → 10366 字节 |
| 15:48 | `frontend/src/styles/space.css`（新增 25.6 节） | 追加约 150 行：`.qwe-*` 全套样式 + 夜色主题覆写 + `<700px` 窄屏（数值条改 2×2）。文件 114320 → 121080 字节，花括号 934/934 配平 |

**需求 3 · 星球跳转核查（结论：本来就是对的，未改代码）**

- 用户给的 4 图是首页「知识星系」区块（`app.js:311` 的 `sq-section-head` + `app.js:247` 的 `.sq-planet` 卡），不是底部「星球」tab 的 `planet.html`（那一页标题是「选择一颗星球，开始复习」，且刻意跳 `quiz.html` 答题）。
- 三张卡的 HTML 本来就已经是 `<a class="sq-planet" href="./classroom.html?kp=kpX">`；实测三张卡依次点到 `classroom.html?kp=kp1 / kp2 / kp3`，`.sq-sky` / `.sq-scene` 装饰层的 `pointer-events` 都是 `none`，不会挡点击（`.sq-planet.locked` 也只是降透明度，不禁用）。**所以无需改动**。

**修掉的 bug（本轮真实踩到的）**

| 问题 | 原因 | 修法 |
|------|------|------|
| 错题本小卡做不出「去重讲 →」 | `/api/learning/error-book` 的桶里只有 `knowledgePoints`（知识点**名称**），没有 id；`classroom.html` 要的是 `?kp=kp3` 这种 id | 后端分桶时同步收 `knowledgePointIds`，前端取第一个 id 拼链接 |

**验证**

- 需求 1：浏览器实测 1075×750，`head 45 / stream 331 / quick 27 / voice 31（一整行）/ input 38 / main 515`；`scrollWidth === clientWidth === 1075` 无横向溢出，整页 `scrollHeight === innerHeight === 750` 不滚动。
- 需求 2：浏览器实测 `pages/student/correction.html`：错题本 5 张卡、雷达图 5 条轴、图例 5 行、重讲记录 5 条；点第 1 张「去重讲 →」（公式错误）→ 落地 `classroom.html?kp=kp3`（正确）；日夜两套主题各截图确认；700×900 窄屏下 `qwe-grid` 由两栏变一栏、数值条 2×2，无横向溢出（测完已 `viewport.reset()`）。控制台无 error / warn。
- 需求 3：三张卡逐一点击实测，分别落到 `kp1 / kp2 / kp3` 的讲题课堂。
- 接口：`GET /api/learning/error-book`（演示账号 u1）→ `totalWrong=7 sessionCount=11 errorSignals=6 fixRate=54`，`causes=公式错误2(100%) / 概念不清2(100%) / 符号混淆1(50%) / 条件识别1(50%) / 计算失误1(50%)`，`items[].knowledgePointIds` 分别是 `[kp3] / [kp2,kp1] / [kp1] / [kp1] / [kp3]`。
- `node tests/run-all.js` → **10/10 套件全绿**（`agent-ui-smoke` 23 条、`xiaowen-ui-smoke` 26 条、`classroom-ui-smoke` 12 条）；`learning.js` / `correction.js` 均过 `node --check`。
- 未验证（需要人工）：真实麦克风链路；≥1366px 宽屏下错题本小卡会不会变成 3 列（本机视口只有 1075px）。

### 2026-09-14（第 4 天 · 深夜场）VIP 权益对齐 · 免费版 AI 对话配额（每天 10 次）

**当天需求（一条消息里两条要求）**

1. 看 VIP 权益的部分，调整对应的代码（附参考图：三张方案卡的权益文案）。
2. 免费版只有每天 10 次免费智能体对话，可用星尘 / 星钻兑换次数。

**第 1 条 · 参考图逐条核对（结论：文案本来就一致，缺的是「对话配额」这条权益）**

| 方案 | 参考图权益 | 代码 `vipService.js` 的 PLANS | 本轮处理 |
|------|-----------|------------------------------|----------|
| 启航试学体验 ¥0 / 永久有效 | 1 个核心知识星球 / 3 讲沉浸式课堂 / 基础学情速览（3 条 ✓）+ 2 条 ✕ | 与参考图逐字一致 | 补第 4 条 ✓「小问对话每天 10 次，星尘 / 星钻可兑换加次」 |
| 星际月度领航舱 ¥29 月（原价 ¥40） | 5 条 ✓，含「小问不限次苏格拉底式启发」 | 一致 | 方案对象加 `quota: '小问对话不限次'` / `quotaKind: 'unlimited'` |
| 星际年度全通舰长 ¥259 年（原价 ¥348 · 最受欢迎省 ¥89） | 5 条 ✓ | 一致 | 同上 |

**第 2 条 · 配额规则（规则全在后端，前端只负责展示与兑换）**

| 项 | 数值 | 落地位置 |
|----|------|----------|
| 免费版每天免费次数 | **10 次**（可用环境变量 `AGENT_FREE_DAILY` 覆盖；测试里调成 1000） | `config/index.js` → `agentQuota.freeDaily` |
| 星尘兑换 | **20 星尘 = 5 次** | `agentQuota.redeem[0]`（`unit: 星尘`，`cost: 20`，`rounds: 5`） |
| 星钻兑换 | **1 星钻 = 10 次** | `agentQuota.redeem[1]`（`unit: 星钻`，`cost: 1`，`rounds: 10`） |
| 会员（月度 / 年度） | **不限次**，也不走兑换 | `agentQuotaService.membershipOf()` → `repos.vip.isActive()` |
| 兑换到的次数 | **当天有效**，与免费次数合并计数，跨天一起清零 | `qw_gamification.agent_extra` 随 `agent_date` 在 `rollover()` 里归零 |

**改动（后端）**

| 时间 | 文件 | 说明 |
|------|------|------|
| 20:12 | `backend/src/config/index.js` | 新增 `agentQuota: { freeDaily, redeem: [...] }`，免费额度与两档兑换价集中到配置层，后续改价只动这一处 |
| 20:14 | `backend/src/db/schema.js` | `qw_gamification` **加 4 列**（不是加表）：`agent_date`（当天日期）/ `agent_used`（今天用掉几次）/ `agent_extra`（今天兑换到几次）/ `crystal_used`（累计花掉的星钻），插在 `streak_days` 之前。**表数仍是 8 张** |
| 20:18 | `backend/src/services/agentQuotaService.js` | **新文件**（约 180 行）：`state / consume / refund / redeem / membershipOf`。导出 `FREE_DAILY` 与 `redeemOptions` 供路由与前端复用 |
| 20:22 | `backend/src/services/gamificationService.js` | `crystalsOf()` 由「只返回总数」改为 `{ total, spent, available, ... }`；`spent = min(crystal_used, total)`、`available = max(0, total - spent)`，`note` 里带上「已花掉 N，可用 M」 |
| 20:26 | `backend/src/routes/agent.js` | `POST /chat` 与 `POST /chat/stream` 进入前先 `consume()`：不够就返回 `429 { code: 'AGENT_QUOTA_EXHAUSTED', quota }`；成功响应带 `quota`；异常路径 `refund()` 把这一次还回去。新增 `GET /quota`、`POST /quota/redeem`（用 `res.status(result.status \|\| 200)`，因为服务层返回 `{ status, body }` 信封） |
| 20:30 | `backend/src/services/vipService.js` | 三个方案各加 `quota` / `quotaKind`；免费方案的 `features` 由 3 条 → 4 条 |

**改动（前端）**

| 时间 | 文件 | 说明 |
|------|------|------|
| 20:33 | `frontend/src/scripts/auth.js` | **关键 bug 修复**：`request()` 与 `stream()` 原来失败时只 `throw new Error(msg)`，把 429 响应体里的 `quota` 丢了 → 前端拿不到配额去弹兑换卡。改成把 `err.status` / `err.data` 一起挂到 Error 上 |
| 20:35 | `frontend/src/scripts/agent.js` | ① 头部加配额角标 `#qwAgentQuota`；② 新增 `renderQuota / quotaCardHtml / bindRedeem / showQuotaCard`，点角标即可展开兑换卡；③ `catch (err)` 里**新增 429 分支**（在降级重试之前）：弹「今天的免费对话次数用完了」气泡 + 兑换卡，并 `return`，**不会触发降级重试导致重复扣次**；④ `bootstrap()` 首屏先拉一次 `GET /api/agent/quota` |
| 20:36 | `frontend/src/scripts/agent.js` | 兑换成功后 `window.dispatchEvent(new CustomEvent('qw-wallet-change'))`，让顶栏星尘 / 星钻立刻跟着变（本轮补的，之前要刷新页面才更新） |
| 20:37 | `frontend/src/scripts/nav.js` | ① 顶栏星钻改显示**可花的** `crystals.available`（星钻是成就折算值，花掉一部分后总数不该继续充当余额）；② 把拉钱包的代码抽成 `syncWallet()`，并监听 `qw-wallet-change` 事件 |
| 20:38 | `frontend/src/scripts/vip.js` | `planCardHtml()` 在价格行后插一条 `qw-vip-plan-quota`：免费卡是虚线灰条「⚡ 小问对话每天 10 次」，会员卡是金色实线「👑 小问对话不限次」 |
| 20:39 | `frontend/src/styles/space.css`（新增 25.7 节） | 追加约 120 行：`.qw-agent-quota`（含 `.is-low` / `.is-empty` / `.is-vip`）、`.qw-quota-card` 全套（头部 / 余额条 / 两个兑换按钮 / 锁态 / 脚注 / 去会员页链接）、`.qw-vip-plan-quota`（含 `.is-unlimited`）+ 夜色主题覆写 + `<560px` 单列。文件 121080 → 126533 字节，花括号 981/981 配平 |

**设计决策（为什么这么写）**

- **星钻扣的是「偏移量」不是「余额」**：星钻本质是「解锁徽章 + 掌握知识点」折算出来的成就值（`crystal_used` 与总数分开存）。如果直接扣总数，就会出现「刚花 1 钻兑换，回头解锁一个徽章又涨回去」的怪现象；存偏移量则「赚」和「花」互不打架，`available = total - spent`。
- **兑换次数与免费次数合账**：`agent_extra` 只记「今天兑换到几次」，实际拦截条件是 `used >= freeDaily + extra`，所以免费 10 次用完再兑换 10 次，当天一共能问 20 次，跨天一起归零。
- **降级重试必须跳过 429**：智能体在外网不通时会降级到规则引擎并重试一次；如果 429 也走这条路，就会「挡一次、重试一次、再扣一次」，等于白扣用户两次配额。

**修掉的 bug（本轮真实踩到的）**

| 问题 | 原因 | 修法 |
|------|------|------|
| 配额用完点了兑换，前端不弹兑换卡、只报一句模糊错误 | `auth.js` 的 `request()` 把 429 的响应体丢了，`err.data.quota` 读不到 | `request()` / `stream()` 失败时把 `status` 与响应体一起挂到 Error |
| 兑换成功后顶栏星钻还是旧数字 | 星尘 / 星钻只在页面加载时拉一次，兑换走 XHR 不刷新页面 | 兑换成功后广播 `qw-wallet-change`，`nav.js` 的 `syncWallet()` 接住重拉 |
| 连点配额角标会在对话里堆出好几张兑换卡 | `showQuotaCard()` 每次都 `appendChild` 一个新节点 | 追加前先找同容器里已有的 `.qw-quota-card` 并移除，保证只留一张 |

**验证**

- 接口：`GET /api/agent/quota`（演示账号 u1）→ `freeDaily=10, limit=10, used=0, remaining=10, balance={dust:128, gem:12}`，`redeem=[{id:'dust',label:'20 星尘 = 5 次',affordable:true},{id:'gem',label:'1 星钻 = 10 次',affordable:true}]`。
- 接口：`GET /api/vip/plans` → free `quota=小问对话每天 10 次` / `quotaKind=limited` 且 features 4 条；monthly、yearly 均为 `小问对话不限次` / `unlimited`。
- 浏览器实测（把 live 后端的 `AGENT_FREE_DAILY` 临时调成 2，走完「用完 → 兑换」全程）：角标 `⚡ 2/2 → 1/2 → 0/2`；第 3 条消息**不扣次**，弹出兑换卡 +「今天的免费对话次数用完了」气泡；点「💎 1 星钻 = 10 次」→ 角标 `⚡ 10/12`、星钻 12 → 11（**顶栏当场同步**，不用刷新）；再发消息正常回答。
- 浏览器实测 `pages/student/vip.html`：免费卡灰色虚线配额条「⚡ 小问对话每天 10 次」、月 / 年卡金色「👑 小问对话不限次」，与参考图三张卡逐条对得上；日夜两套主题各截图确认；控制台无 error / warn。
- `node tests/run-all.js` → **11/11 套件全绿**，其中新增的 `agent-quota-smoke.js` 29 条 check 全 PASS（覆盖免费次数、429 文案、新账号 0 余额被拒、星钻 / 星尘两档兑换、会员不限次连问 15 次、会员兑换被拒、方案配额文案）。
- `agentQuotaService.js` / `agent.js` / `nav.js` / `auth.js` / `vip.js` 均过 `node --check`。

### 2026-09-14（第 4 天 · 加时场）VIP 星球解锁 · 智能体布局 · 星尘星钻重构 · 星系地图 · 等级体系

**当天需求（一条消息里五条要求，附 4 张参考图）**

1. 首页知识星系：落实 VIP 服务——普通登录用户（免费版）只开放第一颗星球；答题复习页同样受限。
2. 智能体页：去掉「第一次见面，我先看看你的进度」建议卡，把高度让给对话框。
3. 星尘 / 星钻优化：星钻改为等值充值获得；兑换小问次数改成 50 星尘 = 1 次、2 星钻 = 1 次；配一份收支记录。
4. 答题星球页：背景做成宇宙星系，每个星系 = 一个知识点，前面的在前、后面的显示为待解锁。
5. 点等级可跳转等级界面，能看经验值、经验获取方式，以及各等级的名称与图标。

**第 1 条 · 解锁规则（后端唯一口径，落在 `accessService.js`）**

| 项 | 数值 | 说明 |
|----|------|------|
| 免费版开放星球数 | **1** | `FREE_PLANET_COUNT = 1`，按 `order` 取前 N 个，也就是「一次函数」 |
| 数据出口 | `/api/dashboard` 每个知识点带 `locked`，响应另给 `access` 快照 | 首页星球卡与星系地图都读它，前端不硬编码 |
| 硬拦截 | `/api/quiz/questions` 与 `/api/quiz/submit` 对锁住的星球直接 **403 `KP_LOCKED`** | 防止绕过前端直接刷题；响应体带 `unlock` 快照供前端弹引导 |
| 讲题课堂不锁 | `/api/learning/*` 不受限制 | 免费版权益本来就写明含 3 讲沉浸式课堂，是试学钩子，锁了等于砍掉拉新入口 |

**第 3 条 · 星尘 / 星钻口径变更（这次改的是「怎么赚」）**

| 项 | 改前 | 改后 |
|----|------|------|
| 星钻来源 | 徽章 ×2 + 掌握知识点 ×2 的**折算值** | **等值充值**（1 元 = 1 星钻，不做赠送），落 `crystal_recharge` 列 |
| 星钻可用额 | `total - spent`，但 total 会随徽章自动上涨 | `crystal_recharge - min(crystal_used, recharge)`，只认充值 |
| 星尘兑换 | 20 星尘 = 5 次 | **50 星尘 = 1 次** |
| 星钻兑换 | 1 星钻 = 10 次 | **2 星钻 = 1 次** |
| 收支记录 | 无 | 新增 `qw_wallet_log`：签到 / 答对标准题 / 完成讲题课堂 / 兑换都会写一条流水，`/api/wallet/logs` 返回「余额 + 收支汇总 + 最近 N 笔」 |

**改动（后端）**

| 时间 | 文件 | 说明 |
|------|------|------|
| 21:40 | `backend/src/db/schema.js` | `qw_gamification` 加 1 列 `crystal_recharge`（累计充值星钻）；**新增 2 张表** `qw_crystal_order`（星钻充值订单）与 `qw_wallet_log`（星尘 / 星钻收支流水）。**表数 12 → 14** |
| 21:42 | `backend/src/repositories/walletRepository.js` | **新文件**：流水增查、订单建单 / 查单 / 改状态、`sumByWallet` 汇总；种子从 `mockData.walletLogs` / `crystalOrders` 灌入 |
| 21:43 | `backend/src/repositories/index.js` | 注册 `wallet` 仓储 |
| 21:45 | `backend/src/services/walletService.js` | **新文件**：`packs()`（¥6 / ¥30 / ¥68 / ¥128，1 元 = 1 星钻）、`overview()`（余额 + 汇总 + 流水）、`createRecharge()` / `payRecharge()`。演示账号 `13800000001` 免扫码直接到账；订单同时写 `payCode` 与 `code`，复用 VIP 付款码弹窗 |
| 21:47 | `backend/src/services/accessService.js` | **新文件**：`unlockedIds / isUnlocked / summary / decorate / block`，`FREE_PLANET_COUNT = 1` |
| 21:50 | `backend/src/services/gamificationService.js` | `crystalsOf()` 重写：星钻只认充值（`total = crystal_recharge`，`available = recharge - spent`）；徽章与掌握度降级为「成就展示」，不再折算星钻；`levels()` 多返回 `expRules` |
| 21:52 | `backend/src/config/index.js` | `agentQuota.redeem` 改为 `[{ id: dust, cost: 50, rounds: 1 }, { id: gem, cost: 2, rounds: 1 }]` |
| 21:54 | `backend/src/services/agentQuotaService.js` | 兑换成功后写一条 `redeem` 流水；余额不足的文案由「解锁徽章折算星钻」改成「星钻由等值充值获得（1 元 = 1 星钻）」 |
| 21:56 | `backend/src/models/mockData.js` | 新增 `EXP_RULES`（5 条经验获取方式）、`walletLogs`（12 条）、`crystalOrders`（2 笔已支付）；`gamification.u1` 补 `crystalRecharge: 12` |
| 21:58 | `backend/src/routes/dashboard.js` | 知识点列表改用 `accessService.decorate()`（带 `locked`），响应加 `access: summary(user)` |
| 22:00 | `backend/src/routes/quiz.js` | `/questions` 与 `/submit` 对锁住的星球返回 **403 `KP_LOCKED`**；答对标准题发 10 星尘并写流水 |
| 22:01 | `backend/src/routes/gamification.js` | 签到成功写一条星尘流水 |
| 22:02 | `backend/src/routes/learning.js` | 结课发经验加 `firstTime` 守卫（同一会话重复上报不再重复发经验），并写星尘流水 |
| 22:04 | `backend/src/routes/wallet.js` | **新文件**：`GET /packs`、`GET /logs`、`POST /recharge`、`POST /recharge/pay` |
| 22:05 | `backend/src/app.js` | 挂载 `/api/wallet` |
| 22:30 | `backend/src/services/vipService.js` | 免费方案权益文案对齐解锁口径：「1 个核心知识星球（**勾股定理**启航篇）」改成「（**一次函数**启航篇）」，`locked` 里「一次函数与内角和」改成「三角形内角和」 |

**改动（前端）**

| 时间 | 文件 | 说明 |
|------|------|------|
| 22:10 | `frontend/src/scripts/app.js` | ① `renderHome()`：新增 `isLocked()` / `accessBanner`（`.sq-lock-banner` + 👑 开通领航员），锁住的星球卡带 `is-vip-locked` 并改跳 `vip.html`，英雄区第二格在免费版改为「已开放星宿 1 / 3 宿」；② `renderPlanet()` 整段重写为**星系地图**；③ `renderQuiz()` 的 `catch` 新增 403 `KP_LOCKED` 分支，渲染 `.qw-locked-panel` 解锁引导；④ `renderLevel()` 新增「经验怎么来」（`expRules`）与等级阶梯区间 |
| 22:12 | `frontend/src/scripts/agent-page.js` | `suggest: true` → **`false`**：去掉主动建议卡，把高度让给对话框（组件本身仍支持 `suggest`，课堂内嵌面板还在用） |
| 22:14 | `frontend/src/scripts/agent.js` | 兑换卡脚注加「📜 收支记录 / 星钻充值」链接（跳 `vip.html#wallet`） |
| 22:16 | `frontend/src/scripts/vip.js` | 新增「星钻充值 · 收支记录」整块：4 档充值卡、余额卡、收支汇总、最近 12 笔流水、下单即支付；`location.hash === #wallet` 时自动滚到该块 |
| 22:20 | `frontend/src/styles/space.css`（新增 25.8 / 25.9 / 25.10 / 25.11 四节） | 追加约 470 行：免费版解锁条与锁卡、`.qwp-*` 星系地图（三层星云 + 两圈轨道 + 闪烁星点 + `--depth` 纵深感）、等级页 `.qw-exp-*`、钱包 `.qw-wallet-*`，含夜色主题覆写与窄屏。文件 126533 → 142025 字节，花括号 1145/1145 配平 |

**设计决策（为什么这么写）**

- **星钻改等值充值，根因是「赚不到」**：原来星钻是「徽章 ×2 + 掌握 ×2」的**折算值**——学生解锁徽章并不会真的让余额变多，只是个展示数字，拿它当兑换余额会与「徽章数一变余额就跟着变」打架。改成充值后星钻有了唯一来源 `crystal_recharge`，`crystal_used` 仍存偏移量，`available = recharge - spent` 的两段式口径不变。
- **讲题课堂不锁**：免费版权益本来就写明含 3 讲沉浸式课堂，锁掉课堂等于把试学钩子砍了。所以锁只落在「练题」这一环（`/api/quiz/*`），课堂与智能体保持开放。
- **锁定判断放后端**：`/api/dashboard` 下发 `locked`、`/api/quiz/*` 直接 403，前端只做展示与引导。这样即使有人手动改 URL 也刷不到锁住的题。
- **英雄区口径跟着权限走**：免费版第二格显示「已开放星宿 1 / 3 宿」而不是「已点亮星宿 2 / 3 宿」——后者统计的是历史学习记录（演示数据里这个学生确实学过内角和），会和旁边两颗挂锁的星球自相矛盾。

**修掉的 bug（本轮真实踩到的）**

| 问题 | 原因 | 修法 |
|------|------|------|
| 首页英雄区写「已点亮星宿 2 / 3 宿」，下面两颗星球却挂着锁 | 「点亮」统计的是历史学习记录，与 VIP 权限无关 | 免费版改为按 `access.unlockedCount` 显示「已开放星宿 1 / 3 宿」，并挂「👑 解锁全部」小字 |
| VIP 免费方案卡写「1 个核心知识星球（勾股定理启航篇）」，实际开放的却是「一次函数」 | 文案是早期「免费送勾股定理」的设计残留，`FREE_PLANET_COUNT` 落地后没同步 | 文案改成「一次函数启航篇」，`locked` 里「一次函数与内角和」改成「三角形内角和」 |
| 讲题课堂结课后重复上报会重复发经验 | `learning.js` 的结课分支没判断会话是否已经是 `completed` | 加 `firstTime` 守卫，只有首次完成才发经验 |
| `agent-quota-smoke.js` / `layers-smoke.js` 还在用旧兑换价与旧表数 | 改价 / 加表后测试没跟着走 | 改成 50 星尘 = 1 次、2 星钻 = 1 次，表数 12 → 14 |

**验证**

- `node tests/run-all.js` → **11 / 11 套件全绿**（`agent-quota-smoke.js` 已按新兑换价改写，`layers-smoke.js` 表数改 14）。
- 接口实测（`13800000001` / `123456`）：`/api/dashboard` → `access = { vip: false, total: 3, unlockedCount: 1, unlockedIds: [kp1], label: 免费版 · 已开放 1 / 3 颗星球 }`；三个知识点 `kp1 locked=false`、`kp2` / `kp3 locked=true`。
- 接口实测：`/api/quiz/questions?knowledgePointId=kp2` → **403 `KP_LOCKED`**（带 `unlock` 快照）；`kp1` → 200 五题；`/api/quiz/submit` 交锁住星球的题同样 403；不带参数时只返回 kp1 的题。
- 接口实测：`/api/agent/quota` → `limit=10`、`balance={dust:128, gem:12}`、`redeem=[{label:50 星尘 = 1 次},{label:2 星钻 = 1 次}]`。
- 接口实测：`/api/wallet/packs` → 4 档（¥6 / ¥30 / ¥68 / ¥128，星钻数与金额 1:1）；`/api/wallet/logs?limit=40` → 12 条流水、余额 `{dust:128, gem:12}`。
- 接口实测（充值闭环）：`POST /api/wallet/recharge {packId: c30}` → 订单 `CR2026091445F50E8E`；`POST /api/wallet/recharge/pay` → `{paid:true, instant:true, receipt:{balance:{gem:42}}}`，**星钻 12 → 42**，流水里多一条充值记录。
- 接口实测：`/api/gamification/levels` → `current = Lv.3 解题能手 · 128 经验 · 本级区间 8 / 100`，`expRules` 5 条，等级阶梯 5 级。
- 浏览器实测（Playwright + Edge，视口 1280×880，日夜两套主题各截图）：首页免费版解锁条 + 两张锁卡（CTA 跳 `vip.html`）+ 英雄区「已开放星宿 1 / 3 宿」；`planet.html` 三颗星系纵深排列、后两颗挂锁与「开通领航员解锁」、底部统计条 `Lv.3 → 查看等级` 跳 `level.html`；`quiz.html?kp=kp2` 弹 `.qw-locked-panel`；智能体页建议卡已消失、对话区明显变长；`vip.html` 星钻充值 4 档 + 12 笔收支记录；`level.html` 经验获取 5 卡 + 五维雷达 + 5 级阶梯；控制台无 page error。
- 备注：Playwright 截图时控制台有 `net::ERR_NETWORK_ACCESS_DENIED`，是外网 CDN 字体被沙箱拦住，与本轮改动无关。

### 2026-09-15（第 5 天）部署准备 · 前端去 CDN 化 · 局域网可访问

**当天需求**

把网站部署出去，让别人也能用。

**体检结论（动手前先查出来的三个真实阻断点）**

| # | 问题 | 影响 |
|---|------|------|
| 1 | 12 个学生端页面都从外网加载 `@tailwindcss/browser@4`（jsdelivr）与 `lucide@latest`（unpkg） | 内网与离线环境图标全丢；国内网络下慢或超时 |
| 2 | `common.css` 的主题变量写成 Tailwind 的 `@theme {}` 块 | 这是 Tailwind 语法，浏览器当未知 at-rule **整段忽略**；变量实际由每页的 `<style type="text/tailwindcss">` 经 CDN 生成，所以一旦没网 `var(--color-*)` 全部取不到值 |
| 3 | `JWT_SECRET` 有代码内默认值、`.env` 里是真实大模型 Key、演示账号密码公开 | 直接公开部署可被伪造登录态、盗刷模型额度 |

**断网前后的实测对比（Playwright 拦掉所有非本机请求）**

| 指标 | 改造前 | 改造后 |
|------|--------|--------|
| `window.lucide` | false | true |
| 底栏图标 | 只有「星球」1 个（自绘 SVG 兜底），其余 5 个是空占位 | 6 个全在 |
| `--color-primary` | 空（未定义） | `#2BA4A0` |
| `body` 的 `min-height` | 0 | 880px（等于视口高） |

**改动**

| 时间 | 文件 | 说明 |
|------|------|------|
| 09:12 | `frontend/assets/vendor/lucide.min.js` | **新文件**（397450 字节）：把 Lucide UMD 包放进项目，图标不再依赖 unpkg |
| 09:14 | `frontend/pages/student/*.html`（12 个） | ① 删掉 Tailwind CDN 整行；② `unpkg.com/lucide@latest` 换成 `/assets/vendor/lucide.min.js`；③ 删掉每页冗余的 `<style type="text/tailwindcss">@theme{...}</style>` 块（变量改由 common.css 提供） |
| 09:18 | `frontend/src/styles/common.css` | ① `@theme {` 改成真实的 `:root {`，17 个 `--color-*` / `--font-*` 变量这才真正生效；② 新增 Tailwind 兼容层 4 条：`.min-h-screen` / `.font-body` / `.font-display` / `.font-medium`（`<body>` 上原本靠 CDN 提供的类） |
| 09:26 | `backend/src/app.js` | 启动横幅新增局域网访问地址：`lanAddresses()` 枚举 IPv4 并过滤虚拟机 / VPN / 蓝牙网卡，多条时提示选 WLAN / 以太网；同时提示 `http://` 下麦克风会被浏览器禁用 |
| 09:34 | `backend/.env.example` | **新文件**：所有 `process.env.*`（PORT / NODE_ENV / JWT_SECRET / LLM_* / AGENT_* / DB_* / CACHE_* / REDIS_URL / RL_*）的模板，含密钥生成命令 |
| 09:35 | `.gitignore` | **新文件**：排除 `node_modules`、`.env`（含密钥）、日志、`*.pem` |
| 09:37 | `Dockerfile` / `.dockerignore` | **新文件**：Node 20 alpine 镜像，先装依赖吃层缓存；同时拷 `backend/` 与 `frontend/`（后端按 `../../frontend` 定位前端目录）；带 HEALTHCHECK |
| 09:42 | `部署文档.md` | **新文件**：上线前必改 3 项 / 方案 A 局域网 / 方案 B 内网穿透 / 方案 C 云服务器（systemd + Nginx + certbot）/ 语音与 HTTPS / Docker / 内存数据落库路径 / 排错 / 自查清单 |

**设计决策（为什么这么写）**

- **去 Tailwind，而不是把它本地化**：实测 12 个学生端页面里 Tailwind 工具类的使用数是 **0**——它只贡献了 `@theme` 里的 17 个变量和 `<body>` 上的 `min-h-screen` / `font-body` 两个类。把变量搬进 `common.css` 的 `:root`（它本来就写着同名变量，只是用了无效的 `@theme` 语法）再补 4 条工具类，比往项目里塞一个约 200KB 的浏览器端 JIT 编译器干净得多。
- **只改学生端 12 个页面**：`frontend/pages/*.html` 下还有 9 个旧版页面（badge-popup、blackboard-classroom、checkin-points 等）确实在用 Tailwind 工具类，但全项目**没有任何地方引用它们**（rg 全仓 0 命中），是重构后的孤儿页；本轮不动，避免为死代码做无谓改造。
- **语音功能必须 HTTPS 是硬约束**：浏览器规定麦克风（`getUserMedia`）与语音识别（`SpeechRecognition`）只在安全上下文可用。所以 `http://192.168.x.x:3000` 下语音会自动隐藏（键盘输入不受影响）。要有语音就走内网穿透自带的 https、云服务器 certbot，或局域网自签证书。这条直接写进启动横幅和部署文档，免得部署完才发现「语音按钮不见了」。
- **部署脚手架按「换存储不动业务代码」的分层来写**：数据现在还在内存，落库路径是「`npm run db:schema` 生成 SQL → 实现 `db/table.js` 那层调用面的 MySQL 驱动 → `.env` 设 `DB_DRIVER=mysql`」，仓储层与业务层不用改。

**验证**

- 断网模拟（Playwright 拦截所有非 `localhost:3000` 请求，跑 home / agent / planet / level / vip / login 六个页面）：`window.lucide = true`、`i[data-lucide]` 占位 0 个、底栏 6 个图标全在、`--color-primary` 解析为 `#2BA4A0`、`body.minHeight = 880px`、`fontFamily = "Noto Sans SC", sans-serif`。整轮只剩 1 个外部请求被拦（`fonts.googleapis.cn`，字体走系统回退，不影响功能）。
- 截图比对：改造前底栏只有「星球」1 个图标，改造后 6 个齐全（均为离线状态）。
- 启动横幅实测输出 `http://10.1.126.184:3000/pages/auth/login.html   [WLAN]`，虚拟网卡 `Radmin VPN`（26.230.179.80）已被过滤。
- `GET /assets/vendor/lucide.min.js` → 200，397450 字节。
- `node tests/run-all.js` → **11 / 11 套件全绿**（含「页面与静态资源检查」「前端页面运行冒烟」）。
- `node --check backend/src/app.js` 通过。

**还没做（见第 3 节待办）**：数据仍在内存，公开部署前建议先落库；9 个孤儿页仍挂 Tailwind CDN。

### 2026-09-15（第 5 天 · 下半场）同行方案对照 · 手绘星球图形库 · 星座背景 · 安全加固 · 工程清理

**背景**：同组同学把他的「问数星途」部署到了 `http://47.98.251.194`。我把它的前端产物全部拉下来逐段读完
（9 个页面片段 + 一个 4003 行 / 166KB 的 `xingtu-api.js`），和本项目做了完整对照。
结论写进了 `同行方案借鉴分析.md`（231 行），自审问题写进了 `待改进清单.md`（278 行）。
一句话结论：**他的架构（静态页 + 运行期补丁脚本）不可取，但交互细节和教学可视化值得抄。**

**一、新增图形库 `frontend/src/scripts/space-art.js`（352 行，零依赖）**

纯函数式 SVG 图案生成器，不依赖任何第三方库与网络资源：

| API | 作用 |
|-----|------|
| `planet(spec)` | 手绘表情星球：径向渐变球体 + 陨石坑 + 表情（4 种心情）+ 光环穿插 + 星点闪烁 + 知识点专属道具 + 公式标签 |
| `constellation(spec)` | 星座连线（虚线连接 + 闪烁星点） |
| `sideDots(x1,y1,x2,y2,n,color)` | 数形结合：一条边上均匀画 n 个点 |
| `pythagorasArt({a,b})` | 勾股定理整图：两边打点 + 三边方糖 + 直角标记 + 边长标签 |
| `burst(host)` | 粒子特效（8 方向飞散，CSS 变量控方向） |
| `ORB_ART` | 三个知识点的现成星球插画 |

设计约束：只返回字符串不做 DOM 操作；所有 `<defs>` id 带自增后缀避免同页多实例冲突；
配色优先取 CSS 变量，取不到时回退内置常量。

**二、三个知识点的「手持道具」**（借鉴他的思路，自己重画）

- **kp1 一次函数彗星**：左下角坐标系 + 上升的橙色虚线 + 金色端点（行星在直线顶端）
- **kp2 内角和之星**：环着球体的虚线三角形 + 三个角的圆弧标记
- **kp3 勾股星**：左下角直角三角（含直角标记）+ 弦线 + 20% 透明度填色

**三、接入位置**

- `frontend/src/scripts/app.js`：`ORB_ART` / `PLANET_ORB_ART` 改为优先取 `QWSpaceArt.ORB_ART`，原来的几何点线插画降级为 `*_FALLBACK`（图形库没加载时自动回退，不会白图）。首页星球卡与答题星球页都加 `has-art` 类，去掉 CSS 画的圆底与 `::after` 虚线环（避免和 SVG 里的脸/环叠成两张脸）。
- `frontend/src/scripts/space.js`：装饰层新增 **4 组星座连线**，并补了第二颗流星（错开 4.2s 延迟）。
- `frontend/src/scripts/board.js`：新增 `sideDots()`，在 `pythagorasGrid` 与 `rightTriangleSquares` 两条直角边上按数量打点 —— **b=3 就真的画出 3 个点**，孩子可以「数」出边长，而不是只读一个数字。
- 16 个页面统一注入 `space-art.js`（注意必须排在 `app.js` **之前**，否则 `app.js` 执行时取不到图形库）。

**四、安全加固（后端）**

| 问题 | 改法 |
|------|------|
| `JWT_SECRET` 有硬编码兜底，忘配就用代码里的密钥签发令牌，任何人都能伪造登录态 | 生产环境（`NODE_ENV=production`）下**未配置 / 沿用开发默认值 / 长度 < 32 位**都直接抛错拒绝启动；开发环境保留兜底 |
| 密码哈希只有单轮 SHA-256，GPU 每秒能试几十亿次 | 换成 `crypto.scryptSync`（N=16384, r=8, p=1，16 字节随机盐），新哈希带 `scrypt$` 前缀；**历史 sha256 与明文密码仍可登录，校验通过后自动升级**，用户无感 |
| token 可通过 `?token=xxx` 传递，会进 Nginx 日志 / 浏览器历史 / Referer | 去掉该分支，只认 `Authorization: Bearer` |
| 没有任何安全响应头 | 手写中间件补齐 `X-Content-Type-Options` / `X-Frame-Options` / `Referrer-Policy` / `Permissions-Policy` / `X-DNS-Prefetch-Control`（不引入 helmet，保持零新增依赖） |
| `cors()` 全开，任何人都能跨域调 API | 改为读 `CORS_ORIGIN` 环境变量（逗号分隔白名单）；留空时维持本地联调的全开行为 |
| **上 Nginx 后 `req.ip` 全变 127.0.0.1，限流把全站访客算作同一个人** | `app.set('trust proxy', 1)` |

**五、工程清理**

- 删掉测试残留：`backend/src/models/_probe.txt`（内容 `hello`）、`tmp-test-create.md`（内容 `x`）。
- 删掉 `frontend/dist/`：过期构建产物，里面的 `dist/pages/student/*.html` 已被 `frontend/pages/student/*.html` 完全覆盖成**死代码**，却还挂着 unpkg 56 处 + jsdelivr 19 处；需要时 `cd frontend && npm run build` 重新生成。同时给 `app.js` 的两层 `express.static` 补上顺序注释（先注册的优先命中）。
- 9 个孤儿页从 `frontend/pages/` 移到 `docs/legacy-pages/`：它们 0 代码引用、且挂着 Tailwind CDN + unpkg + `lucide@latest`（无版本锁定）。移出后不再被 `express.static` 托管、URL 不再可达，但文件完整保留可随时搬回，并附了一份 `README.md` 说明各自被哪个新页面取代。
- 新增 `frontend/assets/favicon.svg`（手绘表情星球）与 `frontend/robots.txt`，并给 16 个页面补上 `<link rel="icon">` —— 之前浏览器每次都要发一次 `/favicon.ico` 吃 404。
- `backend/.env.example` 补充密钥长度要求、`CORS_ORIGIN`，以及 Nginx 必须转发的三个头（`Host` / `X-Forwarded-For` / `X-Forwarded-Proto`）。

**验证**

- 生产模式护栏实测：无 `JWT_SECRET` → 拒绝启动；用开发默认密钥 → 拒绝启动；用 28 位占位值 → 拒绝启动；配 32 位以上随机串 → 正常放行。
- scrypt 实测：`setPassword` 后哈希前缀为 `scrypt$`，正确密码校验 `true`、错误密码 `false`。
- 安全响应头实测（`fetch` 读回值）：`nosniff` / `SAMEORIGIN` / `strict-origin-when-cross-origin` / `geolocation=(), camera=(), microphone=(self)` / `off` 全部生效。
- `/assets/favicon.svg` → 200（1209 B）、`/robots.txt` → 200（198 B）。
- Playwright 跑 14 个页面（首页 / 登录 / 首页 / 答题星球 / 讲题课堂 / 标准题 / 错题订正 / 报告 / 等级 / 签到 / 徽章 / 智能体 / 会员 / 管理端）：**console 错误 0、4xx/5xx 0**；`home` / `planet` / `admin` 各渲染 3 颗手绘星球，其余学生端页面各挂 4 组星座。
- `node tests/run-all.js` → **11 / 11 套件全绿**。

**还没做（见第 3 节待办）**：数据仍在内存；HTTPS 未上（麦克风 / 语音识别仍不可用）；服务端 TTS 尚未落地。

---

### 2026-09-15（第 5 天 · 收尾场）图案系统扩写 · 讲题步骤高亮引导 · gzip 压缩 · 测试套件修复

**当天需求**：「改进目前的项目，并且要求前端界面参考他的（尤其是各种图案）」——在上一场（手绘星球 + 星座）的基础上，把「图案」这一条做透，顺带把上一场留下的坑补掉。

**改动**

| 时间 | 改动 | 说明 |
|------|------|------|
| 16:00 | `frontend/src/scripts/space-art.js` | 新增 `mix()` / `shade()` / `tint()` 配色工具与 `starPath()`；新增 **`emblem()` 勋章纹章**（六边形 / 盾形 / 圆形 / 菱形四种底座 × 渐变填充 × 内圈虚线 × 顶部高光 × 中心符号 × 1–5 颗星刻度 × 两条垂下的绶带，`is-locked` 自动灰阶）；新增 **`galaxy()` 螺旋星系**（倾斜盘状光晕 + 对数螺旋臂 + 亮核，螺旋臂用 `<g scale(1 .42)>` 压扁成盘）；新增 **`celebrate()`**（连打多组粒子）；新增 `setVar()` 兜底（轻量 DOM 环境没有 `style.setProperty`）；风格里补 `.qw-emblem` / `.qw-galaxy` / `.qw-dust-host` / `.qw-badge-icon.has-emblem` / `.qw-level-badge.has-emblem` |
| 16:20 | `frontend/src/scripts/app.js` | 新增 `BADGE_TIERS`（10 枚徽章各自的造型 / 配色 / 星数）、`LEVEL_EMBLEM_SHAPE`（5 级等级造型）、`RESULT_MEDALS`（按正确率发「满分 / 优秀 / 进阶 / 起航」四档）、`emblemHtml()` 回退函数、`GALAXY_DECOS`；徽章卡与等级阶梯的 emoji 换成纹章（取不到图形库时回退成原 emoji）；答题星球页背景叠两层大星系；**标准题答对时撒粒子**；**结果页新增勋章卡 + 英雄区庆祝粒子** |
| 16:35 | `frontend/src/scripts/space.js` | 新增 `SCENE_GALAXIES` 3 组，`ensureScene()` 里遍历生成 `.sq-galaxy` 节点 —— 16 个页面统一多出 3 个背景螺旋星系（贴视口边缘，不压版心） |
| 16:45 | `frontend/src/styles/space.css` | 新增第 28 节「图案系统」：`.sq-galaxy` / `.qwp-galaxy-holder` 摆放、纹章尺寸、结果页勋章卡（含 `qwBob` 浮动与窄屏单列）、夜色覆写 |
| 17:00 | `frontend/src/scripts/tour.js`（**新文件**） | 新手引导 `QWTour`：遮罩挖洞（单个 `box-shadow: 0 0 0 9999px` 做聚光灯，比四块遮罩简单且无缝隙）+ 气泡。要点：① 洞的位置每帧 `getBoundingClientRect` 现算，`scroll/resize` 与 700ms 定时器都会重算；② 气泡优先放下方，下方塞不下就翻到上方，横向贴边收回来；③ 非当前步骤用一层透明遮罩锁住点击（`clickable` 的步骤放开并支持点高亮区自动进入下一步）；④ 键盘 `→/←/Esc` 且 `stopPropagation`，避免和课堂自己的 Enter / 数字键抢事件；⑤ 看过的记在 `localStorage.qw_tour_seen_v1`；⑥ `supported()` 探测不到 `window.addEventListener` 时直接不启动，绝不影响页面 |
| 17:15 | `frontend/src/scripts/classroom.js` | 新增 `TOUR_STEPS`（6 步：学习目标 → 黑板图形 → 五步路线 → 讲解区 → 作答区 → 智能体）与 `bindTour()`；顶部星尘条加「❓ 怎么用」按钮；首次进课堂自动跑一遍 |
| 17:20 | `frontend/src/scripts/app.js` | 新增 `HOME_TOUR_STEPS`（5 步）与首页 hero 的「❓ 怎么用」按钮；首次登录自动跑一遍 |
| 17:30 | `frontend/pages/student/{classroom,home}.html` | 加载 `tour.js`（排在页面主逻辑之前） |
| 17:45 | `backend/src/middleware/compress.js`（**新文件**）+ `backend/src/app.js` | 零依赖 gzip 中间件（Node 自带 `zlib`）：拦截 `res.write` / `res.end` 攒响应体 → 只压「2xx + 文本类 MIME + ≥1 KB」→ 带 ETag 的静态资源缓存压缩结果（上限 200 条）。**关键坑**：`/api/agent/chat/stream` 会先 `res.flushHeaders()` 再写分片，此时响应头已发、加不了 `Content-Encoding`，必须立刻切成透传并把已攒的分片按原样写出去，否则那些分片永远发不出去（表现为响应体为空、`JSON.parse` 报 `Unexpected end of JSON input`）；同时把 `text/event-stream` 从可压缩类型里排除 |
| 18:10 | `frontend/src/scripts/space.js` + `space.css` | 断网提示条 `.sq-net-banner`：监听 `online` / `offline`，掉线时页面底部弹一条横幅说明「页面还能看，但讲题 / 对话 / 提交答案会失败」；恢复联网时顺手 `fetch('/api/health')` 探一下后端，连着 WiFi 但后端挂了会改成「连上了网，但连不上服务」。`addEventListener` 做了能力探测，轻量 DOM 环境不启动 |
| 18:00 | `tests/agent-smoke.js` | 该用例表有 15 条，超过免费版每天 10 次 AI 对话的额度，第 11 条必然 429。改为先给演示账号下单支付开通会员（演示单直接成功；注意 `/api/vip/order` 返回的是 **201** 不是 200），让「意图分类」验证不被配额挡住；配额本身由 `agent-quota-smoke.js` 单独覆盖。顺带补一条「拿不到 reply 就打印原始响应」的失败信息 |

**验证**

- `node tests/run-all.js` → **11 / 11 全绿**（修复前是 8/11：`agent-smoke` 卡在配额、`dom-smoke` 卡在 `tour.js` 的 `window.addEventListener`、`agent-ui-smoke` 卡在共享后端配额被吃光）
- gzip 实测（`Accept-Encoding: gzip`）：`space.css` 150374 → **29985** B（20%）、`common.css` 97852 → **17737** B（18%）、`app.js` 66575 → **19311** B（29%）、`favicon.svg` 1209 → 533 B；带 ETag 的静态资源二次请求 **7 ms**（命中压缩缓存）；`/api/learning/lessons/kp1` 5853 → 2679 B；`Vary: Accept-Encoding` 正确带上
- 流式接口回归：NDJSON 事件计数 `start=1 / tool=1 / delta=19 / done=1`，拼装文本 77 字 —— 修复后逐条完整、不再被攒成一坨
- Playwright 扫 14 个页面（1440×950，登录态）：**console 错误 0、4xx/5xx 0**；`badge` 页 10 枚纹章、`level` 页 5 枚纹章、`planet` 页 5 个星系（3 背景 + 2 页内）、其余学生端页面各 3 个星系 + 4 组星座，所有页面 `artStyle=Y`
- 断网横幅实测（Playwright `setOffline`）：在线时不显示 → 断网后 `.show` → 恢复后自动收起；全程 console 错误 0
- 引导实测：首页 5 步走完自动关闭并写入 `qw_tour_seen_v1` 的 `home`；讲题课堂 6 步，第 1 步聚光灯量到 `[30, 82, 1380, 67]`（正好罩住 `.qw-classroom-goal`）；`Esc` 可关闭；`#qwTourBtn` 可重看
- 截图：`%TEMP%\wenshu-art\` 下 `v-*.png`（14 页）、`t-home-*.png` / `t-class-*.png`（引导）、`n-result.png`（勋章卡）、`n-level-ladder.png`（等级纹章）

**已知取舍**

- `space.css` 已到第 28 节 / 3500+ 行，再往里堆图案就该拆成 `space-art.css` 了；本轮先按现有约定集中在一处。
- 背景星系是 3 个固定位置，窄屏（<900px）沿用 `.sq-scene` 原有的隐藏规则，没有专门适配。

---
### 2026-09-15（第 5 天 · 图案深化场）数形结合方格阵 · 星空专注计时

**当天需求**：「改进目前的项目，并且要求前端界面参考他的（尤其是各种图案）」——上半场已经把纹章 / 星系 / 星座 / 手势道具补齐，这一场专门啃同行最抢眼的那张图：**把「面积」画成能一格一格数出来的小方格**。顺带把他们另一个我们缺的功能（专注计时）补上。

**改动**

| 时间 | 改动 | 说明 |
|------|------|------|
| 19:10 | `frontend/src/scripts/space-art.js` | 新增 **`cellGrid(spec)` 数形结合方格阵**：给一块正方形按 `n × n` 切格，棋盘格浓淡交替（`tint(color, .14)` / `tint(color, .46)`）、每格中心一颗五角星（复用 `starPath()`）、外框圆角描边；支持 `gap` / `radius` / `mark(star\|dot\|none)` / `opacity` / `tilt`（整体绕中心旋转，斜边上的正方形用得到）。格子太小（<9px）时自动不画星，避免糊成一片。已导出到 `QWSpaceArt` |
| 19:30 | `frontend/src/scripts/board.js` | 新增两个本地小工具：`cellGrid()` 转发到图案库（取不到就返回空串，图里仍保留虚线外框，不会画崩）与 `chip(cx, cy, text, color, size)` 带底色小标签（文字压在方格上时保证读得清）。**讲题图改造**：`rightTriangleSquares()` 的两个直角边正方形从「一块半透明色块」换成 **4 × 4 = 16 格 / 3 × 3 = 9 格**，斜边上的那块换成 **5 × 5 = 25 格并跟着斜边一起旋转**，公式卡补一行「9 颗 + 16 颗 = 25 颗星尘」；`areaProof()` 中间的 `c²` 同样切 25 格（`opacity: .52`），四个直角三角形改成「自己的颜色 + `fill-opacity=.34`」，让「大正方形 = 4 个三角形 + 中间小正方形」一眼分得开 |
| 19:35 | `frontend/src/scripts/board.js` | **顺手修掉一个一直存在的几何 bug**：斜边上的正方形原本算的是 `px = uy, py = -ux`，画到了三角形的**内侧**，和两条直角边上的正方形大面积重叠（以前是半透明填充，看不太出来；换成一格一格的实心方块后重叠就很扎眼）。改成 `px = -uy, py = ux` 画到外侧 |
| 19:40 | `frontend/src/scripts/board.js` | 边长标签挪出方格阵（`a = 4` 放到下方、`b = 3` 放到左侧、`c = 5` 用带底色的 `chip` 贴着斜边放），不在小方块上压字 |
| 19:45 | `frontend/pages/student/classroom.html` | 把 `space-art.js` 提到 `board.js` 之前加载 —— 黑板图要用图案库的 `cellGrid()`，加载顺序不能反 |
| 20:10 | `frontend/src/scripts/focus.js`（**新文件**） | 星空专注计时 `QWFocus`：`mount(el)` 往挂载点里渲染（取不到挂载点就安静返回 `null`，绝不碰别的 DOM）。10 / 15 / 20 / 25 分钟四档，环形进度用 `stroke-dashoffset` 推；开始 / 暂停 / 继续一键切换；「今日已专注」与选中档位都存 `localStorage`；倒计时按 `Date.now()` 差值算（切到后台再回来不会算错，时钟被往回调也不会倒扣）。到点弹提示条 + 调 `QWVoice.speak()` 语音 + 调 `QWSpaceArt.burst()` 撒粒子，提醒起身眺望远方 |
| 20:25 | `frontend/src/styles/space.css` | 新增第 29 节「星空专注计时」：环形（`.sq-focus-ring`，`rotate(-90deg)` 让进度从 12 点开始走）、四档按钮（选中态是青绿渐变）、主按钮（未运行金色渐变 / 运行中靛蓝渐变）、到点提示条（`.sq-focus-done`），外加夜航模式覆写 |
| 20:30 | `frontend/src/scripts/app.js` + `pages/student/home.html` | 首页「白天专注模式」卡片内部插一个 `#qwFocusCard` 挂载点（放在三条数据行下面），`renderHome()` 渲染完再 `QWFocus.mount()`；`home.html` 加载 `focus.js` |
| 20:50 | `frontend/src/scripts/focus.js` + `space.css` | **改紧凑版**：最开始的实现是「一张独立侧栏卡 + 132px 大环 + 标题」，实测把首页右栏撑到 1900px、左栏只有 1500px，左右明显不平衡。改成「78px 小环 + 四档按钮 + 按钮」横排、去掉自带标题、辅以一条虚线分隔，整体从 ~340px 压到 ~150px 并挪进「白天专注模式」卡片内部 —— 卡片高度正好落在旁边「伴学领航员」卡片的空档里，`.sq-bottom` 那一行高度没变（452px），左右两栏回到 638 / 666px |

**验证**

- `node tests/run-all.js` → **11 / 11 全绿**（改动前后各跑一次，无回归）
- 图案实测（Playwright 在真实课堂页里渲染 `QWBoard.render()`）：`rightTriangleSquares` 出 **53 个 rect = 50 个格子 + 3 个外框**、50 颗星；`areaProof` 出 26 个 rect = 25 格 + 1 个内框；`pythagorasGrid` 维持原样（0）
- 布局实测：三块方格阵互不重叠，`a² = 16` / `b² = 9` / `c² = 25 格` 三个标签都在图案外面，`c = 5` 贴在斜边上
- 专注计时实测（Playwright，`Date.now` 可拨快）：`25:00` → 点「10 分」→ `10:00` → 点开始 → `09:58` / 进度环 offset `1.1` / 按钮变「暂停一下」/ `qw_focus_log` 写入 `{"date":"2026-09-15","seconds":2}` → 点暂停 → 「继续专注 09:57」→ 拨快 10 分钟 → **到点提示条出现 + 粒子层 1 个** + 自动归位 `10:00`；全程 console 错误 0
- Playwright 扫 12 个学生端页面（1440×950，登录态）：**console 错误 0、4xx/5xx 0**；`classroom` 页 `cells=3`（三块方格阵），`badge` 10 枚纹章、`level` 5 枚纹章，其余页面各 6 个星系
- 截图：`%TEMP%\wenshu-art\` 下 `cells-rightTriangleSquares.png` / `cells-areaProof.png`、`f-focus-running.png` / `f-focus-done.png`、`w-*.png`（12 页）

**已知取舍**

- `pythagorasGrid()`（网格上的 3-4-5）**没有**再叠方格阵：那张图本身就是 44px 的网格，「数格子」信息已经有了，再铺一层小方格会糊成一片。
- `space-art.js` 里的 `pythagorasArt()` 目前仍没有页面在用（组件先备着），所以这一场只改了 `board.js` 实际会渲染的两张图。
- 专注计时只挂在首页侧栏；讲题课堂顶部本来就有一个「已用时长」计时器，没有重复挂。

---

### 2026-09-16（第 6 天）部署路线落地：免费托管（路线一）与云服务器（路线二）

**当天需求**：先问「想要自己电脑关机网站也能打开，只能在服务器部署吗，还有什么办法」→ 给了四条路线对照；接着「**考虑路线一和路线二**」，即把**免费托管平台**和**云服务器**两条正式路线真正落成可执行的东西，而不是只写在文档里。

**改动**

| 时间 | 改动 | 说明 |
|------|------|------|
| 09:40 | `.dockerignore`（**修 bug**） | 原来写的 `.env` 在 dockerignore 里**只匹配根目录那一层**，`backend/.env`（含真实 DeepSeek Key）会被照打进镜像 —— 上传到托管平台或推镜像仓库就等于泄露密钥。改成 `**/.env` / `**/.env.local` / `**/.env.*.local` 按任意层级排除；顺带补 `.pnpm-store`、`tools`、`docs`、`dist-package`、`*.zip`、`*.tar.gz`，把构建上下文从「本地依赖一起传」压回纯源码 |
| 09:45 | `render.yaml`（**新文件**） | Render Blueprint：`runtime: docker` 复用仓库根目录的 `Dockerfile`；`region: singapore`；`healthCheckPath: /api/health`；`JWT_SECRET` 用 `generateValue: true` 让平台生成 256 位随机串（满足后端「生产环境 >= 32 位」的启动护栏）；`LLM_API_KEY` 用 `sync: false` 让值不进 Git；默认 `AGENT_PROVIDER=rules` + `AGENT_FREE_DAILY=10`；不设 `PORT`（平台自动注入） |
| 09:50 | `fly.toml`（**新文件**） | Fly.io 配置：同一份 `Dockerfile`；`force_https = true`（语音要安全上下文）；`auto_stop_machines = "stop"` 省额度；`/api/health` 健康检查；`shared-cpu-1x / 256mb` |
| 09:55 | `docker-compose.yml`（**新文件**） | 路线二备选：容器方式起服务。`restart: unless-stopped`、`env_file: backend/.env` 配 `environment` 强制 `NODE_ENV=production`、健康检查、json-file 日志轮转（`max-size 10m`） |
| 10:10 | `tools/pack-for-server.ps1`（**新文件**） | 服务器上传包：递归打包为 zip，排除 `node_modules` / `.pnpm-store` / `.git` / `frontend/dist` / `.env` / 日志。**动机**：本地 `backend/node_modules` 是 pnpm 的软链接结构（`express` 等指向 `.pnpm/` 里的真身），直接拷到 Linux 必然全是坏链接，必须让服务器重新 `npm install`。实测 **176 个文件 / 4.61 MB**，中文文件名带 UTF-8 标记（Linux `unzip` 不乱码） |
| 10:35 | `tools/deploy-server.sh`（**新文件**） | 云服务器一键部署，7 步全中文提示：① 装 curl/tar + NodeSource 装 Node 20（拉不到源退回系统源并校验 >= 18）② `tar` 流式同步代码到 `/opt/wenshu`（排除 node_modules / .git / dist）③ 生成 `.env`：**自动签发 64 位随机 `JWT_SECRET`**、`NODE_ENV=production`、`LLM_API_KEY` 取环境变量、`chmod 600` ④ `npm install --omit=dev`（失败自动换淘宝镜像重试 + 校验 `node_modules/express` 确实存在）⑤ systemd 单元（`Restart=always` / `User=www-data` / `NoNewPrivileges` / `PrivateTmp` / `ProtectSystem=full`）并设为开机自启 ⑥ Nginx 站点（`proxy_buffering off` 保流式、`client_max_body_size 4m` 保头像、移除 default 站点、放行 ufw / firewalld 的 80-443）⑦ 有域名就 certbot 申请证书 + 301 跳 HTTPS；最后打印健康检查 JSON 与常用运维命令 |
| 11:05 | `部署文档.md`（**大改**） | ① 开头新增「**路线对照：先看这张表**」——四条路线对照「代码跑在哪 / 别人怎么访问 / **自己电脑关机后还能不能开** / 成本 / 适合场景」，并点明「只有路线一、二能做到关机后还能开」。② 方案 C 整节重写为**路线二**：3.0 选型（轻量应用服务器 / 国外 VPS / 备案问题）、3.1 传代码（**明确警告不要传 pnpm 那套 `node_modules`**）、3.2 一键脚本逐项说明、3.3 手动逐步命令（折叠）、3.4 HTTPS、3.5 日常运维速查表。③ **新增方案 D = 路线一**：平台对照表（Render / Koyeb / Fly / Railway / 微信云托管）、Render 逐步操作（含 `git init` + 推 GitHub 的前置步骤）、`render.yaml` 逐项解释、Fly / Koyeb 折叠说明、**免费档三大坑**（实例休眠冷启动 / 内存数据重启即重置 / 额度与陌生人刷 Key）、容器文件系统临时性。④ 章节号顺延（原 4~8 → 5~9）并同步交叉引用。⑤ 排错表补 6 行（托管平台转圈 / 502、构建失败、缺 `JWT_SECRET`、systemd failed、安全组没放行、certbot 失败）。⑥ 自查清单补「分路线的额外自查」 |
| 11:20 | `README.md` | 项目树补 `render.yaml` / `fly.toml` / `docker-compose.yml` / `pnpm-lock.yaml` / `tools/deploy-server.sh` / `tools/pack-for-server.ps1`；部署清单从「三条路线」扩写为「四条路线 + 两个工具脚本 + `.dockerignore` 密钥泄漏修复」 |
| 11:25 | `tools/deploy-server.sh` | 同步章节交叉引用（「详见 部署文档.md 第 6 节」→ 第 7 节，因为插入了新章节） |

**验证**

- `node tests/run-all.js` → **11 / 11 全绿**（本轮没动任何业务代码，属回归确认）
- 配置语法：`render.yaml` / `docker-compose.yml` 用 PyYAML 解析通过；`fly.toml` 用 Python `tomllib` 解析通过
- `tools/deploy-server.sh`：`bash -n` 语法检查通过（exit 0）；**三处 heredoc 分别实跑验证渲染结果** —— Nginx 配置里 `$host` / `$remote_addr` / `$proxy_add_x_forwarded_for` / `$scheme` 都正确保留为字面量（没被 shell 展开成空串），`.env` 渲染出 64 位十六进制 `JWT_SECRET`，systemd 单元里 `WorkingDirectory` / `ExecStart` 路径正确
- `tools/pack-for-server.ps1`：**在 Windows PowerShell 5.1 下真跑通过**（exit 0），产出 `dist-package/wenshu-fullstack-20260916-0953.zip`；用 Python `zipfile` 复核：176 条目、**无任何 `.env` 条目、无 node_modules 条目**、4 个中文 `.md` 均带 UTF-8 标志位
- 生产模式冒烟（模拟托管 / 服务器环境：只给环境变量，不依赖 `backend/.env`）：`NODE_ENV=production` + `PORT=3312` + 64 位 `JWT_SECRET` + `AGENT_PROVIDER=rules` 启动成功 → `GET /api/health` 返回 `{"status":"ok","env":"production","db":"memory","cache":"memory","llm":"rules"}`；`/`、`/pages/auth/login.html`、`/src/styles/space.css`、`/assets/vendor/lucide.min.js` 全部 200；`POST /api/auth/login`（13800000001）返回 token + 林小明；gzip 实测 `space.css` **154637 → 30820 B（约 20%）**，带 `Content-Encoding: gzip` 与 `Vary: Accept-Encoding`
- 收尾清理：杀掉上一轮遗留的 3311 端口生产冒烟进程（pid 25068）与本轮 3312 冒烟进程

**已知取舍**

- **Dockerfile 的依赖安装特意保持 `npm install --omit=dev`**，没改成 `pnpm install --frozen-lockfile`。原因：仓库里只有 `backend/pnpm-lock.yaml`，Docker 镜像里没有 pnpm；而这 4 个依赖（express / cors / dotenv / uuid）都在维护期且用 `^` 限位，npm 解出的版本与锁文件基本一致。改成 pnpm 反而会引入「改了 `package.json` 忘了更新锁文件 → 镜像构建直接失败」的新坑（对还在加功能的学生项目不友好）。替换方法已写在 Dockerfile 注释里。
- 免费托管档**没有**做数据持久化（`db: 'memory'`），平台一重启就回到演示数据。真要持久化得挂 Volume 或接免费 Postgres / Redis，属于下一阶段。
- `render.yaml` 默认 `AGENT_PROVIDER=rules`（离线引擎）是**故意**的：公开地址谁都能调 `/api/agent/chat`，挂着真实 DeepSeek Key 会被刷额度。文档里写了怎么改成用真实模型。
- **本机没有 Docker，镜像构建本身没能实测**（只验证了配置语法，以及「容器里会执行的那条命令链」在宿主机上的等价行为）。

---
### 2026-09-16（第 6 天 · 下半场）线上保活 + DeepSeek 正式接入

**当天需求**：站点已经在 https://wenshu-xingtu.onrender.com 跑起来了，要 ① 解决免费实例休眠（冷启动 30~60 秒）② 把真实 DeepSeek Key 接上，并且不能让公开地址把额度刷爆。

**改动**

| 时间 | 改动 | 说明 |
|------|------|------|
| — | `.github/workflows/keepalive.yml`（新增） | 每 5 分钟从 GitHub 云端 ping `/api/health` 保活；探测失败只出一条 `::warning` 而不把仓库 CI 标红；支持 `workflow_dispatch` 手动跑一次 |
| — | `tools/keepalive.ps1`（新增） | 本机保活脚本：默认循环 ping，`-Once` 单次自检，`-InstallTask` / `-RemoveTask` 注册、卸载「登录后自动跑」的计划任务；文件带 UTF-8 BOM，避免 PowerShell 5.1 按 GBK 解码导致中文注释乱码 |
| — | `render.yaml` | 删掉写死的 `AGENT_PROVIDER: rules`（改成注释保留）→ 变成**自动模式**：有 Key 走大模型、没 Key 自动降级。新增 `LLM_MODEL_FALLBACKS` / `LLM_TIMEOUT=45000` / `LLM_DAILY_CAP=300` |
| — | `backend/src/config/index.js` | `ai` 段新增 `modelFallbacks`（默认 `deepseek-chat,deepseek-reasoner`）与 `dailyCap`（默认 300，≤0 表示不限量） |
| — | `backend/src/agent/providers/openai.js` | 抽出 `postChat()`（带超时）与 `chat()`（模型兜底链）：返回 400/404 且报文含 `model` 时自动换用候选链里的下一个模型，跑通的模型记进 `activeModel` 供后续复用；`respond()` 返回体改用 `activeModelName()`，并导出 `activeModelName` / `modelChain` |
| — | `backend/src/agent/index.js` | 新增**全站每日大模型预算闸门**：`llmBudgetState()` / `consumeLlmBudget()` 按自然日计数，`reply()` 里额度用满就退回规则引擎并标记 `degraded + degradeReason`；`providerInfo()` 增加 `budget` 字段，`/api/agent/session` 能直接看到今日用量 |
| — | `backend/src/routes/agent.js` | `metaOf()` 透出 `degradeReason`，能区分「额度用满」还是「大模型报错」导致的离线回答 |
| — | `backend/.env.example` | 补 `LLM_MODEL_FALLBACKS` / `LLM_DAILY_CAP` 说明 |
| — | `tests/llm-budget-smoke.js`（新增）+ `tests/run-all.js` | 额度闸门专项冒烟：自起一个 `LLM_DAILY_CAP=1` 的后端，断言「第 1 次消耗额度 → 第 2 次必然降级且原因可读 → 额度不越界」，并已登记进 `run-all.js`（12 个套件） |
| — | `README.md` / `部署文档.md` | 项目树补两个新文件；部署文档 4.1 改为「代码已在 GitHub」+ DeepSeek 接入三步 + 环境变量表；新增 **4.5 保活**章节（云端 / 外部监控 / 本机脚本三种方案） |

**为什么这么设计**

1. **模型名兜底**：DeepSeek 各账号可用的模型 id 不完全一致（`deepseek-flash` / `deepseek-chat` / `deepseek-reasoner`），线上配错一个名字整条链路就不可用。改成候选链后，运维只要把 Key 填对就行。
2. **为什么还要全局闸门**：线上是公开地址，`/api/agent/chat` 匿名也能调，而且免费用户能反复注册，只靠「每人每天 10 次」拦不住。加一层按自然日的全站上限，超了自动退回离线规则引擎 —— 网站照常能用，只是不再花钱。
3. **为什么不把 Key 写进 `render.yaml`**：仓库是公开的，写进去等于公开 Key。Key 走 Render 控制台的 `Environment`（`sync: false`），不进 Git。

**验证**

- `node tests/run-all.js` → **12/12 套件全绿**（改动前后各跑一次，结果一致；新增的 `tests/llm-budget-smoke.js` 11 条断言专门盯额度闸门）。
- 额度闸门专项：以 `LLM_DAILY_CAP=1` 起服务，第 1 次对话会真的尝试调用大模型（降级原因是真实报错），第 2 次对话直接返回 `degraded + "今日大模型调用额度已用满，已自动切回离线规则引擎"`，`/api/agent/session` 的 `budget` 显示 `{used:1, cap:1, remaining:0}`。
- 线上站点（`https://wenshu-xingtu.onrender.com`）实测：首页渲染正常 → 演示账号 `13800000001 / 123456` 登录成功 → 学生首页数据正常、控制台无报错。
- **未验证**：真实 DeepSeek 调用链路（当前开发机不能直连外网）。需要在 Render 控制台填好 `LLM_API_KEY` 后，按「部署文档 4.1 → 接入 DeepSeek → 第 3 步」复核一次。

---

### 2026-09-17（第 7 天）产品定位收敛：下线教师端与班级维度 · 立项天梯赛

**当天需求**：① 明确产品面向**学生 + 家长**，去掉「班级课堂模式」与登录用的老师账号；② 把此前梳理的优化清单与新增方向（除班级课堂外）落成一份可执行的**计划清单**；③ 评估能否做一个像小猿口算那样的**联机挑战（天梯赛积分制）**。

**改动**

| 时间 | 改动 | 说明 |
|------|------|------|
| — | `backend/src/permissions.js` | `ROLES` 删掉 `teacher`；4 个 `admin:*` 权限码允许角色收敛为 `['ops']`；`isStaff()` 改为只认 `ops` |
| — | `backend/src/models/mockData.js` | 删掉教师演示账号 `t1`（王老师 / `teacher`）；删掉学生与教师档案里的 `className`；隐私设置 `shareToTeacher` → `shareToFamily` |
| — | `backend/src/routes/admin.js` | 看板 `users` 不再返回 `teacher` 计数；文件头与订单接口注释同步改为「运营」 |
| — | `backend/src/routes/report.js` | 4 处下发结构去掉 `className`；设置默认值改 `shareToFamily` |
| — | `backend/src/repositories/studyRepository.js` | 新学生默认设置同步改 `shareToFamily` |
| — | `backend/src/db/schema.js` / `schema.sql` | `qw_user` 去掉 `class_name` 列；表注释与 `role` 注释改为「学生 / 运营」 |
| — | `backend/src/{app.js,middleware/auth.js,services/vipService.js}` | 启动横幅与注释里的「教师」措辞清理 |
| — | `frontend/src/scripts/auth.js` | `ROLE_HOME` 删掉 `teacher` 映射；`isStaff()` 只认 `ops`；守卫文案与回跳角色改「运营」 |
| — | `frontend/src/scripts/auth-page.js` | 演示账号芯片从 3 个减到 2 个（学生 / 运营） |
| — | `frontend/src/scripts/admin.js` | 模块 `roles` 去掉 `teacher`；经营概览删掉「教师」KPI；`roleName()` 去掉教师 |
| — | `frontend/src/scripts/report.js` | 「参与班级排行」→「参与星系排行榜」；「共享学习报告给老师」→「共享学习报告给家长」（键名 `shareToFamily`）；两处用户副标题的 `className` 换成 `school` |
| — | `frontend/src/scripts/app.js` | 账号卡副标题去掉 `className` |
| — | `tests/layers-smoke.js` | 权限断言改为运营 / 学生对照；新增「已下线的教师账号登录返回 401」与「学生访问 `/api/admin/orders` 被 403 拦下」（越权防护用例，此前缺失） |
| — | `tests/dom-smoke.js` | 控制台用例改用 `admin / admin123` 登录 |
| — | `计划清单.md`（新增） | 46 条待办的总清单：4 条已落地 + P0~P3 技术债 20 条 + 新增方向（架构 / 实现 / 功能）+ 天梯赛专项设计 |
| — | `README.md` / `部署文档.md` / `同行方案借鉴分析.md` / `开发文档.md` | 演示账号表、分层框架表、演进路线、权限说明、缓存用途、Electron 定位等所有「教师 / 班级」措辞同步收敛 |

**为什么这么设计**

1. **为什么删班级维度而不是留个空壳**：产品是 B2C（家长付费、学生使用），不存在「学校统一开课」的 B 端场景；留一个用不上的 `className` 与「共享给老师」开关，只会让家长看不懂「老师是谁」。
2. **为什么运营端保留**：会员订单、题库与错因字典需要有人维护，且运营不是教学角色，与「面向家长学生」不冲突。教师账号只是从「可见题库 + 学情」降级为**直接删除**，`teacher` 登录现在返回 401。
3. **为什么先立项天梯赛而不是直接写**：联机对战有两个硬前提——数据持久化（内存重启即清空，排行榜会「集体失忆」）与大模型/出题成本控制（每局现生成题目会烧额度）。所以天梯赛排在数据层改造之后，本轮只出方案与数据模型。
4. **本轮第 3 天那条「管理端权限对照（教师 vs 运营）」表已作废**，请以本文档本条与 `计划清单.md` 为准。

**验证**

- `node tests/run-all.js` → **12/12 套件全绿**（含本轮新增的两条权限/越权断言）。
- `node --check` 遍历 `backend/src/**/*.js` 与 `frontend/src/scripts/*.js`，语法全部通过；`backend/src/db/dump-sql.js` 重新生成 `schema.sql` 与手改结果一致（`class_name` 已消失）。
- 全仓复扫 `teacher` / `教师` / `班级` / `className:`：只剩 `blocklist.js` 安全文案里的「请让家长或老师来操作」与「学校班级」隐私词条——这两处是**未成年人保护提醒**，刻意保留。

**未落地（见 `计划清单.md`）**：上半场只做「下线教师端 + 立项」，优化清单里的 P0~P3 仍未动工。

> ⏭ **天梯赛在下半场已落地**（同日「第 7 天 · 下半场」条目）：阶段 1 异步排位赛已实现并通过 13/13 套件，阶段 2/3 仍待数据落库。

### 2026-09-17（第 7 天 · 下半场）星际天梯（阶段 1 · 异步排位赛）落地

**当天需求**：承接上半场的「立项天梯赛」——**可以加入天梯赛**。按 `计划清单.md` 的**阶段 1「异步天梯」**实现：限时闯关 → 计分 → 段位 → 天梯榜。

**为什么直接做阶段 1 而不是等数据落库**：阶段 1 全走 REST，用现有内存数据层就能跑通；阶段 2 的实时 1v1 才需要 Redis 匹配队列与长连接。先上线可玩的版本，把「零大模型成本 + 服务端判分 + 不泄答案」这三条不变量跑实，等 `P0-2` 落库时天梯只是换个持久层实现。

**改动**

| 时间 | 改动 | 说明 |
|------|------|------|
| — | `backend/src/models/ladderBank.js`（新增） | **确定性题库**：`mulberry32` + FNV hash 播种（同一局永远同一套题，便于复盘与防作弊）；kp1 四型 / kp2 四型 / kp3 三型；九段星阶 `TIERS`（陨石 0-999 → 星海之王 2400+）；规则常量 `RULES`（每题 20 秒 / 一局 8 题）；起分 `START_RATING = 900`（新号从「陨石」起步，不再一上来就是「流光」）；`deltaFor` 胜 +25 / 平 +10 / 负 -15 + 连胜加成（3 连起 +5，上限 +10）；`tierFloor` **段位保护**（积分掉不到当前段位门槛以下）；`questionScore` = 100 基础 + 剩余秒 × 3（上限 45） |
| — | `backend/src/db/schema.js` | 新增 4 张表（表数 14 → **18**）：`qw_ladder_season` / `qw_ladder_profile` / `qw_ladder_match` / `qw_ladder_answer`；对局表带 `current_seq`（当前第几题）与 `seq_started_at`（本题起始时间，服务端算超时），作答表带唯一索引 `uk_lanswer_match_seq`（防重放） |
| — | `backend/src/db/schema.sql` | 用 `schema.toSql()` 重新生成 DDL（天梯四表 + 星钻两表 + 教师列已删） |
| — | `backend/src/models/mockData.js` | 播种赛季 `s1`（星海启航）/ 我的档案 `u1`（1128 分「流光」）/ 6 名星海对手（机器人补位，避免人少匹配不到人）；天梯对局与作答初始为空表；**顺带删掉上半场下线的教师账号 `t1`**（`users` 现在只剩 u1、o1，横幅回到 `qw_user(2)`） |
| — | `backend/src/repositories/ladderRepository.js`（新增） | 赛季 / 档案 / 对局 / 作答四组读写 + `rivals()`、`rivalsOf(rating)`（按积分就近找对手）；在 `repositories/index.js` 注册为 `ladder` |
| — | `backend/src/services/ladderService.js`（新增） | 赛季 / 我的 / 天梯榜 / 开局 / 逐题作答 / 结算 / 家长守护。**`questionView()` 在出站前剥掉 `correctAnswer`**，答案只在结算后的 `replay` 里揭晓；**每日场次**免费 3 局 / 会员 10 局，按北京时间跨天重置；**家长守护**复用 `repos.study` 的 `studyWindow` / `quietHours`；结算**幂等**（重复调用不重复加减分）；星尘奖励 胜 12 / 平 8 / 负 3 走 `walletService.record()` 写真实流水 |
| — | `backend/src/routes/ladder.js`（新增）+ `backend/src/app.js` | 8 个接口：`GET /season`、`/leaderboard`、`/me`、`/match/:id`，`POST /guard`、`/match`、`/match/:id/answer`、`/match/:id/finish`；挂到 `/api/ladder`，启动横幅加「🛸 星际天梯」 |
| — | `backend/src/services/walletService.js` | 钱包分类标签加 `ladder: '星际天梯'`（否则收支记录里会显示英文 key） |
| — | `frontend/pages/student/ladder.html`（新增） | 星际天梯页：引 `common.css` / `space.css` / `ladder.css` + `auth.js` / `xiaowen.js` / `nav.js` / `ladder.js` / `space-art.js` / `space.js`，与其它学生端页面保持一致 |
| — | `frontend/src/scripts/ladder.js`（新增） | 三视图：**主界面**（段位徽章 + 赛季规则卡 + 开始匹配 + 每日场次星点 + 近 10 局战绩 + 天梯榜前三名星台 + 家长守护开关）/ **对局**（并排进度条 + 倒计时环 + 连击粒子）/ **结算**（结算卡 + 逐题回放） |
| — | `frontend/src/styles/ladder.css`（新增） | `sql-` 前缀、全部走设计令牌 `--sq-ink/--sq-surface/--sq-line/--sq-muted/--sq-shadow/--sq-sun/--sq-display`，与星途宇宙主题同源；`color-mix` 带 `@supports not` 回退 |
| — | `frontend/src/scripts/nav.js` + `frontend/vite.config.js` | `ALIAS.ladder = 'planet'`（天梯挂在星球 tab 下，不新增 tab）；顶栏加「🛸 星际天梯」；vite 多页入口加 `student-ladder` |
| — | `tests/ladder-smoke.js`（新增） | **50 条断言**：赛季与规则 / 我的段位与战绩 / 开局 8 题且**题面无 `correctAnswer`** / 跳题被拒 / 逐题提交只回对错 / **超时判错且速度分归零** / 没答完不能结算 / 结算后揭晓答案且回放与「我的选择」一致 / 积分不低于段位门槛 / 星尘按胜负发放 / **重复结算幂等** / 流水分类为「星际天梯」/ 免费版第 4 局 429 / 家长守护 403 `LADDER_GUARD` / 天梯榜倒序与名次 / 看不了也结算不了别人的对局（403）/ 新号自动初始化 900 分「陨石」 |
| — | `tests/run-all.js` | 注册天梯套件（`{ selfHost: true }`，自起 3201 端口，排在 `classroom-ui-smoke` 之后） |
| — | `tests/page-check.js` | 加 `ladder.html` 页面断言 + `ladder.js` / `ladder.css` / 3 个 `/api/ladder/*` 只读断言（页面数 15 → **16**，断言 66 → **72**）；`/me` 断言写成 `d.daily.limit === (d.daily.vip ? 10 : 3)`，不写死 3 |
| — | `tests/dom-smoke.js` | 加 `['student-ladder', '/pages/student/ladder.html']`，真实执行页面脚本捕获运行时错误 |
| — | `tests/layers-smoke.js` | 把写死的「14 张表」断言改成「≥14 张 + 每张有主键 + 表名不重复」（当前 18 张），并新增「天梯四张表已登记」（37 → **39** 条断言） |
| — | `README.md` / `计划清单.md` / `更新说明.md` / `应用更新.ps1` / `开发文档.md` | 项目结构、页面地址表、测试表、接口文档、功能清单同步；`计划清单.md` 把天梯赛阶段 1 标为已完成并补「本轮已落地」表；启动横幅展示同步 |

**为什么这么设计**

1. **为什么对手是「异步 + 机器人补位」**：真·实时 1v1 需要匹配队列 + 长连接，而且人少时匹配不到人体验直接崩。阶段 1 让玩家和「同段位水平的星海对手」比同一套题（题库确定性播种），**体感等同竞速、成本为 0、无并发风险**。
2. **为什么答案要等结算才揭晓**：对局中只回 `correct: true/false`，玩家无法从响应里反推答案；`questionView()` 是唯一的出站口，与课堂 / 标准题共用同一条「不泄答案」不变量。阶段 2 的实时对战也能直接复用。
3. **为什么新号从 900 分起步**：原来定 1000 会直接落在「流光」，与演示档案撞段。改成 900 让**新用户从最低段「陨石」开始爬**，段位变化才有正反馈；同时把 `tierFloor` 段位保护接上，避免一把输穿掉段。
4. **为什么家长守护是「复用」而不是新做一套**：家长在「报告 → 提醒设置」里设的学习时段与免打扰，本来就是防沉迷口径；天梯直接读同一份 `studySettings`，家长设了 21:30 免打扰，天梯自动关闭——**几乎零成本的现成能力**。
5. **为什么天梯挂在「星球」tab 而不是新开 tab**：`planet.html` 已经是「星系地图 = 知识点」的语义，天梯是「同一批知识点的排位赛」，两者同源；合并进星球 tab 可以让底部导航保持在 5 个（不破坏已有的圆形图标布局）。
6. **为什么全程不调大模型**：一局 8 题 × 20 秒，每局都现生成题目会**既慢又贵还不确定**，且无法服务端判分。固定题库 + 确定性播种让边际成本归零，也让「防作弊」成为可能。

**验证**

- `node tests/run-all.js` → **13/13 套件全绿**，最后一行「全部测试套件通过」。逐套件：api 17 · layers **39** · agent 意图 15 条 · persona 全通过 · quota 全通过 · llm-budget 11 · scaffold 27 · page-check **72** · dom 全部页面 OK · agent-ui 23 · xiaowen 26 · classroom 12 · **ladder 50**。
- 天梯套件单独复跑一次（`node tests/ladder-smoke.js`，3.2 秒），`PASS=50 FAIL=0`。
- 全量套件**连跑两遍**结果一致（排除偶发假失败）。
- 后端启动横幅确认：`qw_user(2)`（教师账号确实没了）+ 13 张落地表 + 🛸 星际天梯入口。

**未落地**：天梯赛阶段 2（实时 1v1 匹配队列 / 断线重连）与阶段 3（赛季重置 / 好友房 / 观战 / 战绩分享长图），前置是 `P0-2` 数据落库 + Redis；见 `计划清单.md`「阶段 2 / 阶段 3」。

---
## 2. 需求对照表

| 需求 | 落地位置 | 状态 |
|------|----------|------|
| 家长端与学生端不分家，家长端功能压进学生端 | `frontend/pages/student/report.html` + `scripts/report.js`（`nav.js` 注释标明收敛原因） | ✅ |
| 学生端：完善讲题步骤 | `backend/src/models/lessons.js`（3 知识点 × 5 步）+ `scripts/classroom.js` | ✅ |
| 学生端：生成对应图片 | `frontend/src/scripts/board.js`（11 类 SVG 图形）+ 知识点配图 | ✅ |
| 前端界面参考同行（尤其各种图案） | `frontend/src/scripts/space-art.js`（手绘表情星球 / 星座 / 螺旋星系 / 徽章纹章 / **数形结合方格阵** / 粒子）+ `space.js` 星座层 + `board.js` 边长打点与方格阵 | ✅ |
| 图案：徽章 / 等级 / 结果页勋章 | `space-art.js` 的 `emblem()` + `app.js` 的 `BADGE_TIERS` / `LEVEL_EMBLEM_SHAPE` / `RESULT_MEDALS` + `space.css` 第 28 节 | ✅ |
| 图案：背景螺旋星系（全站统一） | `space-art.js` 的 `galaxy()` + `space.js` 的 `SCENE_GALAXIES` + `app.js` 的 `GALAXY_DECOS` | ✅ |
| 图案：通关 / 答对庆祝粒子 | `space-art.js` 的 `burst()` / `celebrate()`，挂在 `#quizFeedback` 与结果页英雄区 | ✅ |
| 图案：数形结合方格阵（把面积切成 9 / 16 / 25 个能数的格子） | `space-art.js` 的 `cellGrid()` + `board.js` 的 `rightTriangleSquares()` / `areaProof()` + `space.css` 第 29 节 | ✅ |
| 借鉴同行：专注计时（10/15/20/25 分钟 + 到点提醒） | `frontend/src/scripts/focus.js` + `space.css` 第 29 节 + 首页 `#qwFocusCard` | ✅ |
| 首屏体积（97 KB + 146 KB CSS 裸传） | `backend/src/middleware/compress.js`（零依赖 gzip + ETag 压缩缓存） | ✅ |
| 借鉴同行：讲题步骤高亮引导 | `frontend/src/scripts/tour.js`（遮罩挖洞 + 聚光灯 + 气泡 + 锁点击）+ `classroom.js` 的 6 步 `TOUR_STEPS` + 首页 5 步 `HOME_TOUR_STEPS` | ✅ |
| 借鉴同行：服务端 TTS + 三级降级 | 已分析并给出方案，**未落地**（仍是浏览器内置 `speechSynthesis` 单级） | ⏳ |
| 增加登录注册模块 | `pages/auth/{login,register}.html` + `scripts/auth-page.js` + `routes/auth.js` | ✅ |
| 智能体：引导式教学通用模版 | `backend/src/agent/scaffold.js`（五步闭环 / 六步 SOP / 错因矩阵） | ✅ |
| 智能体：自动生成学习报告、错题记录 | `/api/agent/study-report`、`/api/agent/error-book`、`agent/records.js` | ✅ |
| 智能体：接 DeepSeek + 限制词 | `agent/providers/openai.js`、`agent/blocklist.js`、`agent/guardrail.js`（`.env` 里 `LLM_*`） | ✅ |
| 我的界面：五维能力雷达图 | `agent/ability.js` + `/api/student/ability`，页面在「我的」 | ✅ |
| 语音对话 / 播报 / 选择声音 | `frontend/src/scripts/voice.js`（听 / 说 / 声音列表 / 语速） | ✅ |
| VIP 充值（随机付款码、演示账号直接成功） | `pages/student/vip.html` + `scripts/vip.js` + `services/vipService.js` | ✅ |
| 按 README 框架完善代码 | 后端 9 层拆分 + 管理控制台 + 分层冒烟测试 | ✅ |
| **加入天梯赛（联机挑战 · 阶段 1 异步排位）** | `models/ladderBank.js` + `services/ladderService.js` + `routes/ladder.js` + `pages/student/ladder.html` + `scripts/ladder.js` + `styles/ladder.css` + `tests/ladder-smoke.js`（18 张表 / 8 个接口 / 50 条断言） | ✅ |
| 前端童趣 / 宇宙探索风 | `space.css` + `space.js`（星野 / 星球 / 彗星 / 双主题）+ 吉祥物 | ✅ |
| 智能体定名「小问」，接入设计稿里的各种形象状态 | `assets/images/mascot/xiaowen/*`（12 动图 + 22 静帧）+ `scripts/xiaowen.js`（30 态状态机） | ✅ |
| 语音对话：喊一声「小问」就应答 | `scripts/xiaowen.js` 的 `QWXiaowen.wake` + `agent.js` / `mascot.js` 的唤醒开关 | ✅ |
| 智能体对话框偏小、内容显示不全 | `space.css`（语音设置浮层 / 对话区保底高度 / 卡片高度公式）+ `agent.js`（语音设置开关、`has-msgs`） | ✅ |
| 智能体页上下选项条 / 工具条再压小，对话框拉长 | `space.css` 25.2 节（语音条从两行压到一行、对话区 245→331px） | ✅ |
| 错题订正页：错题本 | `scripts/correction.js` 的 `.qwe-item` 小卡 + `routes/learning.js` 补 `knowledgePointIds`（「去重讲 →」跳对应课堂） | ✅ |
| 错题订正页：错因雷达图 | `correction.js` 的 `causeRadar()` 自绘 SVG + `/api/learning/error-book` 新增 `causes`；样式在 `space.css` 25.6 节 | ✅ |
| 首页「知识星系」点星球跳讲题课堂 | `app.js:247` `.sq-planet` 本就是 `<a href="./classroom.html?kp=kpX">`，本轮核查无改动 | ✅ |
| VIP 权益对齐（三张方案卡文案与参考图逐条一致） | `services/vipService.js` 的 PLANS + `vip.js` 的配额条 | ✅ |
| 免费版每天 10 次 AI 对话，星尘 / 星钻可兑换加次 | `services/agentQuotaService.js` + `routes/agent.js`（`/quota`、`/quota/redeem`）+ `agent.js` 兑换卡 | ✅ |
| 免费版只开放第一颗星球（首页 + 答题复习页） | `services/accessService.js`（`FREE_PLANET_COUNT = 1`）+ `routes/dashboard.js` 下发 `locked` + `routes/quiz.js` 403 + `app.js` 锁卡 / 解锁面板 | ✅ |
| 智能体页去掉主动建议卡，对话框拉长 | `frontend/src/scripts/agent-page.js`（`suggest: false`） | ✅ |
| 星钻改等值充值（1 元 = 1 星钻）+ 收支记录 | `services/walletService.js` + `routes/wallet.js` + `db/schema.js`（`qw_crystal_order` / `qw_wallet_log`）+ `vip.js` 钱包块 | ✅ |
| 兑换价：50 星尘 = 1 次 / 2 星钻 = 1 次 | `backend/src/config/index.js` → `agentQuota.redeem` | ✅ |
| 答题星球页改为宇宙星系地图（前面的在前、后面的锁） | `scripts/app.js` 的 `renderPlanet()` + `space.css` 25.9 节 | ✅ |
| 等级界面：经验值 + 经验获取方式 + 各级名称与图标 | `scripts/app.js` 的 `renderLevel()` + `mockData.EXP_RULES` + `space.css` 25.10 节 | ✅ |
| 前端不依赖外部 CDN（内网 / 离线可用） | `frontend/assets/vendor/lucide.min.js` + `common.css` 的 `:root` 与 Tailwind 兼容层 + 12 个学生端页面去 CDN | ✅ |
| 启动时打印局域网访问地址 | `backend/src/app.js` 的 `lanAddresses()`（过滤虚拟网卡） | ✅ |
| Docker / 环境变量模板 / Git 忽略 | `Dockerfile`、`.dockerignore`、`backend/.env.example`、`.gitignore` | ✅ |
| 部署方案与上线自查文档 | `部署文档.md`（四条路线：局域网 / 内网穿透 / **云服务器＝路线二** / **免费托管＝路线一**）+ `render.yaml` + `fly.toml` + `docker-compose.yml` + `tools/deploy-server.sh` + `tools/pack-for-server.ps1` | ✅ |

---

## 3. 已知问题 / 待办

**已知问题**
- 本机外网受限时，智能体会在约 2 秒内自动降级到内置规则引擎（功能不受影响，`/api/health` 仍显示配置的 `deepseek-flash`）。
- 会员订单 / 会员成员初始为空表，需先用演示账号在「会员领航舱」下单支付才会出现数据。
- 数据仍在服务端内存，重启即回到初始状态（演示设定，不是缺陷）。
- 演示账号（林小明）的历史学习记录里包含内角和 / 勾股定理，而这两颗星球现在属于领航员权益：所以学习报告与五维雷达里会出现「锁住的星球」的数据。这是演示数据与新增权限规则的固有出入，接真实数据后自然消失。
- 9 个旧版页面（badge-popup / blackboard-classroom / checkin-points / error-correction / learning-result / level-system / quiz-planet / quiz-training / student-home）已归档到 `docs/legacy-pages/`（各附一条「被哪个新页面取代」的说明），移出静态托管目录后 URL 不再可达。
- 字体来自 `fonts.googleapis.cn`（`common.css` 顶部的 `@import`），无外网时浏览器自动回退到系统无衬线字体；中文思源黑体体积较大，未做本地化。
- 语音唤醒依赖浏览器 `SpeechRecognition`（Chrome / Edge 支持最好，且需要麦克风权限，点开开关时才会申请）；不支持时唤醒按钮自动隐藏、只保留键盘输入。
- `frontend/assets/images/mascot/xiaowen.svg` 与 `cheese-cat.svg` 已于本轮删除（`[System.IO.File]::Delete`）；`rocket.svg` 仍在用，保留。

**待办（对应 README 演进路线）**
1. 数据落库：内存表 → MySQL（`backend/src/db/schema.sql` 已就绪），持久层换 MyBatis。
2. 接真实 Redis：设 `CACHE_DRIVER=redis` + `REDIS_URL` 即可切换。
3. 后端换基座：Node + Express → Java + Spring Boot + 若依（接口契约与权限码不变）。
4. 前端组件化：原生多页 → Vue 3 + Vite（复用 `qw-*` / `sq-*` 类名）。
5. 管理端接入若依内置后台（一期原生控制台已可用，替换页面即可）。
6. 可选：MQTT 接入智能手写板 / 摄像头传感器。

---

## 4. 后续每天怎么记

复制下面模板追加到「1. 开发日志」末尾即可：

```markdown
### YYYY-MM-DD（第 N 天）当天主题

**当天需求**：一句话说清要解决什么。

**改动**

| 时间 | 改动 | 说明 |
|------|------|------|
| HH:MM | `文件路径` | 改了什么、为什么这么改 |

**验证**：跑了哪条命令 / 在哪个页面确认了什么；没验证的写进「待办」。
```

记录约定：
1. **只写真实改动**：文件路径 + 行为变化，不写「优化了一下」这类空话。
2. **写明原因**：尤其是修 bug，要写清「原来错在哪、为什么这样修」。
3. **写清验证**：命令或页面 + 结果；未验证的必须标注。
4. **需求变更也记**：例如「家长端并入学生端」这类方向调整要写进当天需求，避免以后看不懂为什么少了半个端。