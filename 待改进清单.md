# 待改进清单（全量）

> 对象：本项目 `wenshu-fullstack`
> 日期：2026-09-15
> 取证方式：全仓静态扫描 + 关键文件逐行阅读（111 个前端文件 + backend/src 全量）
> 分级：**P0 严重** → **P1 重要** → **P2 建议**

---

## 进度速览（2026-09-15 更新）

| 条目 | 状态 | 落地位置 |
|------|------|----------|
| P0-1 JWT 密钥硬编码兜底 | ✅ 已修 | `backend/src/utils/auth.js`：生产环境未配置 / 沿用开发默认值 / 短于 32 位一律 `throw` 拒绝启动 |
| P0-2 密码单轮 SHA-256 | ✅ 已修 | `backend/src/utils/auth.js`：换 `scryptSync(N=16384, r=8, p=1)`，新哈希带 `scrypt$` 前缀，老哈希校验通过后自动升级 |
| P0-3 token 可走 URL query | ✅ 已修 | `backend/src/utils/auth.js` 的 `readToken()` 只认 `Authorization: Bearer` |
| P0-4 无安全响应头 / `cors()` 全开 | ✅ 已修（CSP 未加） | `backend/src/app.js`：手写 5 个安全响应头；`cors()` 支持 `CORS_ORIGIN` 白名单。**CSP 仍留白**，等前端把内联脚本收干净再加 |
| P0-5 无 HTTPS | ⏳ 未落地 | `部署文档.md` 已写全流程；代码层没有加 301 跳转（本地开发必须走 http，所以只在生产分支判断） |
| P0-6 反代后限流失效 | ✅ 已修 | `backend/src/app.js`：`app.set('trust proxy', 1)` |
| P1-7 `frontend/dist/` 死代码 | ✅ 已删 | 整个 `frontend/dist/` 已移除 |
| P1-8 9 个孤儿页挂 CDN | ✅ 已处理 | 移到 `docs/legacy-pages/`（附取代说明），不再被静态托管 |
| P1-9 中文字体走 `fonts.googleapis.cn` | ⏳ 未落地 | `common.css` 顶部仍是 `@import`；`font-display: swap` 保证断网时优雅回退 |
| P1-10 测试残留文件 | ✅ 已删 | `_probe.txt` / `tmp-test-create.md` |
| P1-11 无 favicon / robots | ✅ 已补 | `frontend/assets/favicon.svg`（手绘表情星球）+ `frontend/robots.txt` |
| P1-12 语音只靠浏览器 TTS | ⏳ 未落地 | 仍是 `speechSynthesis` 单级；服务端 `/api/tts` 方案见 `同行方案借鉴分析.md` |
| P1-13 讲题步骤无高亮引导 | ✅ 已落地 | 新增 `frontend/src/scripts/tour.js`（遮罩挖洞 + 聚光灯 + 气泡 + 锁点击）；讲题课堂 6 步、首页 5 步 |
| P1-14 几何题无「数形结合」配图 | ✅ 已落地 | `space-art.js` 的 `sideDots()` / `pythagorasArt()`；`board.js` 的勾股图两直角边按数量打点（b=3 真的画 3 个点） |
| P2-15 无专注计时 / 护眼模式 | ✅ 已落地 | 白天 / 夜航双主题（`space.js` + `localStorage.qw_mode`）；**专注计时 `focus.js`**（10/15/20/25 分钟环形倒计时 + 到点提示条 + 语音 + 粒子）已嵌进首页「白天专注模式」卡片 |
| P2-16 雷达图无下限保护 | ✅ 本就已有 | `app.js` 的 `radarChart()` 已是 `Math.max(0.08, score/100)`，本轮核查无需改 |
| P2-17 宠物无互动 | ✅ 已落地 | `space-art.js` 的 `burst()` 粒子（8 方向飞散，`--dx` / `--dy` 控方向）；亲密度仍是展示态，没有接 `gamificationService` |
| P2-18 `cors()` 全开 | ✅ 已修 | 见 P0-4 |
| P2-19 无 gzip 压缩 | ✅ 已落地 | `backend/src/middleware/compress.js`（零依赖 zlib + ETag 压缩缓存）；实测 CSS 压到 18%–20% |
| P2-20 无错误边界 / 离线提示 | 🟡 部分 | `space.js` 的 `.sq-net-banner` 已做 `online` / `offline` 横幅 + 回到线上时探活 `/api/health`；**API 统一超时仍未做** |

**当前剩余**：P0-5（HTTPS）、P1-9（字体自托管）、P1-12（服务端 TTS）、P2-20（**API 统一超时**），以及 P0-4 里的 CSP。

---

## P0-1 安全：JWT 密钥有硬编码兜底

**证据**：`backend/src/utils/auth.js:5`

```js
const SECRET = process.env.JWT_SECRET || 'wenshu_dev_secret_key_2025';
```

**风险**：只要运维忘了配 `JWT_SECRET`，线上就用这个**写在代码里、人人都能在 GitHub 搜到**的密钥签发令牌。
任何人都可以自己伪造一个 token 冒充任意用户（包括运营 admin）。

**改法**：生产环境（`NODE_ENV === 'production'`）下若无 `JWT_SECRET` 就**直接抛错退出**，不给兜底：

```js
const SECRET = process.env.JWT_SECRET;
if (!SECRET || SECRET.length < 32) {
  if (process.env.NODE_ENV === 'production') {
    throw new Error('生产环境必须配置 JWT_SECRET（至少 32 位随机串）');
  }
  // 仅开发环境允许兜底
}
```

---

## P0-2 安全：密码哈希只有单轮 SHA-256

**证据**：`backend/src/utils/auth.js:40-42`

```js
function hashPassword(password, salt) {
  return crypto.createHash('sha256').update(String(password) + '::' + salt).digest('hex');
}
```

**风险**：单轮 SHA-256 是**为速度设计的**，GPU 每秒能算几十亿次。
数据库一旦泄露，弱口令秒破。正确做法是**带迭代次数的慢哈希**。

**改法**：改用 Node 内置的 `crypto.scryptSync`（无需新增依赖）：

```js
function hashPassword(password, salt) {
  return crypto.scryptSync(String(password), String(salt), 64, { N: 16384, r: 8, p: 1 }).toString('hex');
}
```

注意：**改完要兼容老密码**——登录时若老哈希校验通过，顺手把存储值升级成新哈希。

> ✅ 好的地方：`verifyPassword` 已经用了 `crypto.timingSafeEqual` 做常量时间比较，这点是对的。

---

## P0-3 安全：令牌可以走 URL 查询串传递

**证据**：`backend/src/utils/auth.js` 的 `readToken()`

```js
const header = req.headers.authorization || '';
if (header.indexOf('Bearer ') === 0) return header.slice(7).trim();
if (req.query && typeof req.query.token === 'string') return req.query.token;   // ← 这一行
return null;
```

**风险**：URL 上的 token 会进入 **Nginx access log、浏览器历史、Referer 头、CDN 日志**，
等于把登录凭证到处撒。而且 Nginx 日志通常是明文长期保存的。

**改法**：去掉 `req.query.token` 这条分支，只用 `Authorization` 头。
若确有"链接直达"需求，改用**一次性短时效的 ticket** 换取正式 token。

---

## P0-4 安全：没有任何安全响应头

**证据**：`backend/package.json` 依赖只有 4 个 —— `express` / `cors` / `dotenv` / `uuid`。
全仓搜索 `helmet`、`Content-Security-Policy` 均为 **0 处**。

**风险**：缺少 `X-Content-Type-Options`、`X-Frame-Options`、`CSP` 等，
站点可被 iframe 嵌套（点击劫持），也少了 XSS 的第二道防线。

**改法**：加 `helmet`（或手写几行 `res.setHeader`）：

```js
app.use(require('helmet')({ contentSecurityPolicy: false })); // CSP 先关，逐步收紧
```

注意：`cors()` 现在是**无参数全开**（`app.js:30`），任何人都能跨域调你的 API。
上线前应改成白名单：`cors({ origin: ['https://你的域名'] })`。

---

## P0-5 部署：没有 HTTPS（会连带废掉一批功能）

**证据**：`部署文档.md` 已提到，但代码层没有强制跳转。

**风险**（不是"看着不专业"，是**功能直接不可用**）：

| 依赖安全上下文的功能 | `http://` 下 | 原因 |
|---|---|---|
| 麦克风录音 `getUserMedia` | ❌ 不可用 | 浏览器要求 secure context |
| 语音识别 `SpeechRecognition` | ❌ 不可用 | 同上 |
| 剪贴板 `navigator.clipboard` | ❌ 不可用 | 同上 |
| Service Worker / PWA | ❌ 不可用 | 同上 |

所以你现有的语音功能**在 `http://` 部署下等于没有**，这一点必须让用户知道。

**改法**：域名 + Let's Encrypt，或自签证书 + `mkcert`。同时在 express 加：

```js
app.use((req, res, next) => {
  if (process.env.NODE_ENV === 'production' && req.headers['x-forwarded-proto'] === 'http') {
    return res.redirect(301, 'https://' + req.headers.host + req.url);
  }
  next();
});
```

---

## P0-6 部署：反代后限流失效（**最容易踩的坑**）

**证据**：`backend/src/middleware/rateLimit.js`

```js
function defaultKey(req) {
  if (req.user && req.user.id) return req.user.id;
  return req.ip || (req.connection && req.connection.remoteAddress) || 'anonymous';
}
```

**风险**：一上 Nginx，`req.ip` 会**全部变成 `127.0.0.1`**（因为是 Nginx 转发过来的）。
结果：**所有未登录用户共用同一个限流桶** —— 一个爬虫就能把全站访客限流掉，
而且"防暴力破解"完全失效。这是本项目上云后**第一个会炸的地方**。

**改法**：`backend/src/app.js` 里加一行（放在限流中间件之前）：

```js
app.set('trust proxy', 1);
```

并且确认 Nginx 配置里带了 `proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;`。

---

## P1-7 依赖：`frontend/dist/` 是过期构建产物，还挂着 CDN

**证据**：

| 目录 | unpkg | jsdelivr | 状态 |
|---|---|---|---|
| `frontend/dist/pages/student/*.html` | 56 处 | 19 处 | **过期产物，且已被覆盖成死代码** |
| `frontend/pages/*.html`（9 个孤儿页） | 有 | 有 | 可达，见 P1-8 |

`frontend/dist/pages/student/` 里有 `home.html`、`level.html` 等 10 个页面（8–12 KB），
但**同名文件在 `frontend/pages/student/` 下也存在**，而 `app.js:64` 先托管 `frontendDir`：

```js
app.use(express.static(config.frontendDir));                        // 64 行，先命中
app.use(express.static(path.join(config.frontendDir, 'dist')));     // 66 行，被覆盖
```

→ `dist/pages/**` **永远访问不到**，是纯死代码；但它里面那些老页面**还挂着 unpkg/jsdelivr 外链**，
一旦哪天静态目录顺序调换，断网用户立刻白屏。

**改法**：直接删掉 `frontend/dist/pages/`。
若 `dist/assets/`（`common-DGnLl7kN.css`、`nav-CUCugJJR.js`）也不再被引用，整个 `dist/` 一起删。
**并且明确静态托管只保留一层**，避免"哪份生效"靠顺序猜。

---

## P1-8 依赖：9 个孤儿页仍挂 CDN，且 URL 可直达

**证据**：`frontend/pages/` 下这 9 个页面（残留在根目录，不在 `student/` 里）：

```
badge-popup.html      blackboard-classroom.html   checkin-points.html
error-correction.html learning-result.html        level-system.html
quiz-planet.html      quiz-training.html          student-home.html
```

每个头部都挂着：

```html
<link rel="preconnect" href="https://unpkg.com" />
<script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
<script src="https://unpkg.com/@fortawesome/fontawesome-free@6.7.2/js/all.min.js"></script>
<script src="https://unpkg.com/lucide@latest"></script>          <!-- ← 还用了 latest -->
<link rel="stylesheet" href="https://fonts.googleapis.cn/css2?..." />
```

**风险**：虽然全仓 0 引用，但它们是**可通过 `/pages/badge-popup.html` 直接访问**的。
断网/校园网屏蔽 unpkg 时打开就是满屏裸样式；而且 `lucide@latest`
意味着**上游一发新版，你的页面可能当天就崩**（无版本锁定）。

**改法**：三选一 ——
1. 确认为废弃 → 删掉这 9 个文件（最干净）；
2. 还有用 → 把 CDN 换成已自托管的 `frontend/assets/vendor/lucide.min.js` + 去掉 Tailwind CDN；
3. 至少先把 `lucide@latest` 钉死成具体版本号。

---

## P1-9 依赖：中文字体走 `fonts.googleapis.cn`（共 11 处）

**证据**：全仓 11 处，分布在 `frontend/pages/*.html` 和 `frontend/dist/assets/common-DGnLl7kN.css`。

**风险**：Google Fonts 中文镜像在**国内网络环境下经常不可达**
（第 13 轮实测：`fonts.googleapis.cn` 的 TLS 握手直接失败）。
虽然 `font-display: swap` 会让它优雅回退到系统字体，但首屏会有一次无谓的 DNS + TLS 阻塞。

**改法**：把用到的字体（`Noto Sans SC` 子集）自托管到 `frontend/assets/fonts/`，
用本地 `@font-face`。或者干脆用系统字体栈（`PingFang SC` / `Microsoft YaHei`），
零请求、零闪烁 —— 对一个中文教学产品，**这是更划算的选择**。

---

## P1-10 工程卫生：测试残留文件

**证据**：

| 文件 | 大小 | 内容 |
|---|---|---|
| `backend/src/models/_probe.txt` | 6 B | `hello` |
| `tmp-test-create.md` | 1 B | `x` |

**改法**：删掉。这类文件留在 `src/` 里会让人以为是正式资产。

---

## P1-11 工程卫生：无 favicon / robots.txt / manifest

**证据**：全仓搜索 `favicon*`、`robots.txt`、`manifest.json` → **0 个文件**。

**影响**：
- 浏览器每个页面都发一次 `/favicon.ico` → **404**（控制台常年报错，不美观）
- 无法被搜索引擎正确索引 / 无法"添加到主屏幕"

**改法**：加 `frontend/favicon.ico`（或 `<link rel="icon" href="data:,">` 先消掉 404），
再补一个 `robots.txt`。

---

## P1-12 体验：语音只依赖浏览器内置 TTS

**证据**：`backend` 无 `/api/tts` 路由（搜索 `tts` **0 处**）；
`frontend/src/scripts/voice.js` 只用了 `speechSynthesis`（3 处）。

**风险**：用户电脑**没装中文语音包**、或用的是精简版系统，小问就是**哑的**，且没有任何提示。
这在一个"语音对话"是核心卖点的产品里是硬伤。

**改法**（借鉴同行，详见 `同行方案借鉴分析.md`）：加服务端 `/api/tts` + 三级降级：

```
服务端 MP3（预取缓存） → WebAudio decodeAudioData → speechSynthesis 兜底
```

配套两个细节：**预取**（进页面前 `fetch` 成 `ArrayBuffer`，播出零延迟）和
**token 自增比对**（解决"快速连点后旧语音回调覆盖新状态"的竞态）。

---

## P1-13 体验：讲题步骤只有文字，没有引导

**证据**：讲题相关逻辑在 `frontend/src/scripts/board.js`（24 KB）/ `classroom.js`（21 KB），
但**没有遮罩高亮 + 气泡的引导机制**。

**改法**：抄同行的 `STEPS=[{sel, text, clickable}]` + overlay/bubble：
非当前步骤 `pointerEvents='none'` 锁死强制按顺序；气泡算 `bottom+14` 超屏自动上翻；
`sessionStorage` 记住已看过不再弹。

---

## P1-14 体验：几何题没有"数形结合"配图

**证据**：无按数量画点的作图逻辑。

**改法**：`svgSideDots()` —— a 边画 a 个点、b 边画 b 个点，
配合"两块小正方形星尘倒进斜边大正方形"的动画隐喻讲 `a²+b²=c²`。
比你原本设想的"调文生图模型"**更稳定、零成本、且保证数学正确**。

---

## P2-15 体验：无专注计时 / 护眼模式

**现状**：`space.css` 有宇宙主题，但没有"今日专注 N 分钟"+ 到点提醒 + 夜航模式。

**价值**：面向 7–14 岁 + 家长付费，**防沉迷和护眼是最能打动家长的功能点**，
且实现成本极低（纯前端 + `localStorage`/`sessionStorage`）。
同行做得挺完整：10/15/20/25 分钟可选、进度条、`toast` + **语音双通道**提醒。

---

## P2-16 体验：雷达图缺下限保护

**改法**：算顶点时加 `Math.max(0.12, min(1, score/100))`。
否则某项得 0 分时，图形会**塌成一个点**，视觉上像是"图坏了"。

---

## P2-17 体验：宠物是静态形象，没有互动

**现状**：`frontend/src/scripts/xiaowen.js`（15 KB）+ `mascot.js`（8 KB）有小问形象，
但评论区提醒：**同行那边的亲密度是空壳**（`getAffection()` 直接 `return 0`）。
你抄的时候要**接到真实的 `gamificationService`**，否则就是抄了个假功能。

**可抄**：点击宠物冒 `+1`/`✦` 粒子（8 方向飞散，CSS keyframes 用 `--dx/--dy` 变量控方向）
+ 6 句鼓励文案轮播。

---

## P2-18 工程：`cors()` 全开

**证据**：`backend/src/app.js:30` —— `app.use(cors());`（无参数 = 允许任何来源）

**改法**：上线前收成白名单。注意别把 `credentials` 和 `origin: '*'` 混用（浏览器会直接拒绝）。

---

## P2-19 工程：无 gzip 压缩

**证据**：`backend/package.json` 无 `compression` 依赖。

**影响**：`common.css` 97 KB + `space.css` 146 KB + `app.js` 59 KB 全是**裸传**。
开 gzip 后 CSS/JS 通常能压到 **20–30%**，首屏提升明显。

**改法**：`app.use(require('compression')())`，或在 Nginx 开 `gzip on;`（后者更省 Node 的 CPU）。

---

## P2-20 工程：无错误边界 / 离线提示

**现状**：切到没有网络的教室网络时，页面只会一直转圈。

**改法**：给 API 调用加统一的超时 + 失败兜底 toast；监听 `online`/`offline` 事件给个横幅提示。

---

## 汇总表

| # | 级别 | 类别 | 问题 | 关键位置 |
|---|---|---|---|---|
| 1 | **P0** | 安全 | JWT 密钥硬编码兜底 | `backend/src/utils/auth.js:5` |
| 2 | **P0** | 安全 | 密码哈希单轮 SHA-256 | `backend/src/utils/auth.js:40` |
| 3 | **P0** | 安全 | token 可走 URL query | `backend/src/utils/auth.js` readToken |
| 4 | **P0** | 安全 | 无 helmet / CSP；`cors()` 全开 | `backend/src/app.js:30` |
| 5 | **P0** | 部署 | 无 HTTPS（语音/麦克风全废） | `部署文档.md` |
| 6 | **P0** | 部署 | 无 `trust proxy` → 限流上线即失效 | `backend/src/middleware/rateLimit.js` |
| 7 | P1 | 依赖 | `frontend/dist/` 过期产物挂 CDN + 死代码 | `frontend/dist/` |
| 8 | P1 | 依赖 | 9 个孤儿页挂 CDN 且可直达；`lucide@latest` | `frontend/pages/*.html` |
| 9 | P1 | 依赖 | 中文字体走 `fonts.googleapis.cn`（11 处） | `frontend/pages/`, `dist/assets/` |
| 10 | P1 | 卫生 | 测试残留 `_probe.txt` / `tmp-test-create.md` | `backend/src/models/`, 根目录 |
| 11 | P1 | 卫生 | 无 favicon / robots / manifest | — |
| 12 | P1 | 体验 | 语音仅浏览器内置 TTS（无服务端兜底） | `frontend/src/scripts/voice.js` |
| 13 | P1 | 体验 | 讲题步骤无高亮引导 | `frontend/src/scripts/board.js` |
| 14 | P1 | 体验 | 几何题无"数形结合"配图 | — |
| 15 | P2 | 体验 | 无专注计时 / 护眼模式 | `frontend/src/scripts/focus.js` + `frontend/src/styles/space.css` |
| 16 | P2 | 体验 | 雷达图无下限保护 | `frontend/src/scripts/report.js` |
| 17 | P2 | 体验 | 宠物无互动、亲密度未做 | `frontend/src/scripts/xiaowen.js` |
| 18 | P2 | 工程 | `cors()` 全开 | `backend/src/app.js:30` |
| 19 | P2 | 工程 | 无 gzip 压缩 | `backend/package.json` |
| 20 | P2 | 工程 | 无错误边界 / 离线提示 | — |

---

## 建议的执行顺序

**第一批（上线前必须做，0.5–1 天）**
- P0-1 JWT 密钥强制校验
- P0-2 换 scrypt 哈希（带老密码平滑升级）
- P0-6 `trust proxy` + 确认 Nginx 的 `X-Forwarded-For`
- P1-10 删残留文件、P1-11 补 favicon

**第二批（干净度，0.5 天）**
- P1-7 删 `frontend/dist/`
- P1-8 处理 9 个孤儿页（删或去 CDN）
- P1-9 字体自托管或改系统字体栈

**第三批（分数提升最大，2–3 天）**
- P1-12 服务端 TTS + 三级降级 + 预取 + 竞态 token
- P1-13 讲题步骤高亮引导
- P1-14 数形结合作图

**第四批（打磨）**
- P2-15 / 16 / 17 体验项
- P2-18 / 19 / 20 工程项
- P0-3 / P0-4 / P0-5 安全与 HTTPS 收口

---

## 已知做得好的地方（不用改）

- ✅ `verifyPassword` 用了 `crypto.timingSafeEqual`（防时序攻击）
- ✅ 有 `rateLimit` 中间件且计数放在缓存层（多实例天然共享）
- ✅ 有 `cache.isRevoked(token)` 做登出/踢下线
- ✅ 分层清晰：routes / services / repositories / middleware / agent
- ✅ Agent 有 12 个 tool + guardrail + blocklist + memory + persona
- ✅ 12 个测试文件，`node tests/run-all.js` 11/11 全绿
- ✅ `frontend/index.html` 与 `pages/student/*.html` 已完成去 CDN 化，全部指向本地 vendor
- ✅ `viewport` 用的是 `width=device-width`（比同行的写死 1280 正确）